(self.webpackChunk_N_E=self.webpackChunk_N_E||[]).push([[6],{4032:(n,e,t)=>{Promise.resolve().then(t.bind(t,5016))},5016:(n,e,t)=>{"use strict";t.d(e,{default:()=>M});var o=t(5155),s=t(2619),i=t.n(s),r=t(5921),l=t(7937),a=t(9332),d=t(8150),c=t(5626),p=t(2987),g=t(6549);let u={c01:{content:"\n<h2>启动引擎概述</h2>\n<blockquote>Claude Code 的启动过程是一个精心编排的性能优化管道，通过并行预取和延迟初始化将冷启动时间压缩到毫秒级。</blockquote>\n\n<h3>入口文件：main.tsx</h3>\n<p>整个应用的入口点是 <code>src/main.tsx</code>，这是一个约 800 行的文件，使用 Commander.js 解析 CLI 参数并编排整个启动过程。最关键的设计决策是：<strong>在模块导入之前就发起并行预取</strong>。</p>\n\n<p>启动的第一行代码不是 import 语句，而是三个性能关键的副作用调用：</p>\n<ol>\n<li><code>profileCheckpoint('main_tsx_entry')</code> — 标记入口时间戳，用于启动性能追踪</li>\n<li><code>startMdmRawRead()</code> — 启动 MDM（移动设备管理）子进程，通过 <code>plutil</code>（macOS）或注册表查询（Windows）读取企业策略配置</li>\n<li><code>startKeychainPrefetch()</code> — 启动 macOS Keychain 读取，预加载 OAuth token 等安全凭据</li>\n</ol>\n\n<p>这三个调用会在 import 阶段（约 135ms）与模块加载<strong>并行执行</strong>，从而将总启动时间减少约 100-200ms。</p>\n\n<h3>初始化管道：init.ts</h3>\n<p><code>src/entrypoints/init.ts</code> 导出一个<strong>记忆化的</strong> <code>init()</code> 函数，确保只执行一次。初始化管道的顺序经过精心设计：</p>\n\n<ol>\n<li><strong>enableConfigs()</strong> — 验证并启用 JSON 配置文件</li>\n<li><strong>applySafeConfigEnvironmentVariables()</strong> — 在建立信任之前应用安全的环境变量</li>\n<li><strong>applyExtraCACertsFromConfig()</strong> — 加载额外的 CA 证书（必须在首次 TLS 握手之前！）</li>\n<li><strong>setupGracefulShutdown()</strong> — 注册进程清理处理程序（SIGINT/SIGTERM）</li>\n<li><strong>并行异步任务</strong> — 1P 事件日志、OAuth 填充、JetBrains 检测、仓库检测</li>\n<li><strong>configureGlobalMTLS()</strong> — 配置双向 TLS 认证</li>\n<li><strong>configureGlobalAgents()</strong> — 配置 HTTP/HTTPS 代理</li>\n<li><strong>preconnectAnthropicApi()</strong> — TCP+TLS 预热连接，节省 100-200ms 首次 API 调用延迟</li>\n</ol>\n\n<h3>REPL 启动器：replLauncher.tsx</h3>\n<p>REPL 启动器是一个精简的中间层，负责动态导入 <code>App</code> 和 <code>REPL</code> 组件：</p>\n<pre><code class=\"language-typescript\">const { App } = await import('./components/App.js')\nconst { REPL } = await import('./screens/REPL.js')\nawait renderAndRun(root, &lt;App {...appProps}&gt;&lt;REPL {...replProps} /&gt;&lt;/App&gt;)</code></pre>\n\n<p>动态导入确保了 UI 组件的代码只在交互模式下加载，<code>--print</code>（headless）模式完全跳过 UI 初始化。</p>\n\n<h3>迁移系统</h3>\n<p>Claude Code 维护了一个编号迁移系统（当前版本 11），处理配置 schema 演进：模型名称重命名、设置路径迁移、数据结构升级。<code>runMigrations()</code> 在 init() 之后同步执行，确保后续代码始终面对最新的数据结构。</p>\n\n<h3>延迟预取策略</h3>\n<p><code>startDeferredPrefetches()</code> 在首次渲染之后触发，包含不影响首屏的初始化任务：</p>\n<ul>\n<li><code>initUser()</code> — 用户身份初始化</li>\n<li><code>getUserContext()</code> — 用户上下文信息</li>\n<li>提示信息加载、文件计数、模型能力检测</li>\n<li>变更检测器启动</li>\n</ul>\n",architecture:'\n<h2>启动管道架构</h2>\n\n<h3>时序图</h3>\n<div style="overflow-x:auto">\n<table>\n<thead><tr><th>阶段</th><th>时间</th><th>执行内容</th><th>并行性</th></tr></thead>\n<tbody>\n<tr><td>T0</td><td>0ms</td><td>profileCheckpoint</td><td>—</td></tr>\n<tr><td>T1</td><td>0-1ms</td><td>startMdmRawRead + startKeychainPrefetch</td><td>并行启动子进程</td></tr>\n<tr><td>T2</td><td>1-135ms</td><td>200+ 模块导入</td><td>与 T1 的子进程并行</td></tr>\n<tr><td>T3</td><td>135-140ms</td><td>runMigrations()</td><td>同步</td></tr>\n<tr><td>T4</td><td>140-160ms</td><td>init() 管道</td><td>内部有并行步骤</td></tr>\n<tr><td>T5</td><td>160-180ms</td><td>CLI 解析 + 认证检查</td><td>同步</td></tr>\n<tr><td>T6</td><td>180-200ms</td><td>launchRepl() / runHeadless()</td><td>—</td></tr>\n<tr><td>T7</td><td>200ms+</td><td>startDeferredPrefetches()</td><td>后台异步</td></tr>\n</tbody>\n</table>\n</div>\n\n<h3>模块依赖关系</h3>\n<p>启动引擎涉及以下核心模块：</p>\n<ul>\n<li><strong>main.tsx</strong> → 编排整个启动流程</li>\n<li><strong>entrypoints/init.ts</strong> → 核心初始化（网络、安全、清理）</li>\n<li><strong>replLauncher.tsx</strong> → REPL 模式启动器</li>\n<li><strong>entrypoints/cli.tsx</strong> → SDK/编程模式入口</li>\n<li><strong>migrations/</strong> → 11 个编号迁移处理器</li>\n<li><strong>bootstrap/state.ts</strong> → 全局单例状态初始化</li>\n</ul>\n\n<h3>设计决策</h3>\n<blockquote>\n<p><strong>为什么在 import 之前执行副作用？</strong></p>\n<p>Node.js/Bun 的模块加载是同步阻塞的。在 200+ 个模块的导入过程中（约 135ms），CPU 主要在做语法解析和模块求值，网络和子进程 I/O 处于空闲状态。通过在 import 之前启动子进程和网络请求，这段 I/O 等待时间被完全隐藏在模块加载时间内。</p>\n</blockquote>\n',codeExamples:[{title:"main.tsx — 启动序列（简化）",language:"typescript",code:"// 性能关键：在 import 之前启动并行预取\nprofileCheckpoint('main_tsx_entry')\nstartMdmRawRead()       // 启动 MDM 子进程（plutil/reg query）\nstartKeychainPrefetch() // 启动 macOS Keychain 读取\n\n// 200+ 个模块导入（~135ms，与上述子进程并行）\nimport { Command } from 'commander'\nimport { init } from './entrypoints/init.js'\nimport { launchRepl } from './replLauncher.js'\n// ... 200+ more imports\n\n// 同步迁移\nawait runMigrations()\n\n// 核心初始化\nawait init()\n\n// CLI 解析\nconst program = new Command()\n  .name('claude')\n  .version(VERSION)\n  .option('--print', 'headless mode')\n  .option('--model <model>', 'model override')\n  // ... more options\n\n// 分支：交互 vs headless\nif (options.print) {\n  await runHeadless(options)\n} else {\n  await launchRepl(options)\n}"},{title:"entrypoints/init.ts — 初始化管道",language:"typescript",code:"// 记忆化确保只执行一次\nexport const init = memoize(async () => {\n  // 1. 配置验证\n  enableConfigs()\n\n  // 2. 安全环境变量（信任建立前）\n  applySafeConfigEnvironmentVariables()\n\n  // 3. TLS 证书（必须在首次握手前！）\n  applyExtraCACertsFromConfig()\n\n  // 4. 优雅关闭\n  setupGracefulShutdown()\n\n  // 5. 并行的后台任务（fire-and-forget）\n  void initializeFirstPartyEventLogging()\n  void populateOAuthTokenCache()\n  void detectJetBrains()\n\n  // 6. 网络配置\n  configureGlobalMTLS()\n  configureGlobalAgents()    // HTTP/HTTPS 代理\n\n  // 7. API 预连接（TCP+TLS 预热）\n  preconnectAnthropicApi()   // 节省 100-200ms\n\n  // 8. 平台适配\n  setShellIfWindows()\n\n  // 9. 注册清理处理程序\n  registerCleanup('lsp', () => LSPServerManager.shutdown())\n  registerCleanup('teams', () => cleanupTeams())\n})"},{title:"replLauncher.tsx — REPL 启动",language:"typescript",code:"export async function launchRepl(options: LaunchOptions) {\n  // 动态导入 UI 组件（headless 模式不会触发）\n  const { App } = await import('./components/App.js')\n  const { REPL } = await import('./screens/REPL.js')\n\n  const root = createRoot()\n\n  await renderAndRun(root,\n    <App\n      model={options.model}\n      tools={options.tools}\n      permissionMode={options.permissionMode}\n    >\n      <REPL\n        initialPrompt={options.prompt}\n        resumeSession={options.resume}\n      />\n    </App>\n  )\n}"}],interactive:"\n<h2>启动流程互动解析</h2>\n<h3>第 1 步：预取启动</h3>\n<p>在任何模块被导入之前，系统就开始了两个关键的 I/O 操作。这就像你在等电梯的时候先打开手机查看今天的行程——利用等待时间做有用的事。</p>\n\n<h3>第 2 步：模块加载</h3>\n<p>200+ 个 TypeScript 模块被同步加载。虽然这需要约 135ms，但此时 MDM 子进程和 Keychain 读取已经在后台运行。</p>\n\n<h3>第 3 步：初始化管道</h3>\n<p>init() 函数按严格顺序执行 8 个步骤，每个步骤都有明确的依赖原因。例如 CA 证书必须在任何 HTTPS 请求之前加载。</p>\n\n<h3>第 4 步：分支决策</h3>\n<p>根据 CLI 参数决定进入交互式 REPL 还是 headless 执行模式。headless 模式完全跳过 UI 相关代码的加载。</p>\n\n<h3>关键性能指标</h3>\n<ul>\n<li><strong>并行预取节省</strong>：~100-200ms</li>\n<li><strong>API 预连接节省</strong>：~100-200ms 首 token 延迟</li>\n<li><strong>延迟预取</strong>：不影响首屏渲染的任务推迟到渲染后</li>\n</ul>\n"},c02:{content:'\n<h2>配置系统概述</h2>\n<blockquote>Claude Code 拥有业界罕见的 7 层配置优先级体系，从 CLI 标志到全局默认值，每一层都有明确的覆盖语义。配置系统不仅支持个人开发者的灵活定制，还通过 MDM 企业策略实现了大规模部署管控。</blockquote>\n\n<h3>7 层配置优先级</h3>\n<p>配置的合并遵循<strong>高优先级覆盖低优先级</strong>的原则，从高到低依次为：</p>\n<ol>\n<li><strong>CLI flags</strong> — 命令行参数（如 <code>--model opus</code>），拥有最高优先级</li>\n<li><strong>Policy / MDM</strong> — 企业策略配置，通过 macOS <code>plutil</code> 或 Windows 注册表读取，不可被用户覆盖</li>\n<li><strong>Remote managed</strong> — 远程托管配置，来自服务器下发的策略</li>\n<li><strong>Flag file</strong> — 功能标志文件，用于 A/B 测试和灰度发布</li>\n<li><strong>Project</strong> — 项目级配置 <code>.claude/settings.json</code>，存储在仓库根目录</li>\n<li><strong>User</strong> — 用户级配置 <code>~/.claude/settings.json</code>，跨项目生效</li>\n<li><strong>Global</strong> — 全局默认配置 <code>~/.claude.json</code>，最低优先级</li>\n</ol>\n\n<h3>getInitialSettings() 合并逻辑</h3>\n<p><code>getInitialSettings()</code> 是配置系统的核心入口函数。它按优先级顺序读取所有配置源，使用深度合并（deep merge）策略将它们组合成最终的配置对象。关键设计：</p>\n<ul>\n<li>数组类型的配置项（如 <code>allowedTools</code>）使用<strong>合并</strong>而非覆盖</li>\n<li>对象类型的配置项（如 <code>mcpServers</code>）使用<strong>深度合并</strong></li>\n<li>标量值（如 <code>model</code>）使用<strong>高优先级覆盖</strong></li>\n<li>MDM 策略中的某些字段具有<strong>强制性</strong>，即使 CLI 也无法覆盖</li>\n</ul>\n\n<h3>MDM 企业策略</h3>\n<p>MDM（Mobile Device Management）是 Claude Code 支持企业部署的关键特性。在启动阶段，<code>startMdmRawRead()</code> 会异步启动一个子进程来读取企业策略：</p>\n<ul>\n<li><strong>macOS</strong>：通过 <code>plutil -extract ... -o - /Library/Managed Preferences/com.anthropic.claude-code.plist</code> 读取托管首选项</li>\n<li><strong>Windows</strong>：通过注册表查询 <code>HKLM\\SOFTWARE\\Policies\\Anthropic\\ClaudeCode</code></li>\n<li>MDM 策略可以强制锁定模型选择、禁用特定工具、配置代理服务器等</li>\n</ul>\n\n<h3>Zod v4 Schema 验证</h3>\n<p>所有配置文件在加载时都会通过 Zod v4 schema 进行严格验证。<code>SettingsJson</code> 类型定义了配置的完整结构：</p>\n<ul>\n<li><code>permissions</code> — 权限模式设置</li>\n<li><code>allowedTools</code> — 允许使用的工具白名单</li>\n<li><code>denyTools</code> — 禁止使用的工具黑名单</li>\n<li><code>mcpServers</code> — MCP 服务器配置映射</li>\n<li><code>hooks</code> — 生命周期钩子配置</li>\n<li><code>model</code> — 默认模型设置</li>\n<li><code>customApiKeyResponses</code> — 自定义 API 密钥行为</li>\n</ul>\n<p>验证失败时会给出详细的错误信息并回退到默认值，避免因配置错误导致应用崩溃。</p>\n\n<h3>配置变更检测</h3>\n<p><code>changeDetector.ts</code> 实现了文件系统监控，使用 <code>fs.watch</code> 监听配置文件变更。当用户在编辑器中修改 <code>.claude/settings.json</code> 时，系统能在毫秒级感知变更并自动重新加载配置，无需重启 Claude Code。</p>\n\n<h3>环境变量处理</h3>\n<p><code>applySafeConfigEnvironmentVariables()</code> 在信任链建立之前执行，仅应用被标记为"安全"的环境变量。这个设计避免了恶意项目通过 <code>.env</code> 文件注入危险的环境变量。</p>\n\n<h3>settingsCache 性能优化</h3>\n<p>由于配置读取在热路径上频繁调用（每次工具调用都需要检查权限），系统使用 <code>settingsCache</code> 缓存解析后的配置对象。缓存在检测到文件变更时自动失效，确保一致性与性能的平衡。</p>\n',architecture:'\n<h2>配置系统架构</h2>\n\n<h3>配置合并流程</h3>\n<div style="overflow-x:auto">\n<table>\n<thead><tr><th>优先级</th><th>配置源</th><th>读取方式</th><th>合并策略</th></tr></thead>\n<tbody>\n<tr><td>1 (最高)</td><td>CLI flags</td><td>Commander.js 解析</td><td>直接覆盖</td></tr>\n<tr><td>2</td><td>MDM Policy</td><td>plutil / reg query 子进程</td><td>强制覆盖（部分字段）</td></tr>\n<tr><td>3</td><td>Remote managed</td><td>HTTP API 请求</td><td>深度合并</td></tr>\n<tr><td>4</td><td>Flag file</td><td>文件系统读取</td><td>深度合并</td></tr>\n<tr><td>5</td><td>Project settings</td><td>.claude/settings.json</td><td>深度合并</td></tr>\n<tr><td>6</td><td>User settings</td><td>~/.claude/settings.json</td><td>深度合并</td></tr>\n<tr><td>7 (最低)</td><td>Global defaults</td><td>~/.claude.json</td><td>基础值</td></tr>\n</tbody>\n</table>\n</div>\n\n<h3>核心模块关系</h3>\n<ul>\n<li><strong>utils/settings/</strong> — 配置系统主目录\n  <ul>\n  <li><code>settings.ts</code> — getInitialSettings(), getCurrentSettings()</li>\n  <li><code>config.ts</code> — enableConfigs(), 配置文件路径解析</li>\n  <li><code>changeDetector.ts</code> — 文件监控与缓存失效</li>\n  <li><code>settingsCache.ts</code> — 解析结果缓存</li>\n  </ul>\n</li>\n<li><strong>schemas/</strong> — Zod v4 验证 schema\n  <ul>\n  <li><code>SettingsJsonSchema</code> — 主配置 schema</li>\n  <li><code>McpServerSchema</code> — MCP 服务器配置 schema</li>\n  <li><code>HooksSchema</code> — 钩子配置 schema</li>\n  </ul>\n</li>\n<li><strong>mdm.ts</strong> — MDM 企业策略读取与解析</li>\n</ul>\n\n<h3>设计决策</h3>\n<blockquote>\n<p><strong>为什么用 7 层而不是简单的覆盖？</strong></p>\n<p>Claude Code 需要同时满足个人开发者和企业 IT 管理员的需求。个人开发者需要项目级和用户级的灵活配置；企业 IT 需要通过 MDM 策略强制执行安全策略（如禁止使用某些工具、锁定模型选择）。7 层体系让这两种需求和谐共存，MDM 策略可以覆盖用户设置，但 CLI 标志仍然在开发/调试时拥有最高优先级。</p>\n</blockquote>\n',codeExamples:[{title:"7 层配置合并 — getInitialSettings()",language:"typescript",code:"// utils/settings/settings.ts — 简化版\nexport function getInitialSettings(cliFlags?: Partial<SettingsJson>): SettingsJson {\n  // 第 7 层：全局默认值\n  const globalDefaults = loadJsonSafe<SettingsJson>(\n    path.join(os.homedir(), '.claude.json'),\n    SettingsJsonSchema\n  )\n\n  // 第 6 层：用户级配置\n  const userSettings = loadJsonSafe<SettingsJson>(\n    path.join(os.homedir(), '.claude', 'settings.json'),\n    SettingsJsonSchema\n  )\n\n  // 第 5 层：项目级配置\n  const projectSettings = loadJsonSafe<SettingsJson>(\n    path.join(findProjectRoot(), '.claude', 'settings.json'),\n    SettingsJsonSchema\n  )\n\n  // 第 4 层：Flag file\n  const flagFileSettings = loadFlagFileSettings()\n\n  // 第 3 层：远程托管配置\n  const remoteSettings = getRemoteManagedSettings()\n\n  // 第 2 层：MDM 企业策略\n  const mdmPolicy = getMdmSettings()\n\n  // 深度合并，高优先级覆盖低优先级\n  let merged = deepMerge(\n    globalDefaults,\n    userSettings,\n    projectSettings,\n    flagFileSettings,\n    remoteSettings\n  )\n\n  // MDM 策略的强制字段不可覆盖\n  merged = applyMdmEnforcement(merged, mdmPolicy)\n\n  // 第 1 层：CLI 标志（最高优先级）\n  if (cliFlags) {\n    merged = deepMerge(merged, cliFlags)\n  }\n\n  return merged\n}"},{title:"MDM 企业策略读取",language:"typescript",code:"// mdm.ts — macOS 通过 plutil 读取托管首选项\nlet mdmRawReadPromise: Promise<string> | null = null\n\nexport function startMdmRawRead(): void {\n  if (process.platform === 'darwin') {\n    // 异步启动 plutil 子进程（与模块导入并行）\n    mdmRawReadPromise = execAsync(\n      'plutil -extract . json -o - ' +\n      '/Library/Managed\\ Preferences/com.anthropic.claude-code.plist'\n    ).then(({ stdout }) => stdout)\n     .catch(() => '{}')  // 无 MDM 策略时返回空对象\n  } else if (process.platform === 'win32') {\n    // Windows 通过注册表读取\n    mdmRawReadPromise = execAsync(\n      'reg query \"HKLM\\\\SOFTWARE\\\\Policies\\\\Anthropic\\\\ClaudeCode\" /s'\n    ).then(({ stdout }) => parseRegistryOutput(stdout))\n     .catch(() => '{}')\n  }\n}\n\nexport async function getMdmSettings(): Promise<Partial<SettingsJson>> {\n  if (!mdmRawReadPromise) return {}\n  const raw = await mdmRawReadPromise\n  const parsed = JSON.parse(raw)\n  // 通过 Zod 验证 MDM 策略格式\n  return MdmPolicySchema.safeParse(parsed).data ?? {}\n}"},{title:"Zod v4 Schema 验证与 SettingsJson 类型",language:"typescript",code:"// schemas/settingsSchema.ts\nimport { z } from 'zod/v4'\n\nconst McpServerSchema = z.object({\n  command: z.string(),\n  args: z.array(z.string()).optional(),\n  env: z.record(z.string()).optional(),\n  type: z.enum(['stdio', 'sse']).default('stdio'),\n})\n\nexport const SettingsJsonSchema = z.object({\n  // 权限控制\n  permissions: z.object({\n    allow: z.array(z.string()).default([]),\n    deny: z.array(z.string()).default([]),\n  }).optional(),\n\n  // 工具白名单 / 黑名单\n  allowedTools: z.array(z.string()).optional(),\n  denyTools: z.array(z.string()).optional(),\n\n  // MCP 服务器配置\n  mcpServers: z.record(McpServerSchema).optional(),\n\n  // 生命周期钩子\n  hooks: z.record(z.object({\n    command: z.string(),\n    timeout: z.number().default(30000),\n  })).optional(),\n\n  // 模型设置\n  model: z.string().optional(),\n  maxThinkingTokens: z.number().optional(),\n\n  // 自定义 API 密钥\n  customApiKeyResponses: z.record(z.string()).optional(),\n}).passthrough()  // 允许未知字段，向前兼容\n\nexport type SettingsJson = z.infer<typeof SettingsJsonSchema>\n\n// 安全加载配置文件\nfunction loadJsonSafe<T>(filePath: string, schema: z.ZodType<T>): T {\n  try {\n    const raw = fs.readFileSync(filePath, 'utf-8')\n    const parsed = JSON.parse(raw)\n    const result = schema.safeParse(parsed)\n    if (result.success) return result.data\n    // 验证失败：记录警告并返回默认值\n    logWarning('Config validation failed:', result.error.issues)\n    return schema.parse({})  // 返回默认值\n  } catch {\n    return schema.parse({})  // 文件不存在或格式错误\n  }\n}"}],interactive:'\n<h2>配置系统互动解析</h2>\n\n<h3>第 1 步：理解优先级</h3>\n<p>想象一个场景：你在公司使用 Claude Code。IT 部门通过 MDM 策略禁止了 <code>Bash</code> 工具的使用。你在项目的 <code>.claude/settings.json</code> 中配置了 <code>"allowedTools": ["Bash"]</code>，然后用 <code>--allowedTools Bash</code> 启动。最终 Bash 能用吗？</p>\n<p><strong>答案：不能。</strong>MDM 策略的 <code>denyTools</code> 是强制性的，即使 CLI 标志也无法覆盖它。这就是第 2 层（MDM）对某些字段具有"强制覆盖"语义的体现。</p>\n\n<h3>第 2 步：深度合并如何工作</h3>\n<p>假设用户级配置定义了 <code>mcpServers: { "github": { command: "gh-mcp" } }</code>，项目级配置定义了 <code>mcpServers: { "jira": { command: "jira-mcp" } }</code>。合并结果是什么？</p>\n<p><strong>答案：</strong>两个 MCP 服务器都会存在，因为对象类型使用深度合并。最终的 <code>mcpServers</code> 会同时包含 <code>github</code> 和 <code>jira</code>。</p>\n\n<h3>第 3 步：Schema 验证的安全网</h3>\n<p>如果你不小心在 <code>settings.json</code> 中写了 <code>"model": 123</code>（应该是字符串），会发生什么？</p>\n<p><strong>答案：</strong>Zod schema 验证会捕获这个类型错误，记录一条警告日志，然后回退到默认值。应用不会崩溃，而是静默降级。</p>\n\n<h3>第 4 步：变更检测的实时性</h3>\n<p><code>changeDetector.ts</code> 使用 <code>fs.watch</code> 监听所有配置文件。当你在 VS Code 中修改并保存 <code>.claude/settings.json</code> 时，系统会：</p>\n<ol>\n<li>收到文件系统事件通知</li>\n<li>使 <code>settingsCache</code> 中对应的缓存条目失效</li>\n<li>下次调用 <code>getCurrentSettings()</code> 时重新读取并解析文件</li>\n<li>新配置立即生效，无需重启</li>\n</ol>\n\n<h3>关键设计洞察</h3>\n<ul>\n<li><strong>安全优先</strong>：MDM 策略不可被用户覆盖，确保企业合规</li>\n<li><strong>向前兼容</strong>：Schema 使用 <code>.passthrough()</code> 允许未知字段</li>\n<li><strong>性能意识</strong>：settingsCache 避免重复的文件 I/O 和 JSON 解析</li>\n<li><strong>优雅降级</strong>：配置错误不会导致崩溃，而是回退到默认值</li>\n</ul>\n'},c03:{content:'\n<h2>查询引擎概述</h2>\n<blockquote>QueryEngine 是 Claude Code 的"大脑"，它管理着每次对话的完整生命周期——从消息构建到 API 调用，从工具执行到结果记录。每个 REPL 会话拥有一个 QueryEngine 实例，它维护着对话状态、token 追踪和权限管理。</blockquote>\n\n<h3>QueryEngine 类的核心状态</h3>\n<p><code>QueryEngine</code> 类是一个有状态的查询管理器，内部维护以下关键状态：</p>\n<ul>\n<li><strong>mutableMessages</strong> — 可变的消息数组，存储整个对话历史。这是一个引用类型，外部持有者可以观察到 QueryEngine 对它的修改</li>\n<li><strong>abortController</strong> — 用于取消正在进行的 API 请求。用户按下 Ctrl+C 或 Escape 时触发 abort</li>\n<li><strong>permissionDenials</strong> — 记录用户拒绝过的权限请求，避免在同一会话中重复询问</li>\n<li><strong>totalUsage</strong> — Token 消耗追踪器，按模型分别记录 input/output/cache tokens，用于费用计算和上下文管理</li>\n<li><strong>readFileState</strong> — LRU（最近最少使用）文件缓存，存储最近读取的文件内容，避免重复读取同一文件</li>\n<li><strong>discoveredSkillNames</strong> — 已发现的 Skill 名称集合，用于 Skill 系统的去重</li>\n<li><strong>loadedNestedMemoryPaths</strong> — 已加载的嵌套 CLAUDE.md 记忆文件路径集合，防止循环引用</li>\n</ul>\n\n<h3>submitMessage() 异步生成器</h3>\n<p><code>submitMessage()</code> 是 QueryEngine 的核心公开方法，它返回一个 <code>AsyncGenerator</code>，逐步 yield 出 SDK 消息供 UI 层消费。其完整生命周期：</p>\n<ol>\n<li><strong>清除追踪状态</strong> — 重置 permissionDenials 等临时状态</li>\n<li><strong>包装 canUseTool</strong> — 将权限检查函数与当前上下文绑定，创建带缓存的工具权限检查器</li>\n<li><strong>获取系统提示词片段</strong> — 调用 <code>fetchSystemPromptParts()</code> 组装系统提示词的各个部分</li>\n<li><strong>处理用户输入</strong> — 将用户的文本消息转换为 API 兼容的消息格式</li>\n<li><strong>调用 query()</strong> — 进入查询循环，开始与 Claude API 的交互</li>\n<li><strong>yield SDK 消息</strong> — 将从 API 返回的消息逐个 yield 出去，供 UI 实时渲染</li>\n<li><strong>记录会话转录</strong> — 将完整的对话记录保存到 transcript 文件</li>\n</ol>\n\n<h3>系统提示词组装</h3>\n<p>系统提示词不是一个静态字符串，而是由多个来源动态组装：</p>\n<ul>\n<li><strong>基础提示词</strong> — Claude Code 的核心行为指令</li>\n<li><strong>工具说明</strong> — 当前可用工具的使用说明</li>\n<li><strong>CLAUDE.md 记忆</strong> — 项目级和用户级的记忆文件内容</li>\n<li><strong>MCP 工具说明</strong> — 已连接的 MCP 服务器提供的工具说明</li>\n<li><strong>上下文信息</strong> — 当前工作目录、git 状态等环境信息</li>\n<li><strong>用户自定义指令</strong> — 通过配置注入的自定义系统提示词</li>\n</ul>\n\n<h3>Coordinator 用户上下文注入</h3>\n<p>当 Claude Code 以 Coordinator（协调者）模式运行时，系统会注入额外的上下文信息，包括当前项目的目录结构摘要、最近的 git 变更记录、以及其他 Agent 的执行状态。这使得 Coordinator 能够做出更好的任务分配决策。</p>\n\n<h3>SDK 消息转换与 yield</h3>\n<p>QueryEngine 将 Anthropic SDK 返回的原始消息对象转换为统一的内部格式，然后通过 <code>yield*</code> 逐个发射给调用者。这种 AsyncGenerator 模式使得 UI 层可以实时渲染流式响应，而不需要等待整个回复完成。</p>\n\n<h3>QueryEngineConfig 类型</h3>\n<p><code>QueryEngineConfig</code> 定义了创建 QueryEngine 时所需的配置：</p>\n<ul>\n<li><code>model</code> — 使用的模型名称</li>\n<li><code>maxTurns</code> — 最大对话轮数</li>\n<li><code>systemPrompt</code> — 基础系统提示词</li>\n<li><code>tools</code> — 可用工具列表</li>\n<li><code>permissionMode</code> — 权限检查模式</li>\n<li><code>budget</code> — Token 预算限制</li>\n<li><code>onMessage</code> — 消息回调函数</li>\n</ul>\n',architecture:'\n<h2>查询引擎架构</h2>\n\n<h3>QueryEngine 状态管理</h3>\n<div style="overflow-x:auto">\n<table>\n<thead><tr><th>状态字段</th><th>类型</th><th>用途</th><th>生命周期</th></tr></thead>\n<tbody>\n<tr><td>mutableMessages</td><td>Message[]</td><td>对话历史</td><td>整个会话</td></tr>\n<tr><td>abortController</td><td>AbortController</td><td>请求取消</td><td>每次 submitMessage</td></tr>\n<tr><td>permissionDenials</td><td>Set&lt;string&gt;</td><td>权限拒绝记录</td><td>每次 submitMessage 重置</td></tr>\n<tr><td>totalUsage</td><td>UsageTracker</td><td>Token 消耗追踪</td><td>整个会话累加</td></tr>\n<tr><td>readFileState</td><td>LRUCache</td><td>文件内容缓存</td><td>整个会话（LRU 淘汰）</td></tr>\n<tr><td>discoveredSkillNames</td><td>Set&lt;string&gt;</td><td>已发现 Skill</td><td>整个会话</td></tr>\n<tr><td>loadedNestedMemoryPaths</td><td>Set&lt;string&gt;</td><td>已加载记忆路径</td><td>整个会话</td></tr>\n</tbody>\n</table>\n</div>\n\n<h3>submitMessage() 调用链</h3>\n<p>消息提交的调用链如下：</p>\n<ul>\n<li><strong>REPL UI</strong> → <code>queryEngine.submitMessage(userInput)</code>\n  <ul>\n  <li>→ 清除临时状态</li>\n  <li>→ 包装 canUseTool 权限检查</li>\n  <li>→ fetchSystemPromptParts() 组装系统提示词</li>\n  <li>→ <strong>query()</strong>（进入查询循环）\n    <ul>\n    <li>→ queryLoop()（内部循环）</li>\n    <li>→ API 调用 + 工具执行</li>\n    <li>→ yield StreamEvent</li>\n    </ul>\n  </li>\n  <li>→ recordTranscript() 保存转录</li>\n  </ul>\n</li>\n</ul>\n\n<h3>与其他模块的关系</h3>\n<ul>\n<li><strong>QueryEngine</strong> → 调用 <code>query()</code> 进入查询循环</li>\n<li><strong>QueryEngine</strong> → 使用 <code>fetchSystemPromptParts()</code> 组装提示词</li>\n<li><strong>QueryEngine</strong> → 通过 <code>canUseTool()</code> 检查工具权限</li>\n<li><strong>QueryEngine</strong> → 与 <code>UsageTracker</code> 协作追踪 token 消耗</li>\n<li><strong>REPL</strong> → 持有 QueryEngine 实例，消费 AsyncGenerator 输出</li>\n</ul>\n',codeExamples:[{title:"QueryEngine 类核心结构",language:"typescript",code:"// QueryEngine.ts — 简化版\nexport class QueryEngine {\n  // 核心状态\n  private mutableMessages: Message[] = []\n  private abortController: AbortController | null = null\n  private permissionDenials = new Set<string>()\n  private totalUsage: UsageTracker = createUsageTracker()\n  private readFileState = new LRUCache<string, FileContent>({\n    max: 100  // 最多缓存 100 个文件\n  })\n  private discoveredSkillNames = new Set<string>()\n  private loadedNestedMemoryPaths = new Set<string>()\n\n  // 配置\n  private config: QueryEngineConfig\n\n  constructor(config: QueryEngineConfig) {\n    this.config = config\n  }\n\n  // 取消当前查询\n  abort(): void {\n    this.abortController?.abort()\n  }\n\n  // 获取 token 使用统计\n  getUsage(): UsageSummary {\n    return this.totalUsage.getSummary()\n  }\n\n  // 核心方法：提交消息（见下一个示例）\n  async *submitMessage(userInput: string): AsyncGenerator<Message> {\n    // ...\n  }\n}"},{title:"submitMessage() 完整生命周期",language:"typescript",code:"// QueryEngine.ts — submitMessage 简化版\nasync *submitMessage(\n  userInput: string\n): AsyncGenerator<Message> {\n  // 1. 清除上一轮的临时状态\n  this.permissionDenials.clear()\n\n  // 2. 创建新的 AbortController\n  this.abortController = new AbortController()\n\n  // 3. 包装工具权限检查\n  const canUseTool = wrapCanUseTool(\n    this.config.permissionMode,\n    this.permissionDenials\n  )\n\n  // 4. 组装系统提示词（多源合并）\n  const systemPromptParts = await fetchSystemPromptParts({\n    tools: this.config.tools,\n    memories: this.loadedNestedMemoryPaths,\n    cwd: process.cwd(),\n    skills: this.discoveredSkillNames,\n  })\n\n  // 5. 添加用户消息到对话历史\n  this.mutableMessages.push({\n    role: 'user',\n    content: userInput,\n  })\n\n  // 6. 进入查询循环，yield 所有响应消息\n  const queryGen = query({\n    messages: this.mutableMessages,\n    systemPrompt: systemPromptParts.join('\\n'),\n    model: this.config.model,\n    tools: this.config.tools,\n    canUseTool,\n    abortSignal: this.abortController.signal,\n    usage: this.totalUsage,\n    readFileState: this.readFileState,\n  })\n\n  // 7. 逐个 yield SDK 消息供 UI 消费\n  for await (const event of queryGen) {\n    if (event.type === 'message') {\n      this.mutableMessages.push(event.message)\n      yield event.message\n    }\n  }\n\n  // 8. 记录会话转录\n  await recordTranscript(this.mutableMessages)\n}"},{title:"系统提示词多源组装",language:"typescript",code:"// fetchSystemPromptParts — 简化版\nasync function fetchSystemPromptParts(opts: {\n  tools: Tool[]\n  memories: Set<string>\n  cwd: string\n  skills: Set<string>\n}): Promise<string[]> {\n  const parts: string[] = []\n\n  // 1. 基础系统提示词\n  parts.push(BASE_SYSTEM_PROMPT)\n\n  // 2. 工具使用说明\n  for (const tool of opts.tools) {\n    parts.push(formatToolDescription(tool))\n  }\n\n  // 3. CLAUDE.md 项目记忆\n  const projectMemory = await loadClaudeMd(opts.cwd)\n  if (projectMemory) {\n    parts.push(`<project-memory>\\n${projectMemory}\\n</project-memory>`)\n  }\n\n  // 4. 用户级记忆（~/.claude/CLAUDE.md）\n  const userMemory = await loadUserClaudeMd()\n  if (userMemory) {\n    parts.push(`<user-memory>\\n${userMemory}\\n</user-memory>`)\n  }\n\n  // 5. 环境上下文\n  parts.push(`Current directory: ${opts.cwd}`)\n  parts.push(`Platform: ${process.platform}`)\n\n  // 6. Coordinator 模式额外上下文\n  if (isCoordinatorMode()) {\n    parts.push(await getCoordinatorContext())\n  }\n\n  return parts\n}"}],interactive:'\n<h2>查询引擎互动解析</h2>\n\n<h3>第 1 步：理解 AsyncGenerator 模式</h3>\n<p>为什么 <code>submitMessage()</code> 返回 <code>AsyncGenerator</code> 而不是 <code>Promise</code>？</p>\n<p>因为 Claude 的响应是<strong>流式的</strong>。如果使用 Promise，UI 必须等到整个回复完成才能显示，用户会看到长时间的"思考中..."。AsyncGenerator 让 UI 可以在每个 token 到达时立即渲染，实现打字机效果。</p>\n\n<h3>第 2 步：状态的生命周期</h3>\n<p>注意不同状态字段的生命周期差异：</p>\n<ul>\n<li><code>totalUsage</code> 在整个会话中<strong>持续累加</strong>，因为用户需要看到总费用</li>\n<li><code>permissionDenials</code> 在每次 <code>submitMessage</code> 时<strong>清除</strong>，因为用户可能改变了主意</li>\n<li><code>readFileState</code> 使用 <strong>LRU 缓存</strong>，自动淘汰最久未使用的文件内容</li>\n</ul>\n\n<h3>第 3 步：权限检查的包装</h3>\n<p><code>wrapCanUseTool()</code> 为什么要每次 submitMessage 时重新包装？因为它需要捕获当前轮次的 <code>permissionDenials</code> 引用。当用户拒绝某个工具后，该拒绝记录仅在当前轮次有效——下一轮用户可能会允许它。</p>\n\n<h3>第 4 步：系统提示词的动态性</h3>\n<p>每次调用 <code>fetchSystemPromptParts()</code> 都会重新组装系统提示词。这意味着：</p>\n<ul>\n<li>如果用户在对话中修改了 <code>CLAUDE.md</code>，下一轮对话就会使用更新后的内容</li>\n<li>新发现的 Skill 会自动添加到系统提示词中</li>\n<li>环境上下文（如 cd 到新目录）会实时反映</li>\n</ul>\n\n<h3>核心设计洞察</h3>\n<ul>\n<li><strong>有状态但可控</strong>：QueryEngine 维护必要的状态，但每个状态都有明确的生命周期规则</li>\n<li><strong>流式优先</strong>：AsyncGenerator 模式贯穿整个消息管道，从 API 到 UI</li>\n<li><strong>权限即时性</strong>：权限决策不会跨轮持久化，尊重用户的即时意愿</li>\n</ul>\n'},c04:{content:'\n<h2>查询循环概述</h2>\n<blockquote>查询循环是 Claude Code 的核心执行引擎——一个精密的 AsyncGenerator 状态机，编排着从 API 调用到工具执行的完整生命周期。每次迭代包含 10 个精心排列的步骤，处理上下文管理、错误恢复和停止条件判断。</blockquote>\n\n<h3>AsyncGenerator 模式</h3>\n<p><code>query()</code> 是查询循环的外层包装函数，它返回一个 <code>AsyncGenerator</code>，yield 出三种事件类型：</p>\n<ul>\n<li><strong>StreamEvent</strong> — 流式 token 事件，用于实时渲染</li>\n<li><strong>RequestStartEvent</strong> — API 请求开始事件，用于 UI 加载状态</li>\n<li><strong>Message</strong> — 完整的助理消息或工具结果消息</li>\n</ul>\n<p><code>query()</code> 内部调用 <code>queryLoop()</code>，后者是真正的状态机实现。</p>\n\n<h3>queryLoop() 内部状态</h3>\n<p><code>queryLoop()</code> 维护一个 <code>State</code> 对象，包含以下关键字段：</p>\n<ul>\n<li><strong>messages</strong> — 当前对话的完整消息历史</li>\n<li><strong>toolUseContext</strong> — 工具执行上下文，包含当前工具调用的元数据</li>\n<li><strong>autoCompactTracking</strong> — 自动压缩追踪，记录 token 增长趋势以决定何时触发压缩</li>\n<li><strong>maxOutputTokensRecoveryCount</strong> — 最大输出 token 恢复计数器，<strong>上限为 3 次</strong>。当 API 返回 <code>max_output_tokens</code> 停止原因时，系统最多重试 3 次</li>\n<li><strong>turnCount</strong> — 当前轮次计数，用于限制最大对话轮数</li>\n<li><strong>transition</strong> — 状态转换标志，控制循环的继续/终止</li>\n<li><strong>stopHookActive</strong> — 停止钩子激活标志，当 post-sampling 钩子返回停止信号时设为 true</li>\n</ul>\n\n<h3>10 步迭代管道</h3>\n<p>每次循环迭代包含以下 10 个步骤，顺序经过精心设计：</p>\n<ol>\n<li><strong>Skill 发现预取</strong> — 异步预取可能用到的 Skill 定义，与后续步骤并行</li>\n<li><strong>工具结果预算</strong> — <code>applyToolResultBudget()</code> 对过长的工具输出进行截断，防止 token 爆炸</li>\n<li><strong>Snip 压缩</strong> — 对历史消息中的大文本块进行"剪切"，用摘要替换全文</li>\n<li><strong>Micro-compact</strong> — 微压缩，移除消息中的冗余空白和格式</li>\n<li><strong>上下文折叠</strong> — 将老旧的对话轮次折叠为摘要，减少 token 消耗</li>\n<li><strong>自动压缩</strong> — 当 token 使用量接近上下文窗口限制时，触发激进的上下文压缩</li>\n<li><strong>系统提示词组装</strong> — 重新组装系统提示词（可能包含新发现的 Skill 等动态内容）</li>\n<li><strong>API 流式调用</strong> — 向 Claude API 发送请求并处理流式响应</li>\n<li><strong>工具执行</strong> — 解析 Claude 返回的工具调用，执行工具并收集结果</li>\n<li><strong>Post-sampling 钩子</strong> — 执行用户定义的后采样钩子（如代码审查、安全检查）</li>\n</ol>\n\n<h3>错误恢复机制</h3>\n<p>查询循环内置了多种错误恢复策略：</p>\n<ul>\n<li><strong>max_output_tokens 恢复</strong>（最多 3 次）— 当 Claude 的输出被截断时，系统会自动发送"继续"消息，让 Claude 从断点继续。<code>maxOutputTokensRecoveryCount</code> 限制为 3 次，防止无限循环</li>\n<li><strong>响应式压缩</strong> — 当 API 返回 <code>prompt_too_long</code> 错误时，立即触发上下文压缩并重试</li>\n<li><strong>prompt_too_long 处理</strong> — 如果压缩后仍然超长，系统会尝试更激进的压缩策略（移除中间轮次，仅保留首尾）</li>\n</ul>\n\n<h3>"思考规则"（Rules of Thinking）</h3>\n<p>当 Claude 返回 thinking blocks（扩展思考块）时，查询循环会应用特殊的处理规则：</p>\n<ul>\n<li>thinking blocks 不计入 <code>messages</code> 历史（避免 token 浪费）</li>\n<li>thinking 内容会被记录到 transcript 但不会发送回 API</li>\n<li>连续的 thinking blocks 会被合并以减少消息碎片</li>\n</ul>\n\n<h3>停止条件</h3>\n<p>查询循环在以下条件下终止：</p>\n<ul>\n<li><strong>最大轮数</strong> — <code>turnCount</code> 达到配置的 <code>maxTurns</code></li>\n<li><strong>Token 预算</strong> — 累计消耗超过配置的 <code>budget</code> 限制</li>\n<li><strong>停止钩子</strong> — post-sampling 钩子返回 <code>{ stop: true }</code></li>\n<li><strong>用户取消</strong> — AbortController 被触发</li>\n<li><strong>Claude 主动结束</strong> — 返回 <code>end_turn</code> 停止原因且无工具调用</li>\n</ul>\n',architecture:"\n<h2>查询循环架构</h2>\n\n<h3>状态机概览</h3>\n<div style=\"overflow-x:auto\">\n<table>\n<thead><tr><th>State 字段</th><th>类型</th><th>初始值</th><th>更新时机</th></tr></thead>\n<tbody>\n<tr><td>messages</td><td>Message[]</td><td>传入的历史</td><td>每轮 API 响应后</td></tr>\n<tr><td>toolUseContext</td><td>ToolUseContext</td><td>{}</td><td>工具执行前后</td></tr>\n<tr><td>autoCompactTracking</td><td>CompactTracker</td><td>{ tokenHistory: [] }</td><td>每轮 API 调用后</td></tr>\n<tr><td>maxOutputTokensRecoveryCount</td><td>number</td><td>0</td><td>max_output_tokens 恢复时 +1</td></tr>\n<tr><td>turnCount</td><td>number</td><td>0</td><td>每轮 +1</td></tr>\n<tr><td>transition</td><td>'continue' | 'stop'</td><td>'continue'</td><td>停止条件检查后</td></tr>\n<tr><td>stopHookActive</td><td>boolean</td><td>false</td><td>post-sampling 钩子返回后</td></tr>\n</tbody>\n</table>\n</div>\n\n<h3>10 步管道流程图</h3>\n<p>每次迭代的 10 个步骤形成一个管道：</p>\n<ol>\n<li><code>prefetchSkills()</code> → 异步预取（不阻塞后续步骤）</li>\n<li><code>applyToolResultBudget()</code> → 截断过长的工具输出</li>\n<li><code>snipCompact()</code> → 剪切大文本块</li>\n<li><code>microCompact()</code> → 移除冗余空白</li>\n<li><code>contextCollapse()</code> → 折叠老旧轮次</li>\n<li><code>autoCompact()</code> → 基于 token 增长趋势的自动压缩</li>\n<li><code>assembleSystemPrompt()</code> → 组装系统提示词</li>\n<li><code>streamApiCall()</code> → API 流式调用</li>\n<li><code>executeTools()</code> → 工具执行</li>\n<li><code>runPostSamplingHooks()</code> → 后采样钩子</li>\n</ol>\n\n<h3>错误恢复流程</h3>\n<ul>\n<li><strong>max_output_tokens</strong>：API 响应 → 检查 stop_reason → recoveryCount < 3 → 发送 \"continue\" → 重试</li>\n<li><strong>prompt_too_long</strong>：API 错误 → 触发 reactiveCompact() → 重试 → 仍然失败 → aggressiveCompact() → 重试</li>\n<li><strong>网络错误</strong>：由 API 客户端层处理（见第 5 章），查询循环只处理语义级错误</li>\n</ul>\n\n<h3>设计决策</h3>\n<blockquote>\n<p><strong>为什么 10 步管道的顺序如此重要？</strong></p>\n<p>Skill 预取放在最前面是因为它是异步的，可以与后续步骤并行。上下文压缩步骤（2-6）必须在 API 调用（8）之前执行，否则可能因 token 超限而失败。工具执行（9）必须在 API 响应（8）之后，因为需要解析 Claude 的工具调用指令。这个顺序是经过多次迭代优化的结果。</p>\n</blockquote>\n",codeExamples:[{title:"query() 外层包装与 queryLoop() 状态机",language:"typescript",code:"// query.ts — 简化版\nexport async function* query(\n  opts: QueryOptions\n): AsyncGenerator<StreamEvent | RequestStartEvent | Message> {\n  // 外层 query() 是一个薄包装\n  yield* queryLoop({\n    messages: opts.messages,\n    systemPrompt: opts.systemPrompt,\n    model: opts.model,\n    tools: opts.tools,\n    canUseTool: opts.canUseTool,\n    abortSignal: opts.abortSignal,\n    usage: opts.usage,\n  })\n}\n\n// queryLoop 内部状态类型\ninterface State {\n  messages: Message[]\n  toolUseContext: ToolUseContext\n  autoCompactTracking: {\n    tokenHistory: number[]\n    lastCompactAt: number\n  }\n  maxOutputTokensRecoveryCount: number  // 上限 3\n  turnCount: number\n  transition: 'continue' | 'stop'\n  stopHookActive: boolean\n}\n\nasync function* queryLoop(\n  opts: QueryLoopOptions\n): AsyncGenerator<StreamEvent | RequestStartEvent | Message> {\n  // 初始化状态\n  const state: State = {\n    messages: opts.messages,\n    toolUseContext: {},\n    autoCompactTracking: { tokenHistory: [], lastCompactAt: 0 },\n    maxOutputTokensRecoveryCount: 0,\n    turnCount: 0,\n    transition: 'continue',\n    stopHookActive: false,\n  }\n\n  // 主循环\n  while (state.transition === 'continue') {\n    state.turnCount++\n\n    // 检查停止条件\n    if (state.turnCount > opts.maxTurns) {\n      state.transition = 'stop'\n      break\n    }\n\n    // 执行 10 步管道（见下一个示例）\n    yield* executeIterationPipeline(state, opts)\n  }\n}"},{title:"10 步迭代管道",language:"typescript",code:"// query/pipeline.ts — 简化版\nasync function* executeIterationPipeline(\n  state: State,\n  opts: QueryLoopOptions\n): AsyncGenerator<StreamEvent | RequestStartEvent | Message> {\n\n  // === 步骤 1: Skill 发现预取（异步，不阻塞） ===\n  const skillPrefetch = prefetchSkillDiscovery(state.messages)\n\n  // === 步骤 2: 工具结果预算 ===\n  applyToolResultBudget(state.messages, {\n    maxToolResultTokens: 50_000,  // 单个工具输出上限 50K tokens\n  })\n\n  // === 步骤 3: Snip 压缩 ===\n  snipCompact(state.messages)  // 将大文本块替换为摘要\n\n  // === 步骤 4: Micro-compact ===\n  microCompact(state.messages)  // 移除冗余空白和格式\n\n  // === 步骤 5: 上下文折叠 ===\n  contextCollapse(state.messages, {\n    preserveRecentTurns: 4,  // 保留最近 4 轮\n  })\n\n  // === 步骤 6: 自动压缩（基于 token 增长趋势） ===\n  const compacted = await autoCompact(state.messages, state.autoCompactTracking)\n  if (compacted) {\n    state.autoCompactTracking.lastCompactAt = state.turnCount\n  }\n\n  // === 步骤 7: 系统提示词组装 ===\n  await skillPrefetch  // 等待 Skill 预取完成\n  const systemPrompt = assembleSystemPrompt(opts, state)\n\n  // === 步骤 8: API 流式调用 ===\n  yield { type: 'requestStart' } as RequestStartEvent\n  const response = await streamApiCall({\n    model: opts.model,\n    system: systemPrompt,\n    messages: state.messages,\n    tools: opts.tools,\n    abortSignal: opts.abortSignal,\n  })\n\n  // yield 流式事件\n  for await (const event of response.stream) {\n    yield event as StreamEvent\n  }\n  const assistantMessage = response.finalMessage\n  yield assistantMessage\n\n  // === 步骤 9: 工具执行 ===\n  const toolCalls = extractToolCalls(assistantMessage)\n  if (toolCalls.length > 0) {\n    const toolResults = await executeTools(toolCalls, opts.canUseTool)\n    state.messages.push(...toolResults)\n    for (const result of toolResults) yield result\n  } else {\n    // 无工具调用 → Claude 主动结束\n    state.transition = 'stop'\n  }\n\n  // === 步骤 10: Post-sampling 钩子 ===\n  const hookResult = await runPostSamplingHooks(assistantMessage)\n  if (hookResult?.stop) {\n    state.stopHookActive = true\n    state.transition = 'stop'\n  }\n\n  // 错误恢复：max_output_tokens\n  if (response.stopReason === 'max_output_tokens') {\n    if (state.maxOutputTokensRecoveryCount < 3) {\n      state.maxOutputTokensRecoveryCount++\n      state.messages.push({ role: 'user', content: 'Continue.' })\n      // transition 保持 'continue'，进入下一轮\n    } else {\n      state.transition = 'stop'  // 超过 3 次重试上限\n    }\n  }\n\n  // 更新 token 追踪\n  state.autoCompactTracking.tokenHistory.push(\n    response.usage.inputTokens\n  )\n}"},{title:"错误恢复策略",language:"typescript",code:"// query/errorRecovery.ts — 简化版\n\n// max_output_tokens 恢复\nfunction handleMaxOutputTokens(\n  state: State,\n  response: ApiResponse\n): void {\n  if (response.stopReason !== 'max_output_tokens') return\n\n  if (state.maxOutputTokensRecoveryCount < 3) {\n    // 还有重试机会\n    state.maxOutputTokensRecoveryCount++\n    state.messages.push({\n      role: 'user',\n      content: 'Your response was cut off. Please continue from where you stopped.',\n    })\n    // state.transition 保持 'continue'\n  } else {\n    // 已重试 3 次，放弃\n    state.transition = 'stop'\n  }\n}\n\n// prompt_too_long 响应式压缩\nasync function handlePromptTooLong(\n  state: State,\n  error: ApiError\n): Promise<boolean> {\n  // 第一次尝试：温和压缩\n  const gentleResult = await reactiveCompact(state.messages, {\n    strategy: 'preserve-recent',\n    targetReduction: 0.3,  // 减少 30% token\n  })\n\n  if (gentleResult.success) return true  // 重试\n\n  // 第二次尝试：激进压缩\n  const aggressiveResult = await aggressiveCompact(state.messages, {\n    strategy: 'keep-first-last',  // 仅保留首尾消息\n    targetReduction: 0.6,  // 减少 60% token\n  })\n\n  return aggressiveResult.success  // 成功则重试，否则放弃\n}\n\n// 停止条件检查\nfunction checkStopConditions(state: State, opts: QueryLoopOptions): void {\n  // 最大轮数\n  if (state.turnCount >= opts.maxTurns) {\n    state.transition = 'stop'\n    return\n  }\n\n  // Token 预算\n  if (opts.usage.totalTokens >= opts.budget) {\n    state.transition = 'stop'\n    return\n  }\n\n  // 停止钩子\n  if (state.stopHookActive) {\n    state.transition = 'stop'\n    return\n  }\n}"}],interactive:'\n<h2>查询循环互动解析</h2>\n\n<h3>第 1 步：理解 10 步管道</h3>\n<p>想象你正在和 Claude 进行一个长对话（已经 50 轮），然后发送了新消息。此时 10 步管道是如何保护系统不崩溃的？</p>\n<ul>\n<li>步骤 2-6（压缩步骤）会逐层削减 token 消耗：先截断过长的工具输出，再压缩旧消息，最后在必要时折叠历史轮次</li>\n<li>步骤 6（自动压缩）会检查 token 增长趋势——如果最近几轮 token 增长速度过快，会提前触发压缩，而不是等到 API 报错</li>\n</ul>\n\n<h3>第 2 步：max_output_tokens 的 3 次重试</h3>\n<p>当 Claude 写一段很长的代码，输出被截断时：</p>\n<ol>\n<li>第 1 次：系统发送"继续"，Claude 从断点继续</li>\n<li>第 2 次：如果又被截断，再次发送"继续"</li>\n<li>第 3 次：最后一次机会</li>\n<li>第 4 次截断：系统放弃，将已有内容返回给用户</li>\n</ol>\n<p>为什么限制 3 次？因为无限重试可能导致费用失控，而且 3 次恢复后输出已经足够长（约 16K * 4 = 64K tokens），几乎覆盖所有实际场景。</p>\n\n<h3>第 3 步：压缩策略的层次</h3>\n<p>查询循环的 5 种压缩策略形成一个渐进的"防线"：</p>\n<ol>\n<li><strong>工具结果预算</strong> — 最温和，只截断单个工具的过长输出</li>\n<li><strong>Snip 压缩</strong> — 将大文本块替换为摘要标记</li>\n<li><strong>Micro-compact</strong> — 移除冗余空白，最小化改变</li>\n<li><strong>上下文折叠</strong> — 折叠旧轮次，保留最近 4 轮</li>\n<li><strong>自动压缩</strong> — 最激进，可能重写整个对话历史的摘要</li>\n</ol>\n\n<h3>第 4 步：停止条件的安全网</h3>\n<p>查询循环有 5 种停止条件，形成多层安全网：</p>\n<ul>\n<li><strong>用户取消</strong>（Ctrl+C）— 最即时的停止方式</li>\n<li><strong>Claude 主动结束</strong> — 正常情况下的停止</li>\n<li><strong>最大轮数</strong> — 防止无限循环</li>\n<li><strong>Token 预算</strong> — 防止费用失控</li>\n<li><strong>停止钩子</strong> — 外部系统的停止信号（如 CI 超时）</li>\n</ul>\n\n<h3>关键设计洞察</h3>\n<ul>\n<li><strong>渐进式压缩</strong>：从温和到激进的 5 层压缩策略，确保在最小改变的前提下保持 token 可控</li>\n<li><strong>有限重试</strong>：max_output_tokens 的 3 次重试上限，平衡了完整性和成本</li>\n<li><strong>Skill 预取并行</strong>：步骤 1 的异步预取与步骤 2-6 并行，避免阻塞管道</li>\n<li><strong>状态机清晰</strong>：State 类型明确定义了所有可变状态，transition 字段统一控制流程转换</li>\n</ul>\n'},c05:{content:"\n<h2>API 客户端概述</h2>\n<blockquote>API 客户端层是 Claude Code 与 Anthropic 云端 Claude 模型之间的桥梁。它封装了流式通信、重试逻辑、Thinking 模式管理、提示缓存优化和用量追踪等复杂逻辑，向上层提供简洁的接口。</blockquote>\n\n<h3>流式通信：@anthropic-ai/sdk 与 BetaMessageStream</h3>\n<p>Claude Code 使用 Anthropic 官方 TypeScript SDK（<code>@anthropic-ai/sdk</code>）进行 API 通信。核心的流式调用使用 <code>BetaMessageStream</code>：</p>\n<ul>\n<li>SDK 的 <code>messages.stream()</code> 方法返回一个 <code>BetaMessageStream</code> 对象</li>\n<li>该流对象实现了 <code>AsyncIterable</code> 接口，可以用 <code>for await</code> 逐个消费 token</li>\n<li>流式事件包括：<code>content_block_start</code>、<code>content_block_delta</code>、<code>content_block_stop</code>、<code>message_stop</code></li>\n<li>支持 <code>AbortSignal</code> 参数，允许用户在流式传输过程中取消请求</li>\n</ul>\n\n<h3>Thinking 模式</h3>\n<p>Claude Code 支持三种 Thinking（扩展思考）模式，通过 <code>max_thinking_length</code> 参数控制：</p>\n<ul>\n<li><strong>adaptive（自适应）</strong> — 默认模式。系统根据任务复杂度自动决定是否启用 Thinking。简单任务（如文件读取）不启用，复杂任务（如代码重构）自动启用。通过设置 <code>budget_tokens</code> 让模型自行决定</li>\n<li><strong>enabled（始终启用）</strong> — 强制每次请求都启用 Thinking。设置固定的 <code>max_thinking_length</code> 值，确保 Claude 总是先思考再回答</li>\n<li><strong>disabled（禁用）</strong> — 完全关闭 Thinking。不发送 thinking 相关参数，适用于对延迟敏感的场景</li>\n</ul>\n\n<h3>提示缓存断点检测</h3>\n<p><code>promptCacheBreakDetection.ts</code> 实现了智能的提示缓存优化。Anthropic API 支持在消息中插入 <code>cache_control</code> 断点，被缓存的前缀在后续请求中可以复用，极大降低 token 成本：</p>\n<ul>\n<li>系统分析消息历史，在<strong>不太可能变化的位置</strong>插入缓存断点</li>\n<li>典型的缓存断点位置：系统提示词末尾、长对话中的早期消息</li>\n<li>缓存命中时，input tokens 的计费可降低约 90%</li>\n<li>断点检测算法会避免在频繁变化的消息附近插入断点</li>\n</ul>\n\n<h3>重试逻辑</h3>\n<p>API 客户端内置了健壮的重试机制：</p>\n<ul>\n<li><strong>指数退避 + 抖动</strong>：基础延迟 \xd7 2^(重试次数) + 随机抖动，避免雷群效应</li>\n<li><strong>可配置的最大重试次数</strong>：默认 3 次，可通过配置调整</li>\n<li><strong>可重试的错误类型</strong>：429（频率限制）、500/502/503（服务器错误）、网络超时</li>\n<li><strong>不可重试的错误</strong>：400（请求格式错误）、401（认证失败）、403（权限不足）</li>\n</ul>\n\n<h3>模型回退</h3>\n<p>当配置的主模型不可用时，系统支持自动回退：</p>\n<ul>\n<li>典型的回退链：<code>claude-sonnet-4-20250514</code> → <code>claude-opus-4-20250514</code></li>\n<li>回退触发条件：模型过载（529）、模型不可用（404）</li>\n<li>回退决策记录在日志中，方便排查</li>\n</ul>\n\n<h3>工具 Schema 转换</h3>\n<p>Claude Code 内部使用 Zod schema 定义工具的输入参数。在发送 API 请求前，需要将 Zod schema 转换为 Anthropic API 兼容的 JSON Schema 格式：</p>\n<ul>\n<li>使用 <code>zodToJsonSchema()</code> 转换函数</li>\n<li>处理 Zod 特有的类型（如 <code>z.enum</code>、<code>z.union</code>）到标准 JSON Schema 的映射</li>\n<li>移除 Zod 元数据，只保留 API 需要的 <code>type</code>、<code>properties</code>、<code>required</code> 等字段</li>\n</ul>\n\n<h3>消息规范化</h3>\n<p>在发送消息到 API 之前，系统会对消息进行规范化处理：</p>\n<ul>\n<li>合并相邻的同角色消息（API 要求消息交替 user/assistant）</li>\n<li>移除内部元数据字段（如 UI 渲染标记）</li>\n<li>转换 thinking blocks 的格式</li>\n<li>处理图片和文件附件的编码</li>\n</ul>\n\n<h3>Beta 标志传播</h3>\n<p>Claude Code 会向 API 请求中附加特定的 Beta 标志头，以启用实验性功能（如 extended thinking、tool use 增强等）。这些标志通过 SDK 的 <code>betas</code> 参数传播。</p>\n\n<h3>preconnectAnthropicApi()</h3>\n<p>在 <code>init()</code> 阶段，系统会提前建立到 Anthropic API 的 TCP+TLS 连接。这个预热操作可以节省首次 API 调用约 100-200ms 的连接建立延迟，因为 TLS 握手（特别是 TLS 1.3 的 0-RTT）需要多次网络往返。</p>\n\n<h3>用量追踪</h3>\n<p>API 客户端在每次请求后记录详细的 token 用量：</p>\n<ul>\n<li><strong>input tokens</strong> — 输入 token 数（包含系统提示词和对话历史）</li>\n<li><strong>output tokens</strong> — 输出 token 数（Claude 的回复）</li>\n<li><strong>cache creation tokens</strong> — 创建缓存的 token 数</li>\n<li><strong>cache read tokens</strong> — 从缓存读取的 token 数</li>\n<li>按模型分别统计，支持跨模型的总计计算</li>\n</ul>\n",architecture:"\n<h2>API 客户端架构</h2>\n\n<h3>核心模块</h3>\n<div style=\"overflow-x:auto\">\n<table>\n<thead><tr><th>模块</th><th>职责</th><th>关键函数</th></tr></thead>\n<tbody>\n<tr><td>services/api/claude.ts</td><td>主 API 客户端</td><td>callClaude(), streamClaude()</td></tr>\n<tr><td>services/api/retry.ts</td><td>重试逻辑</td><td>withRetry(), calculateBackoff()</td></tr>\n<tr><td>services/api/thinking.ts</td><td>Thinking 模式管理</td><td>getThinkingConfig()</td></tr>\n<tr><td>services/api/promptCacheBreakDetection.ts</td><td>缓存断点检测</td><td>insertCacheBreakpoints()</td></tr>\n<tr><td>services/api/toolSchemaConversion.ts</td><td>Zod→JSON Schema</td><td>convertToolSchemas()</td></tr>\n<tr><td>services/api/messageNormalization.ts</td><td>消息格式化</td><td>normalizeMessages()</td></tr>\n<tr><td>services/api/usage.ts</td><td>用量追踪</td><td>trackUsage(), getUsageSummary()</td></tr>\n</tbody>\n</table>\n</div>\n\n<h3>请求生命周期</h3>\n<p>一次完整的 API 调用经过以下层：</p>\n<ol>\n<li><strong>QueryLoop</strong> → 调用 <code>streamClaude()</code></li>\n<li><strong>消息规范化</strong> → <code>normalizeMessages()</code> 处理消息格式</li>\n<li><strong>工具 Schema 转换</strong> → <code>convertToolSchemas()</code> Zod→JSON Schema</li>\n<li><strong>缓存断点插入</strong> → <code>insertCacheBreakpoints()</code> 优化 token 费用</li>\n<li><strong>Thinking 配置</strong> → <code>getThinkingConfig()</code> 决定是否启用 Thinking</li>\n<li><strong>重试包装</strong> → <code>withRetry()</code> 处理瞬态故障</li>\n<li><strong>SDK 调用</strong> → <code>anthropic.beta.messages.stream()</code> 实际发送请求</li>\n<li><strong>流式处理</strong> → 逐 token yield 给查询循环</li>\n<li><strong>用量记录</strong> → <code>trackUsage()</code> 记录 token 消耗</li>\n</ol>\n\n<h3>Thinking 模式决策树</h3>\n<ul>\n<li>配置 = <code>adaptive</code>\n  <ul>\n  <li>→ 设置 <code>thinking: { type: 'enabled', budget_tokens: N }</code></li>\n  <li>→ 模型自行决定思考深度</li>\n  </ul>\n</li>\n<li>配置 = <code>enabled</code>\n  <ul>\n  <li>→ 设置固定 <code>max_thinking_length</code></li>\n  <li>→ 模型始终先思考</li>\n  </ul>\n</li>\n<li>配置 = <code>disabled</code>\n  <ul>\n  <li>→ 不发送 thinking 参数</li>\n  <li>→ 模型直接回复</li>\n  </ul>\n</li>\n</ul>\n\n<h3>设计决策</h3>\n<blockquote>\n<p><strong>为什么要做 TCP+TLS 预连接？</strong></p>\n<p>TLS 1.3 握手需要至少 1 个 RTT（往返时延），如果包含证书验证和 OCSP Stapling，可能需要 2-3 个 RTT。在 100ms 延迟的网络上，这就是 200-300ms 的固定开销。通过在 init() 阶段预连接，这个开销被完全隐藏在用户输入第一条消息之前，首次 API 调用的感知延迟大幅降低。</p>\n</blockquote>\n",codeExamples:[{title:"流式 API 调用与 BetaMessageStream",language:"typescript",code:"// services/api/claude.ts — 简化版\nimport Anthropic from '@anthropic-ai/sdk'\n\nconst anthropic = new Anthropic({\n  apiKey: getApiKey(),\n  // TCP+TLS 预连接在 init() 阶段完成\n})\n\nexport async function* streamClaude(opts: {\n  model: string\n  system: string\n  messages: Message[]\n  tools: ToolDefinition[]\n  abortSignal?: AbortSignal\n  usage: UsageTracker\n}): AsyncGenerator<StreamEvent> {\n  // 1. 消息规范化\n  const normalizedMessages = normalizeMessages(opts.messages)\n\n  // 2. 工具 Schema 转换（Zod → JSON Schema）\n  const apiTools = convertToolSchemas(opts.tools)\n\n  // 3. 缓存断点插入\n  const messagesWithCache = insertCacheBreakpoints(normalizedMessages)\n\n  // 4. Thinking 配置\n  const thinkingConfig = getThinkingConfig(opts.model)\n\n  // 5. 带重试的流式调用\n  const stream = await withRetry(async () => {\n    return anthropic.beta.messages.stream({\n      model: opts.model,\n      max_tokens: 16384,\n      system: [{ type: 'text', text: opts.system, cache_control: { type: 'ephemeral' } }],\n      messages: messagesWithCache,\n      tools: apiTools,\n      ...thinkingConfig,\n      betas: ['prompt-caching-2024-07-31', 'extended-thinking-2025-01-24'],\n    }, {\n      signal: opts.abortSignal,\n    })\n  }, { maxRetries: 3 })\n\n  // 6. yield 流式事件\n  for await (const event of stream) {\n    yield {\n      type: event.type,\n      data: event,\n    } as StreamEvent\n  }\n\n  // 7. 获取最终消息并记录用量\n  const finalMessage = await stream.finalMessage()\n  opts.usage.track({\n    model: opts.model,\n    inputTokens: finalMessage.usage.input_tokens,\n    outputTokens: finalMessage.usage.output_tokens,\n    cacheCreationTokens: finalMessage.usage.cache_creation_input_tokens ?? 0,\n    cacheReadTokens: finalMessage.usage.cache_read_input_tokens ?? 0,\n  })\n\n  return finalMessage\n}"},{title:"重试逻辑与指数退避",language:"typescript",code:"// services/api/retry.ts — 简化版\ninterface RetryConfig {\n  maxRetries: number\n  baseDelay: number      // 默认 1000ms\n  maxDelay: number        // 默认 30000ms\n  jitterFactor: number    // 默认 0.1\n}\n\nexport async function withRetry<T>(\n  fn: () => Promise<T>,\n  config: Partial<RetryConfig> = {}\n): Promise<T> {\n  const {\n    maxRetries = 3,\n    baseDelay = 1000,\n    maxDelay = 30000,\n    jitterFactor = 0.1,\n  } = config\n\n  let lastError: Error | null = null\n\n  for (let attempt = 0; attempt <= maxRetries; attempt++) {\n    try {\n      return await fn()\n    } catch (error) {\n      lastError = error as Error\n\n      // 不可重试的错误：立即抛出\n      if (!isRetryableError(error)) {\n        throw error\n      }\n\n      // 最后一次尝试失败：抛出\n      if (attempt === maxRetries) break\n\n      // 计算退避延迟\n      const delay = calculateBackoff(attempt, baseDelay, maxDelay, jitterFactor)\n      await sleep(delay)\n    }\n  }\n\n  throw lastError\n}\n\nfunction calculateBackoff(\n  attempt: number,\n  baseDelay: number,\n  maxDelay: number,\n  jitterFactor: number\n): number {\n  // 指数退避：baseDelay * 2^attempt\n  const exponentialDelay = baseDelay * Math.pow(2, attempt)\n\n  // 加上随机抖动，避免雷群效应\n  const jitter = exponentialDelay * jitterFactor * Math.random()\n\n  // 不超过最大延迟\n  return Math.min(exponentialDelay + jitter, maxDelay)\n}\n\nfunction isRetryableError(error: unknown): boolean {\n  if (error instanceof Anthropic.APIError) {\n    // 可重试：429(频率限制)、500/502/503(服务器错误)\n    return [429, 500, 502, 503].includes(error.status)\n  }\n  // 网络超时也可重试\n  if (error instanceof Error && error.message.includes('ETIMEDOUT')) {\n    return true\n  }\n  return false\n}"},{title:"Thinking 模式配置与用量追踪",language:"typescript",code:"// services/api/thinking.ts — 简化版\ntype ThinkingMode = 'adaptive' | 'enabled' | 'disabled'\n\nexport function getThinkingConfig(model: string): ThinkingParams {\n  const mode = getSettingValue<ThinkingMode>('thinkingMode', 'adaptive')\n  const maxThinkingTokens = getSettingValue<number>(\n    'maxThinkingTokens', 10000\n  )\n\n  switch (mode) {\n    case 'adaptive':\n      // 自适应模式：让模型自行决定思考深度\n      return {\n        thinking: {\n          type: 'enabled',\n          budget_tokens: maxThinkingTokens,\n        }\n      }\n\n    case 'enabled':\n      // 始终启用：设置固定的思考长度\n      return {\n        thinking: {\n          type: 'enabled',\n          budget_tokens: maxThinkingTokens,\n        }\n      }\n\n    case 'disabled':\n      // 完全禁用：不发送 thinking 参数\n      return {}\n  }\n}\n\n// services/api/usage.ts — 用量追踪\nexport interface UsageRecord {\n  model: string\n  inputTokens: number\n  outputTokens: number\n  cacheCreationTokens: number\n  cacheReadTokens: number\n}\n\nexport class UsageTracker {\n  private records: UsageRecord[] = []\n\n  track(record: UsageRecord): void {\n    this.records.push(record)\n  }\n\n  getSummary(): UsageSummary {\n    const byModel = new Map<string, ModelUsage>()\n\n    for (const record of this.records) {\n      const existing = byModel.get(record.model) ?? {\n        inputTokens: 0, outputTokens: 0,\n        cacheCreationTokens: 0, cacheReadTokens: 0,\n      }\n      byModel.set(record.model, {\n        inputTokens: existing.inputTokens + record.inputTokens,\n        outputTokens: existing.outputTokens + record.outputTokens,\n        cacheCreationTokens: existing.cacheCreationTokens + record.cacheCreationTokens,\n        cacheReadTokens: existing.cacheReadTokens + record.cacheReadTokens,\n      })\n    }\n\n    return {\n      byModel: Object.fromEntries(byModel),\n      totalTokens: this.records.reduce(\n        (sum, r) => sum + r.inputTokens + r.outputTokens, 0\n      ),\n    }\n  }\n\n  get totalTokens(): number {\n    return this.records.reduce(\n      (sum, r) => sum + r.inputTokens + r.outputTokens, 0\n    )\n  }\n}"}],interactive:'\n<h2>API 客户端互动解析</h2>\n\n<h3>第 1 步：理解流式传输</h3>\n<p>为什么 Claude Code 使用流式 API 而不是普通的请求/响应模式？</p>\n<p>想象 Claude 生成一段 2000 token 的代码。非流式模式下，用户需要等待 <strong>10-20 秒</strong>才能看到任何输出。流式模式下，第一个 token 通常在 <strong>200-500ms</strong> 内到达，用户立即看到 Claude 开始"打字"。这种<strong>感知延迟</strong>的降低对用户体验至关重要。</p>\n\n<h3>第 2 步：Thinking 模式的权衡</h3>\n<p>三种 Thinking 模式各有适用场景：</p>\n<ul>\n<li><strong>adaptive</strong> — 最推荐。简单问题（"这个文件在哪？"）不浪费 Thinking token；复杂问题（"重构这个模块"）自动启用深度思考。token 节省可达 30-50%</li>\n<li><strong>enabled</strong> — 适用于"我知道接下来的任务很复杂"的场景，确保 Claude 不会跳过思考步骤</li>\n<li><strong>disabled</strong> — 适用于低延迟场景，如 Tab 补全或简单的问答</li>\n</ul>\n\n<h3>第 3 步：提示缓存的经济学</h3>\n<p>假设你的系统提示词有 5000 tokens，对话已进行 20 轮（约 10000 tokens 的历史）。每次新的 API 调用都需要重新发送这 15000 tokens。</p>\n<ul>\n<li><strong>无缓存</strong>：每次请求计费 15000 input tokens</li>\n<li><strong>有缓存</strong>：首次请求计费 15000 tokens + 缓存创建费用；后续请求中被缓存的部分（如系统提示词的 5000 tokens）计费降低约 90%</li>\n<li><strong>实际效果</strong>：20 轮对话可节省约 40-60% 的 input token 费用</li>\n</ul>\n\n<h3>第 4 步：重试策略的数学</h3>\n<p>指数退避 + 抖动的实际延迟序列（假设 baseDelay=1000ms）：</p>\n<ol>\n<li>第 1 次重试：1000ms \xd7 2^0 + 抖动 ≈ 1.0-1.1s</li>\n<li>第 2 次重试：1000ms \xd7 2^1 + 抖动 ≈ 2.0-2.2s</li>\n<li>第 3 次重试：1000ms \xd7 2^2 + 抖动 ≈ 4.0-4.4s</li>\n</ol>\n<p>总等待时间约 7-8 秒。如果 3 次都失败，说明问题不是瞬态的，继续重试没有意义。</p>\n\n<h3>第 5 步：预连接的时机</h3>\n<p><code>preconnectAnthropicApi()</code> 在 <code>init()</code> 阶段调用，此时用户还没有输入第一条消息。当用户开始输入时，TCP 连接和 TLS 握手已经完成。这意味着：</p>\n<ul>\n<li>用户按下回车的<strong>瞬间</strong>，API 请求就可以发出</li>\n<li>没有 200ms 的 TLS 握手等待</li>\n<li>首次 token 到达时间（Time to First Token）大幅降低</li>\n</ul>\n\n<h3>关键设计洞察</h3>\n<ul>\n<li><strong>层次化抽象</strong>：消息规范化 → Schema 转换 → 缓存优化 → 重试 → SDK 调用，每层只关注自己的职责</li>\n<li><strong>成本意识</strong>：提示缓存、Thinking 模式选择、token 追踪都是为了优化 API 调用成本</li>\n<li><strong>延迟优化</strong>：流式传输 + TCP 预连接 + 缓存，三管齐下降低感知延迟</li>\n<li><strong>健壮性</strong>：指数退避重试 + 模型回退，确保在不稳定网络下也能工作</li>\n</ul>\n'},c06:{content:'\n<h2>工具架构概述</h2>\n<blockquote>Claude Code 的核心能力来自于其工具系统。每一个"工具"（Tool）都是一个独立的能力单元——文件读写、代码搜索、Bash 执行、Web 搜索等。工具架构定义了统一的接口约束、注册发现机制、上下文传递模式和特性门控策略，使得 42+ 种工具可以在同一个框架下协同工作。</blockquote>\n\n<h3>Tool&lt;Input, Output, P&gt; 接口：30+ 属性的"超级接口"</h3>\n<p><code>Tool.ts</code> 定义了工具系统最核心的泛型接口 <code>Tool&lt;Input, Output, P&gt;</code>。每个工具实现都必须满足这个接口的约束。三个泛型参数分别是：</p>\n<ul>\n<li><strong>Input</strong> — 工具的输入类型，由 Zod schema 定义（如 BashTool 的 <code>{ command: string, timeout?: number }</code>）</li>\n<li><strong>Output</strong> — 工具的输出类型，通常是 <code>string</code> 或结构化数据</li>\n<li><strong>P</strong> — 权限类型参数，用于类型安全的权限检查</li>\n</ul>\n<p>该接口包含 30+ 个属性和方法，按职责分为以下几组：</p>\n\n<h4>标识与发现</h4>\n<ul>\n<li><code>name</code> — 工具的唯一标识符（如 <code>"Bash"</code>、<code>"Read"</code>）</li>\n<li><code>aliases</code> — 别名列表，用于兼容旧名称</li>\n<li><code>searchHint</code> — 搜索提示词，供 ToolSearchTool 做关键词匹配</li>\n<li><code>userFacingName()</code> — 返回面向用户显示的名称</li>\n</ul>\n\n<h4>Schema 与校验</h4>\n<ul>\n<li><code>inputSchema</code> — Zod schema 定义，用于运行时校验输入</li>\n<li><code>inputJSONSchema</code> — 转换后的 JSON Schema，发送给 API</li>\n<li><code>validateInput()</code> — 自定义输入校验逻辑（超出 schema 的业务规则）</li>\n</ul>\n\n<h4>执行与结果</h4>\n<ul>\n<li><code>call(input, context)</code> — 核心执行方法，接收输入和 ToolUseContext</li>\n<li><code>maxResultSizeChars</code> — 结果最大字符数，超出则持久化到磁盘</li>\n<li><code>mapToolResultToToolResultBlockParam()</code> — 将结果转换为 API 兼容格式</li>\n</ul>\n\n<h4>权限与安全</h4>\n<ul>\n<li><code>checkPermissions()</code> — 检查当前上下文是否有权执行该工具</li>\n<li><code>preparePermissionMatcher()</code> — 准备权限匹配器（如 Bash 的命令分类）</li>\n<li><code>isReadOnly()</code> — 是否为只读工具（读文件、搜索等）</li>\n<li><code>isDestructive()</code> — 是否为破坏性工具（删除文件、强制推送等）</li>\n</ul>\n\n<h4>并发与中断</h4>\n<ul>\n<li><code>isConcurrencySafe()</code> — 是否支持并发执行（如 GlobTool 可以并发，BashTool 不行）</li>\n<li><code>interruptBehavior()</code> — 中断时的行为：<code>\'abort\'</code>（立即停止）或 <code>\'finish\'</code>（完成当前操作）</li>\n</ul>\n\n<h4>提示与渲染</h4>\n<ul>\n<li><code>description()</code> — 发送给 Claude 的工具描述（影响模型选择哪个工具）</li>\n<li><code>prompt()</code> — 额外的提示信息，注入到系统提示词中</li>\n<li><code>renderToolUseMessage()</code> — 在 UI 中渲染工具调用的消息</li>\n<li><code>toAutoClassifierInput()</code> — 转换为自动分类器（权限判断）的输入</li>\n</ul>\n\n<h3>ToolUseContext："上帝上下文"</h3>\n<p><code>ToolUseContext</code> 是传递给每个工具的 <code>call()</code> 方法的上下文对象，它携带了工具执行所需的一切信息：</p>\n<ul>\n<li><code>options</code> — 全局选项（工作目录、模型、权限模式等）</li>\n<li><code>abortController</code> — 用于中断当前工具执行的 AbortController</li>\n<li><code>readFileState</code> — 文件读取状态缓存（避免重复读取）</li>\n<li><code>getAppState()/setAppState()</code> — 全局应用状态的读写接口</li>\n<li><code>messages</code> — 当前对话的完整消息历史</li>\n<li><code>agentId</code> — 当前 Agent 的标识符（主 Agent 或子 Agent）</li>\n<li><code>queryTracking</code> — 查询追踪信息（用于遥测）</li>\n<li><code>contentReplacementState</code> — 内容替换状态（用于大结果的磁盘持久化）</li>\n</ul>\n\n<h3>ToolResult&lt;T&gt;：结构化返回值</h3>\n<p>工具的返回值不只是简单的字符串，而是一个结构化的 <code>ToolResult&lt;T&gt;</code>：</p>\n<ul>\n<li><code>data</code> — 实际的输出数据</li>\n<li><code>newMessages</code> — 工具执行过程中生成的新消息（如 AskUserQuestionTool 收到的用户回复）</li>\n<li><code>contextModifier</code> — 上下文修改器（如 SkillTool 加载 skill 后修改系统提示词）</li>\n<li><code>mcpMeta</code> — MCP 元数据（如果是 MCP 工具调用）</li>\n</ul>\n\n<h3>工具注册与发现</h3>\n<p><code>tools.ts</code> 是工具的注册中心，提供三个关键函数：</p>\n<ul>\n<li><code>getAllBaseTools()</code> — 返回所有 42+ 个内置工具实例的数组</li>\n<li><code>getTools()</code> — 在 <code>getAllBaseTools()</code> 基础上，根据 deny 规则和 <code>isEnabled()</code> 过滤不可用的工具</li>\n<li><code>assembleToolPool()</code> — 合并内置工具和 MCP 工具，使用缓存稳定排序确保工具顺序一致</li>\n</ul>\n\n<h3>特性门控与 DCE</h3>\n<p>Claude Code 使用 <code>bun:bundle</code> 的编译时死代码消除（DCE, Dead Code Elimination）实现特性门控：</p>\n<ul>\n<li>不同编译目标（如 OSS vs 内部版本）会定义不同的特性标志</li>\n<li>编译器在打包时将关闭的特性对应的代码完全移除</li>\n<li>这意味着某些工具在特定版本中根本不存在于最终产物中，而不是运行时判断</li>\n</ul>\n\n<h3>循环依赖破解</h3>\n<p>由于工具系统与其他模块（如查询循环、权限系统）存在循环依赖，Claude Code 使用了延迟 <code>require()</code> 模式：</p>\n<ul>\n<li>在文件顶层不导入有循环依赖的模块</li>\n<li>在函数体内使用 <code>require()</code> 延迟加载</li>\n<li>这确保模块初始化顺序正确，避免导入时获得 <code>undefined</code></li>\n</ul>\n',architecture:'\n<h2>工具架构图</h2>\n\n<h3>核心类型层次</h3>\n<div style="overflow-x:auto">\n<table>\n<thead><tr><th>类型</th><th>职责</th><th>关键字段/方法</th></tr></thead>\n<tbody>\n<tr><td>Tool&lt;I,O,P&gt;</td><td>工具接口（30+ 属性）</td><td>name, call(), checkPermissions(), inputSchema</td></tr>\n<tr><td>ToolUseContext</td><td>执行上下文（"上帝对象"）</td><td>options, abortController, messages, readFileState</td></tr>\n<tr><td>ToolPermissionContext</td><td>权限上下文</td><td>mode, alwaysAllowRules, alwaysDenyRules</td></tr>\n<tr><td>ToolResult&lt;T&gt;</td><td>结构化返回值</td><td>data, newMessages, contextModifier, mcpMeta</td></tr>\n</tbody>\n</table>\n</div>\n\n<h3>工具发现流程</h3>\n<ol>\n<li><strong>getAllBaseTools()</strong> — 实例化 42+ 个内置工具</li>\n<li><strong>getTools(deny)</strong> — 过滤被禁用的工具（deny rules + isEnabled()）</li>\n<li><strong>assembleToolPool(built-in, MCP)</strong> — 合并内置 + MCP 工具，缓存稳定排序</li>\n<li><strong>发送给 API</strong> — 将工具池序列化为 JSON Schema 格式的工具定义</li>\n</ol>\n\n<h3>Tool 接口属性分组</h3>\n<ul>\n<li><strong>标识组 (4)</strong>：name, aliases, searchHint, userFacingName()</li>\n<li><strong>Schema 组 (3)</strong>：inputSchema, inputJSONSchema, validateInput()</li>\n<li><strong>执行组 (3)</strong>：call(), maxResultSizeChars, mapToolResultToToolResultBlockParam()</li>\n<li><strong>权限组 (4)</strong>：checkPermissions(), preparePermissionMatcher(), isReadOnly(), isDestructive()</li>\n<li><strong>并发组 (2)</strong>：isConcurrencySafe(), interruptBehavior()</li>\n<li><strong>提示组 (4)</strong>：description(), prompt(), renderToolUseMessage(), toAutoClassifierInput()</li>\n<li><strong>门控组 (1)</strong>：isEnabled()</li>\n</ul>\n\n<h3>特性门控机制</h3>\n<p>编译时 DCE（Dead Code Elimination）流程：</p>\n<ol>\n<li>定义特性标志常量（如 <code>FEATURE_MCP = true</code>）</li>\n<li>代码中使用 <code>if (FEATURE_MCP) { ... }</code> 条件分支</li>\n<li>Bun bundler 在编译时求值常量条件</li>\n<li>不可达分支被完全移除，最终产物中不包含相关代码</li>\n</ol>\n\n<h3>设计决策</h3>\n<blockquote>\n<p><strong>为什么 ToolUseContext 是"上帝对象"？</strong></p>\n<p>在工具系统中，每个工具可能需要访问截然不同的上下文信息。BashTool 需要 abortController 来中断进程，FileReadTool 需要 readFileState 做缓存，SkillTool 需要 messages 来理解对话历史。与其让每个工具单独获取这些依赖，不如把所有可能需要的上下文打包成一个对象传入。这虽然违反了"最小知识原则"，但在实践中大幅简化了工具的编写和注册流程。</p>\n</blockquote>\n',codeExamples:[{title:"Tool<I,O,P> 核心接口定义",language:"typescript",code:"// Tool.ts — 简化版核心接口\nimport { z } from 'zod'\n\nexport interface Tool<\n  Input = unknown,\n  Output = unknown,\n  P = unknown\n> {\n  // === 标识与发现 ===\n  name: string\n  aliases?: string[]\n  searchHint?: string\n  userFacingName(): string\n\n  // === Schema ===\n  inputSchema: z.ZodType<Input>\n  inputJSONSchema: Record<string, unknown>\n  validateInput?(input: Input): Promise<string | null>\n\n  // === 执行 ===\n  call(input: Input, context: ToolUseContext): Promise<ToolResult<Output>>\n  maxResultSizeChars?: number\n\n  // === 权限 ===\n  checkPermissions(\n    input: Input,\n    context: ToolPermissionContext\n  ): Promise<PermissionCheckResult>\n  isReadOnly(): boolean\n  isDestructive(): boolean\n\n  // === 并发与中断 ===\n  isConcurrencySafe(): boolean\n  interruptBehavior(): 'abort' | 'finish'\n\n  // === 提示与渲染 ===\n  description(): string\n  prompt?(): string\n  isEnabled?(): boolean\n  renderToolUseMessage?(input: Input): ToolUseMessage\n}\n\n// ToolUseContext — 工具执行的\"上帝上下文\"\nexport interface ToolUseContext {\n  options: GlobalOptions\n  abortController: AbortController\n  readFileState: ReadFileState\n  getAppState: () => AppState\n  setAppState: (updater: (state: AppState) => AppState) => void\n  messages: Message[]\n  agentId: string\n  queryTracking: QueryTracking\n  contentReplacementState: ContentReplacementState\n}\n\n// ToolResult<T> — 结构化返回值\nexport interface ToolResult<T = string> {\n  data: T\n  newMessages?: Message[]\n  contextModifier?: (context: SystemPromptContext) => SystemPromptContext\n  mcpMeta?: McpMeta\n}"},{title:"工具注册与发现机制",language:"typescript",code:"// tools.ts — 工具注册中心（简化版）\n\n// 1. 获取所有内置工具\nexport function getAllBaseTools(): Tool[] {\n  return [\n    new BashTool(),\n    new FileReadTool(),\n    new FileEditTool(),\n    new FileWriteTool(),\n    new GlobTool(),\n    new GrepTool(),\n    new LSPTool(),\n    new WebFetchTool(),\n    new WebSearchTool(),\n    new NotebookEditTool(),\n    new SkillTool(),\n    new AskUserQuestionTool(),\n    new ToolSearchTool(),\n    new TodoWriteTool(),\n    new EnterPlanModeTool(),\n    new ExitPlanModeV2Tool(),\n    // ... 42+ 工具实例\n  ]\n}\n\n// 2. 按规则过滤不可用的工具\nexport function getTools(\n  denyRules: DenyRule[] = []\n): Tool[] {\n  return getAllBaseTools().filter(tool => {\n    // 特性门控：编译时 DCE 可能已移除某些工具\n    if (tool.isEnabled && !tool.isEnabled()) return false\n\n    // 运行时禁用规则\n    if (denyRules.some(rule => matchTool(rule, tool.name))) {\n      return false\n    }\n\n    return true\n  })\n}\n\n// 3. 合并内置 + MCP 工具\nexport function assembleToolPool(\n  builtInTools: Tool[],\n  mcpTools: Tool[]\n): Tool[] {\n  const allTools = [...builtInTools, ...mcpTools]\n\n  // 缓存稳定排序：确保相同的工具集合\n  // 总是产生相同的排序顺序\n  // 这对 API 缓存命中很重要\n  return stableSortTools(allTools)\n}\n\nfunction stableSortTools(tools: Tool[]): Tool[] {\n  // 内置工具保持注册顺序\n  // MCP 工具按名称字母序排列\n  // 合并时内置工具在前，MCP 在后\n  const builtIn = tools.filter(t => !t.isMcpTool)\n  const mcp = tools\n    .filter(t => t.isMcpTool)\n    .sort((a, b) => a.name.localeCompare(b.name))\n  return [...builtIn, ...mcp]\n}"},{title:"特性门控与循环依赖破解",language:"typescript",code:"// === 特性门控：编译时 DCE ===\n\n// features.ts — 特性标志（编译时常量）\n// Bun bundler 会在编译时将这些替换为字面量\nexport const FEATURE_MCP = true        // MCP 工具支持\nexport const FEATURE_POWER_SHELL = false // PowerShell (仅 Windows)\nexport const FEATURE_WORKTREE = true    // Git worktree 支持\n\n// 在工具注册中使用\nimport { FEATURE_MCP, FEATURE_POWER_SHELL } from './features'\n\nexport function getAllBaseTools(): Tool[] {\n  const tools: Tool[] = [\n    new BashTool(),\n    new FileReadTool(),\n    // ... 核心工具\n  ]\n\n  // 编译时 DCE：如果 FEATURE_POWER_SHELL = false,\n  // 整个分支（包括 PowerShellTool 的 import）被移除\n  if (FEATURE_POWER_SHELL) {\n    tools.push(new PowerShellTool())\n  }\n\n  // 编译时 DCE：MCP 相关代码仅在启用时保留\n  if (FEATURE_MCP) {\n    // MCP 工具在 assembleToolPool 中动态添加\n  }\n\n  return tools\n}\n\n// === 循环依赖破解：延迟 require() ===\n\n// 问题：Tool → QueryLoop → Tool（循环）\n// 解决方案：在函数体内延迟加载\n\nexport class SkillTool implements Tool {\n  async call(input: SkillInput, context: ToolUseContext) {\n    // 延迟 require，避免循环依赖\n    // 如果在文件顶层 import，模块初始化时\n    // QueryLoop 可能还未完成导出\n    const { runSubConversation } = require('../queryLoop')\n\n    return runSubConversation({\n      skill: input.skill,\n      messages: context.messages,\n      // ...\n    })\n  }\n}\n\n// 另一个常见模式：类型导入不会引起循环依赖\nimport type { QueryLoop } from '../queryLoop' // OK: 仅类型\n// import { QueryLoop } from '../queryLoop' // 危险: 运行时导入"}],interactive:'\n<h2>工具架构互动解析</h2>\n\n<h3>第 1 步：理解 30+ 属性接口的设计意图</h3>\n<p>为什么一个工具接口需要 30+ 个属性？让我们从一个工具的"生命周期"来理解：</p>\n<ol>\n<li><strong>发现阶段</strong> — Claude 需要知道有哪些工具可用（name, description, searchHint）</li>\n<li><strong>选择阶段</strong> — Claude 决定使用哪个工具（inputJSONSchema 告诉 Claude 参数格式）</li>\n<li><strong>权限阶段</strong> — 系统检查是否允许执行（checkPermissions, isReadOnly, isDestructive）</li>\n<li><strong>执行阶段</strong> — 实际运行工具逻辑（call, validateInput, maxResultSizeChars）</li>\n<li><strong>渲染阶段</strong> — 在 UI 中展示结果（renderToolUseMessage, userFacingName）</li>\n<li><strong>编排阶段</strong> — 多工具并发调度（isConcurrencySafe, interruptBehavior）</li>\n</ol>\n<p>每个阶段都需要工具提供不同的信息，这就是为什么接口如此"丰富"。</p>\n\n<h3>第 2 步：ToolUseContext 的"上帝对象"模式</h3>\n<p>ToolUseContext 包含了工具可能需要的所有上下文。这是一种务实的设计选择：</p>\n<ul>\n<li><strong>优点</strong>：新增工具时不需要修改调用方代码，只需要从 context 中取需要的数据</li>\n<li><strong>优点</strong>：工具签名统一，所有工具的 call 方法参数一致</li>\n<li><strong>缺点</strong>：工具可以访问它不需要的数据（如 BashTool 可以访问 readFileState）</li>\n<li><strong>权衡</strong>：在 42+ 工具的规模下，统一接口的生产力收益远大于松耦合的理论优势</li>\n</ul>\n\n<h3>第 3 步：assembleToolPool 的缓存稳定排序</h3>\n<p>为什么工具排序需要"缓存稳定"？这与 Anthropic API 的提示缓存有关：</p>\n<ul>\n<li>API 请求中的 <code>tools</code> 数组是请求的一部分</li>\n<li>如果工具顺序变化，整个请求的哈希变化，缓存失效</li>\n<li>通过保证相同工具集合始终以相同顺序排列，缓存命中率大幅提高</li>\n<li>这是一个典型的"看不见的优化"——用户无感知但节省大量 token 成本</li>\n</ul>\n\n<h3>第 4 步：编译时 DCE vs 运行时判断</h3>\n<p>传统做法是运行时 <code>if (config.enableFeature)</code>，但 Claude Code 选择了编译时 DCE：</p>\n<ul>\n<li><strong>运行时判断</strong>：代码仍在最终产物中，只是不执行。增加包大小和攻击面</li>\n<li><strong>编译时 DCE</strong>：代码从最终产物中完全移除。包更小，不可能意外启用</li>\n<li><strong>代价</strong>：需要为不同特性集合构建不同的二进制文件</li>\n<li><strong>适用场景</strong>：OSS 版本 vs 内部版本等需要完全隔离的场景</li>\n</ul>\n\n<h3>第 5 步：循环依赖是如何产生的？</h3>\n<p>工具系统中循环依赖的典型案例：</p>\n<ol>\n<li>SkillTool 需要调用 QueryLoop 来运行子对话</li>\n<li>QueryLoop 需要 getTools() 来获取工具列表</li>\n<li>getTools() 实例化所有工具，包括 SkillTool</li>\n<li>形成闭环：SkillTool → QueryLoop → getTools() → SkillTool</li>\n</ol>\n<p>延迟 <code>require()</code> 打破了这个闭环：SkillTool 在定义时不导入 QueryLoop，只在 <code>call()</code> 被实际调用时才加载。此时所有模块都已完成初始化，不会得到 undefined。</p>\n\n<h3>关键设计洞察</h3>\n<ul>\n<li><strong>接口驱动</strong>：30+ 属性的接口看似笨重，但确保了工具系统的统一性和可预测性</li>\n<li><strong>务实 > 纯洁</strong>：ToolUseContext 的"上帝对象"模式牺牲了耦合度，换取了开发效率</li>\n<li><strong>缓存意识</strong>：工具排序的稳定性直接影响 API token 成本</li>\n<li><strong>编译时安全</strong>：DCE 确保不同版本之间的特性隔离在物理层面不可逆</li>\n</ul>\n'},c07:{content:'\n<h2>工具执行引擎概述</h2>\n<blockquote>工具架构定义了"工具是什么"，而工具执行引擎则决定了"工具怎么跑"。它管理并发调度、流式执行、权限校验、钩子集成、结果预算控制和错误分类。这是 Claude Code 从"收到模型输出"到"产生真实副作用"的关键路径。</blockquote>\n\n<h3>StreamingToolExecutor：流式并发执行器</h3>\n<p><code>StreamingToolExecutor</code> 是工具执行引擎的顶层管理器。当 Claude 的流式回复中包含多个 tool_use 块时，该执行器负责：</p>\n<ul>\n<li><strong>流式缓冲</strong> — 工具调用的 JSON 参数是流式到达的（一个 token 一个 token）。执行器将这些 delta 累积到缓冲区，直到完整的 JSON 输入到达</li>\n<li><strong>即时启动</strong> — 一旦某个工具的参数完整，立即开始执行，不等待其他工具</li>\n<li><strong>结果排序</strong> — 多个工具可能以不同顺序完成，但结果必须按 API 中 tool_use 块的原始顺序返回，以保证对话历史的一致性</li>\n<li><strong>错误隔离</strong> — 一个工具的失败不应影响其他工具的执行（除非它们是兄弟 Bash 命令）</li>\n</ul>\n\n<h3>toolOrchestration.ts：并发编排</h3>\n<p><code>partitionToolCalls()</code> 是并发编排的核心函数。它将一批工具调用分为两组：</p>\n<ul>\n<li><strong>并发组（parallel batch）</strong> — 所有 <code>isConcurrencySafe() === true</code> 的工具调用。这些工具不会互相干扰（如多个 GlobTool 或 GrepTool 调用），可以安全地使用 <code>Promise.all()</code> 并发执行</li>\n<li><strong>串行组（serial batch）</strong> — 所有 <code>isConcurrencySafe() === false</code> 的工具调用。这些工具可能有副作用或资源竞争（如 BashTool、FileWriteTool），必须逐个执行</li>\n</ul>\n<p>执行顺序：先执行并发组（所有并发安全的工具同时启动），再执行串行组（逐个执行）。这最大化了吞吐量，同时保证了安全性。</p>\n\n<h3>runToolUse() 执行管线</h3>\n<p><code>toolExecution.ts</code> 中的 <code>runToolUse()</code> 是单个工具调用的完整执行管线，按以下步骤执行：</p>\n<ol>\n<li><strong>查找工具</strong> — 根据 tool_use 块中的 name 查找匹配的工具实例</li>\n<li><strong>PreToolUse 钩子</strong> — 执行所有注册的前置钩子（可拦截、修改输入或直接提供结果）</li>\n<li><strong>权限检查</strong> — 调用 <code>tool.checkPermissions()</code> 判断是否允许执行</li>\n<li><strong>输入校验</strong> — 先用 Zod schema 校验，再调用 <code>tool.validateInput()</code> 做业务校验</li>\n<li><strong>执行工具</strong> — 调用 <code>tool.call(input, context)</code> 执行核心逻辑</li>\n<li><strong>PostToolUse 钩子</strong> — 执行所有注册的后置钩子（可修改结果、触发副作用）</li>\n<li><strong>构建返回值</strong> — 将 ToolResult 转换为 API 兼容的 <code>ToolResultBlockParam</code></li>\n<li><strong>文件历史追踪</strong> — 如果工具修改了文件，记录变更到文件历史中</li>\n</ol>\n\n<h3>兄弟 AbortController</h3>\n<p>当 Claude 同时调用多个 BashTool 时（如一个编译命令和一个测试命令），系统为同一批次的 Bash 调用创建"兄弟" AbortController：</p>\n<ul>\n<li>如果其中一个 Bash 命令出错（如编译失败），其兄弟进程会被立即终止</li>\n<li>这避免了无意义的等待（编译都失败了，测试没有意义）</li>\n<li>兄弟关系仅限于同一个 tool_use 批次中的 Bash 调用</li>\n</ul>\n\n<h3>工具结果预算</h3>\n<p><code>toolResultStorage.ts</code> 管理工具结果的大小控制：</p>\n<ul>\n<li>每个工具有 <code>maxResultSizeChars</code> 限制（默认 30000 字符）</li>\n<li>超大结果（如 <code>cat</code> 一个巨大文件）不会全部放入对话历史</li>\n<li>超大结果被持久化到磁盘的临时文件中</li>\n<li>对话历史中只保留一个预览（前 N 行 + "结果已截断" 提示）</li>\n<li>这防止了对话 token 数的爆炸性增长</li>\n</ul>\n\n<h3>错误分类与遥测</h3>\n<p>工具执行中的错误被分类为不同类型，用于遥测和用户反馈：</p>\n<ul>\n<li><strong>TelemetrySafeError</strong> — 可安全发送到遥测服务的错误（不包含敏感信息）</li>\n<li><strong>errno 错误</strong> — 系统错误码（如 ENOENT、EACCES），映射到用户友好的消息</li>\n<li><strong>稳定名称</strong> — 每种错误有一个稳定的字符串标识符，用于错误聚合和趋势分析</li>\n</ul>\n\n<h3>toolHooks.ts：钩子集成</h3>\n<p>工具钩子允许在工具执行的前后注入自定义逻辑：</p>\n<ul>\n<li><strong>PreToolUse 钩子</strong> — 在工具执行前调用。可以拦截调用（返回替代结果）、修改输入参数、执行额外的权限检查</li>\n<li><strong>PostToolUse 钩子</strong> — 在工具执行后调用。可以修改工具结果、触发通知、记录审计日志</li>\n<li>钩子通过 <code>settings.json</code> 配置，以外部命令形式运行</li>\n</ul>\n',architecture:'\n<h2>工具执行引擎架构</h2>\n\n<h3>核心模块</h3>\n<div style="overflow-x:auto">\n<table>\n<thead><tr><th>模块</th><th>职责</th><th>关键函数/类</th></tr></thead>\n<tbody>\n<tr><td>StreamingToolExecutor</td><td>流式并发执行管理</td><td>execute(), bufferDelta(), flushResults()</td></tr>\n<tr><td>toolOrchestration.ts</td><td>并发分组编排</td><td>partitionToolCalls()</td></tr>\n<tr><td>toolExecution.ts</td><td>单工具执行管线</td><td>runToolUse()</td></tr>\n<tr><td>toolResultStorage.ts</td><td>大结果持久化</td><td>persistLargeResult(), getResultPreview()</td></tr>\n<tr><td>toolHooks.ts</td><td>钩子集成层</td><td>runPreToolUseHooks(), runPostToolUseHooks()</td></tr>\n</tbody>\n</table>\n</div>\n\n<h3>runToolUse() 完整管线</h3>\n<ol>\n<li><strong>Find Tool</strong> → 在工具池中查找 tool_use.name 对应的工具实例</li>\n<li><strong>PreToolUse Hooks</strong> → 依次执行前置钩子（可拦截或修改）</li>\n<li><strong>checkPermissions()</strong> → 调用工具的权限检查方法</li>\n<li><strong>validateInput()</strong> → Zod schema + 自定义校验</li>\n<li><strong>tool.call()</strong> → 执行核心逻辑，传入 ToolUseContext</li>\n<li><strong>PostToolUse Hooks</strong> → 依次执行后置钩子</li>\n<li><strong>Build ToolResultBlockParam</strong> → 序列化为 API 格式</li>\n<li><strong>Track File History</strong> → 记录文件变更</li>\n</ol>\n\n<h3>并发编排策略</h3>\n<ul>\n<li><strong>阶段 1：分组</strong>\n  <ul>\n  <li>partitionToolCalls() 遍历所有工具调用</li>\n  <li>检查每个工具的 isConcurrencySafe() 返回值</li>\n  <li>分为 parallelBatch 和 serialBatch</li>\n  </ul>\n</li>\n<li><strong>阶段 2：并发执行</strong>\n  <ul>\n  <li>Promise.all(parallelBatch.map(runToolUse))</li>\n  <li>所有并发安全的工具同时启动</li>\n  </ul>\n</li>\n<li><strong>阶段 3：串行执行</strong>\n  <ul>\n  <li>for (const call of serialBatch) await runToolUse(call)</li>\n  <li>有副作用的工具逐个执行</li>\n  </ul>\n</li>\n</ul>\n\n<h3>兄弟 Abort 机制</h3>\n<p>同一批次的 Bash 工具调用共享一个 "sibling AbortController"：</p>\n<ul>\n<li>创建时：为每批 Bash 调用创建一个共享的 AbortController</li>\n<li>触发时：任一 Bash 命令 error → controller.abort()</li>\n<li>效果：所有兄弟 Bash 进程收到 SIGTERM</li>\n<li>范围：仅限同一 tool_use 批次，不同批次独立</li>\n</ul>\n\n<h3>设计决策</h3>\n<blockquote>\n<p><strong>为什么结果要持久化到磁盘而不是截断？</strong></p>\n<p>直接截断会丢失信息——如果 Claude 需要文件末尾的内容，截断后就无法访问了。持久化到磁盘后，后续的工具调用（如带 offset 参数的 FileReadTool）仍然可以访问完整内容。对话历史中的预览只是为了让 Claude 知道"内容很长，需要分段查看"。</p>\n</blockquote>\n',codeExamples:[{title:"runToolUse() 完整执行管线",language:"typescript",code:"// toolExecution.ts — 简化版\nexport async function runToolUse(\n  toolCall: ToolUseBlock,\n  tools: Tool[],\n  context: ToolUseContext,\n  hooks: ToolHooks\n): Promise<ToolResultBlockParam> {\n  // 1. 查找工具\n  const tool = tools.find(t =>\n    t.name === toolCall.name ||\n    t.aliases?.includes(toolCall.name)\n  )\n  if (!tool) {\n    return {\n      type: 'tool_result',\n      tool_use_id: toolCall.id,\n      content: 'Unknown tool: ' + toolCall.name,\n      is_error: true,\n    }\n  }\n\n  // 2. PreToolUse 钩子\n  const preHookResult = await hooks.runPreToolUseHooks({\n    tool,\n    input: toolCall.input,\n    context,\n  })\n  if (preHookResult?.intercepted) {\n    // 钩子提供了替代结果，跳过实际执行\n    return preHookResult.result\n  }\n  // 钩子可能修改了输入\n  const input = preHookResult?.modifiedInput ?? toolCall.input\n\n  // 3. 权限检查\n  const permResult = await tool.checkPermissions(\n    input,\n    context.options.permissionContext\n  )\n  if (permResult.denied) {\n    // 需要用户确认或直接拒绝\n    return await handlePermissionDenial(permResult, tool, context)\n  }\n\n  // 4. 输入校验\n  const parseResult = tool.inputSchema.safeParse(input)\n  if (!parseResult.success) {\n    return buildErrorResult(toolCall.id, parseResult.error)\n  }\n  if (tool.validateInput) {\n    const validationError = await tool.validateInput(parseResult.data)\n    if (validationError) {\n      return buildErrorResult(toolCall.id, validationError)\n    }\n  }\n\n  // 5. 执行工具\n  let toolResult: ToolResult\n  try {\n    toolResult = await tool.call(parseResult.data, context)\n  } catch (error) {\n    // 错误分类\n    const classified = classifyError(error)\n    trackTelemetry('tool_error', {\n      tool: tool.name,\n      errorName: classified.stableName,\n    })\n    return buildErrorResult(toolCall.id, classified.message)\n  }\n\n  // 6. PostToolUse 钩子\n  toolResult = await hooks.runPostToolUseHooks({\n    tool,\n    input,\n    result: toolResult,\n    context,\n  })\n\n  // 7. 结果预算控制\n  const resultParam = await buildToolResultParam(\n    toolCall.id, tool, toolResult\n  )\n\n  // 8. 文件历史追踪\n  if (!tool.isReadOnly()) {\n    trackFileHistory(tool.name, input, context)\n  }\n\n  return resultParam\n}"},{title:"并发编排与兄弟 Abort",language:"typescript",code:"// toolOrchestration.ts — 简化版\n\ninterface PartitionedCalls {\n  parallelBatch: ToolUseBlock[]\n  serialBatch: ToolUseBlock[]\n}\n\nexport function partitionToolCalls(\n  toolCalls: ToolUseBlock[],\n  tools: Tool[]\n): PartitionedCalls {\n  const parallelBatch: ToolUseBlock[] = []\n  const serialBatch: ToolUseBlock[] = []\n\n  for (const call of toolCalls) {\n    const tool = tools.find(t => t.name === call.name)\n    if (tool?.isConcurrencySafe()) {\n      parallelBatch.push(call)\n    } else {\n      serialBatch.push(call)\n    }\n  }\n\n  return { parallelBatch, serialBatch }\n}\n\n// 执行编排\nexport async function executeToolCalls(\n  toolCalls: ToolUseBlock[],\n  tools: Tool[],\n  context: ToolUseContext\n): Promise<ToolResultBlockParam[]> {\n  const { parallelBatch, serialBatch } = partitionToolCalls(\n    toolCalls, tools\n  )\n\n  // 阶段 1：并发执行（所有并发安全的工具同时启动）\n  const parallelResults = await Promise.all(\n    parallelBatch.map(call => runToolUse(call, tools, context))\n  )\n\n  // 阶段 2：串行执行（有副作用的工具逐个执行）\n  const serialResults: ToolResultBlockParam[] = []\n\n  // 为同批次 Bash 调用创建兄弟 AbortController\n  const siblingAbort = new AbortController()\n  const bashCalls = serialBatch.filter(c => c.name === 'Bash')\n\n  for (const call of serialBatch) {\n    // 如果兄弟 Bash 已失败，跳过剩余 Bash 调用\n    if (call.name === 'Bash' && siblingAbort.signal.aborted) {\n      serialResults.push({\n        type: 'tool_result',\n        tool_use_id: call.id,\n        content: 'Skipped: sibling Bash command failed',\n        is_error: true,\n      })\n      continue\n    }\n\n    try {\n      const result = await runToolUse(call, tools, {\n        ...context,\n        abortController: call.name === 'Bash'\n          ? siblingAbort\n          : context.abortController,\n      })\n      serialResults.push(result)\n    } catch (error) {\n      // Bash 错误触发兄弟 abort\n      if (call.name === 'Bash' && bashCalls.length > 1) {\n        siblingAbort.abort()\n      }\n      serialResults.push(buildErrorResult(call.id, error))\n    }\n  }\n\n  // 按原始顺序合并结果\n  return mergeResultsInOrder(\n    toolCalls, parallelResults, serialResults\n  )\n}"},{title:"结果预算控制与错误分类",language:"typescript",code:"// toolResultStorage.ts — 大结果持久化\nimport { writeFile, mkdtemp } from 'fs/promises'\nimport { join } from 'path'\nimport { tmpdir } from 'os'\n\nconst DEFAULT_MAX_CHARS = 30_000\n\nexport async function buildToolResultParam(\n  toolUseId: string,\n  tool: Tool,\n  result: ToolResult\n): Promise<ToolResultBlockParam> {\n  const maxChars = tool.maxResultSizeChars ?? DEFAULT_MAX_CHARS\n  const content = typeof result.data === 'string'\n    ? result.data\n    : JSON.stringify(result.data)\n\n  // 结果在预算内：直接返回\n  if (content.length <= maxChars) {\n    return {\n      type: 'tool_result',\n      tool_use_id: toolUseId,\n      content,\n    }\n  }\n\n  // 结果超预算：持久化到磁盘\n  const tmpDir = await mkdtemp(join(tmpdir(), 'cc-result-'))\n  const filePath = join(tmpDir, 'result.txt')\n  await writeFile(filePath, content, 'utf-8')\n\n  // 生成预览（前 100 行 + 截断提示）\n  const lines = content.split('\\n')\n  const previewLines = lines.slice(0, 100)\n  const preview = [\n    ...previewLines,\n    '',\n    '... (结果共 ' + lines.length + ' 行，' + content.length + ' 字符)',\n    '完整结果已保存到: ' + filePath,\n    '使用 Read 工具配合 offset 参数查看完整内容',\n  ].join('\\n')\n\n  return {\n    type: 'tool_result',\n    tool_use_id: toolUseId,\n    content: preview,\n  }\n}\n\n// 错误分类系统\nexport interface ClassifiedError {\n  stableName: string      // 用于遥测聚合\n  message: string         // 用户友好的消息\n  isTelemetrySafe: boolean // 是否可安全发送\n}\n\nexport function classifyError(error: unknown): ClassifiedError {\n  // TelemetrySafeError：开发者标记的安全错误\n  if (error instanceof TelemetrySafeError) {\n    return {\n      stableName: error.name,\n      message: error.message,\n      isTelemetrySafe: true,\n    }\n  }\n\n  // 系统 errno 错误\n  if (isErrnoError(error)) {\n    const mapped = ERRNO_MAP[error.code] ?? {\n      name: 'unknown_system_error',\n      message: '系统错误: ' + error.code,\n    }\n    return { ...mapped, isTelemetrySafe: true }\n  }\n\n  // 未知错误：不发送详细信息到遥测\n  return {\n    stableName: 'unknown_error',\n    message: error instanceof Error\n      ? error.message\n      : 'An unknown error occurred',\n    isTelemetrySafe: false,\n  }\n}\n\nconst ERRNO_MAP: Record<string, { name: string; message: string }> = {\n  ENOENT:  { name: 'file_not_found',   message: '文件或目录不存在' },\n  EACCES:  { name: 'permission_denied', message: '没有访问权限' },\n  EISDIR:  { name: 'is_directory',      message: '目标是目录，不是文件' },\n  ENOSPC:  { name: 'no_space',          message: '磁盘空间不足' },\n  EMFILE:  { name: 'too_many_files',    message: '打开的文件数过多' },\n}"}],interactive:'\n<h2>工具执行引擎互动解析</h2>\n\n<h3>第 1 步：理解流式工具执行的挑战</h3>\n<p>Claude 的回复是流式的。当 Claude 决定调用工具时，工具的参数也是逐 token 到达的：</p>\n<pre>\ntoken 1: {"type":"tool_use","name":"Bash","input":{\ntoken 2: "command":"ls\ntoken 3:  -la /\ntoken 4: tmp"}}\n</pre>\n<p>StreamingToolExecutor 面临的挑战：</p>\n<ul>\n<li>什么时候参数"完整"了？（需要解析 JSON 嵌套层级）</li>\n<li>如果同时有 3 个工具调用流入，如何管理 3 个独立的缓冲区？</li>\n<li>如果工具 A 先完成但工具 B 先在流中出现，结果如何排序？</li>\n</ul>\n\n<h3>第 2 步：并发编排的智慧</h3>\n<p>想象 Claude 同时调用 3 个工具：2 个 Grep + 1 个 FileWrite。partitionToolCalls 会这样处理：</p>\n<ul>\n<li><strong>并发组</strong>：2 个 GrepTool（只读、无副作用、可并发）</li>\n<li><strong>串行组</strong>：1 个 FileWriteTool（有副作用、不可并发）</li>\n<li><strong>执行顺序</strong>：先并发执行 2 个 Grep（～200ms），再执行 FileWrite（～50ms）</li>\n<li><strong>如果全串行</strong>：200ms + 200ms + 50ms = 450ms</li>\n<li><strong>并发+串行</strong>：200ms + 50ms = 250ms（节省 44%）</li>\n</ul>\n\n<h3>第 3 步：兄弟 Abort 的实际场景</h3>\n<p>Claude 经常同时调用多个 Bash 命令，比如：</p>\n<ol>\n<li><code>npm run build</code>（编译项目）</li>\n<li><code>npm run test</code>（运行测试）</li>\n<li><code>npm run lint</code>（代码检查）</li>\n</ol>\n<p>如果编译在第 2 秒失败了，测试和 lint 都没有意义了。兄弟 AbortController 确保编译失败后，测试和 lint 进程立即被 SIGTERM 终止，而不是白白等待它们完成。</p>\n\n<h3>第 4 步：结果预算的必要性</h3>\n<p>考虑这个场景：Claude 执行 <code>cat large_file.log</code>，文件有 100 万行。</p>\n<ul>\n<li><strong>无预算控制</strong>：100 万行全部进入对话历史，下一次 API 调用的 input tokens 暴增，成本飙升，可能超过 context window</li>\n<li><strong>有预算控制</strong>：前 100 行作为预览放入对话历史，完整内容存磁盘。Claude 看到预览后可以说"让我读取第 500-600 行"，使用 FileReadTool 的 offset 参数精确获取</li>\n<li><strong>效果</strong>：对话 token 数保持可控，Claude 仍能访问完整信息</li>\n</ul>\n\n<h3>第 5 步：钩子系统的力量</h3>\n<p>PreToolUse 和 PostToolUse 钩子赋予用户扩展工具行为的能力：</p>\n<ul>\n<li><strong>自动审批</strong>：PreToolUse 钩子检查工具调用是否匹配白名单，自动放行</li>\n<li><strong>日志审计</strong>：PostToolUse 钩子将每次工具调用记录到审计日志</li>\n<li><strong>结果改写</strong>：PostToolUse 钩子过滤敏感信息（如 API Key）后再返回</li>\n<li><strong>拦截替换</strong>：PreToolUse 钩子可以完全替代工具执行（如用缓存结果代替实际 Bash 调用）</li>\n</ul>\n\n<h3>关键设计洞察</h3>\n<ul>\n<li><strong>流式友好</strong>：整个引擎围绕"token 逐个到达"设计，而非"一次性拿到完整输入"</li>\n<li><strong>渐进式并发</strong>：不是简单地全并发或全串行，而是按工具特性智能分组</li>\n<li><strong>弹性预算</strong>：大结果不会炸掉对话，但完整信息不会丢失</li>\n<li><strong>可观测性</strong>：错误分类、遥测追踪、文件历史记录确保问题可排查</li>\n</ul>\n'},c08:{content:'\n<h2>内置工具全景概述</h2>\n<blockquote>Claude Code 内置了 42+ 种工具，涵盖文件操作、代码搜索、命令执行、Web 访问、代码智能等多个领域。每种工具都针对特定场景做了深度优化。本章深入剖析最核心的几个工具的内部实现。</blockquote>\n\n<h3>BashTool：最复杂的工具（20+ 文件）</h3>\n<p>BashTool 是整个工具系统中最复杂的工具，由 20+ 个文件组成。它不仅要执行 shell 命令，还要理解命令的语义、评估安全风险、决定是否沙箱化。</p>\n\n<h4>命令分类系统</h4>\n<p>BashTool 将 shell 命令分为多个类别，每个类别有不同的权限和行为策略：</p>\n<ul>\n<li><code>BASH_SEARCH_COMMANDS</code> — 搜索类命令（grep、find、rg 等）：只读，可并发</li>\n<li><code>BASH_READ_COMMANDS</code> — 读取类命令（cat、head、tail 等）：只读，可并发</li>\n<li><code>BASH_LIST_COMMANDS</code> — 列表类命令（ls、tree、du 等）：只读，可并发</li>\n<li><code>BASH_SILENT_COMMANDS</code> — 静默类命令（cd、pwd 等）：无副作用，自动允许</li>\n</ul>\n<p>不属于以上类别的命令被视为"可能有副作用"，需要更严格的权限检查。</p>\n\n<h4>AST 分析（treeSitterAnalysis）</h4>\n<p>BashTool 使用 Tree-sitter 将 shell 命令解析为 AST（抽象语法树），而不是简单的字符串匹配：</p>\n<ul>\n<li>能正确识别管道：<code>cat file | grep pattern</code> 中 cat 是只读的</li>\n<li>能识别命令替换：<code>$(rm -rf /)</code> 被嵌入到看似无害的命令中</li>\n<li>能识别重定向：<code>echo "data" > file</code> 虽然用了 echo 但有写操作</li>\n<li>能识别 && 和 || 链：<code>test -f file && rm file</code> 包含破坏性操作</li>\n</ul>\n\n<h4>沙箱决策（shouldUseSandbox）</h4>\n<p>对于不信任的命令，BashTool 可以在沙箱中执行：</p>\n<ul>\n<li>判断逻辑综合考虑：命令类别、用户权限模式、沙箱可用性</li>\n<li>沙箱通过 <code>sandbox-adapter.ts</code> 对接不同的沙箱实现</li>\n<li>沙箱内的命令无法访问主机文件系统（除显式挂载的目录）</li>\n</ul>\n\n<h4>sed 编辑检测</h4>\n<p>BashTool 能检测用户是否通过 <code>sed</code> 命令编辑文件，并提示用户使用更安全的 FileEditTool：</p>\n<ul>\n<li>检测 <code>sed -i</code>（in-place 编辑）模式</li>\n<li>提供"建议使用 Edit 工具"的提示，因为 Edit 工具有冲突检测</li>\n</ul>\n\n<h3>FileEditTool：精确文本替换</h3>\n<p>FileEditTool 实现了 <code>old_string → new_string</code> 的精确替换逻辑：</p>\n<ul>\n<li><strong>唯一性检测</strong> — old_string 必须在文件中唯一匹配。如果匹配到多处，工具报错并提示用户提供更多上下文以区分</li>\n<li><strong>Diff 计算</strong> — 替换前后计算 diff，用于 UI 展示和版本历史</li>\n<li><strong>replace_all 模式</strong> — 可选的全局替换模式，允许替换所有匹配项</li>\n<li><strong>空白敏感</strong> — 精确匹配缩进和空白字符，避免意外替换</li>\n</ul>\n\n<h3>FileReadTool：多格式智能读取</h3>\n<p>FileReadTool 不只是简单的文件读取，它支持多种文件格式：</p>\n<ul>\n<li><strong>文本文件</strong> — 带行号输出（<code>cat -n</code> 格式），支持 offset 和 limit 参数</li>\n<li><strong>图片</strong> — PNG/JPG 等图片经过处理管线：检测尺寸 → 按需 resize → 降采样 → 转为 base64 发送给多模态 Claude</li>\n<li><strong>PDF</strong> — 支持页面范围读取（<code>pages: "1-5"</code>），大 PDF 强制要求指定页面范围</li>\n<li><strong>Jupyter Notebook</strong> — 解析 .ipynb JSON 格式，展示代码单元、输出和可视化</li>\n<li><strong>大小限制</strong> — 默认读取前 2000 行，超大文件提示用户使用 offset 分段读取</li>\n</ul>\n\n<h3>FileWriteTool：智能写入</h3>\n<p>FileWriteTool 在写入文件时做了多项智能检测：</p>\n<ul>\n<li><strong>编码检测</strong> — 自动检测现有文件的编码（UTF-8、Latin-1 等），新文件使用同样编码写入</li>\n<li><strong>行尾检测</strong> — 检测现有文件使用 LF 还是 CRLF 行尾，保持一致性</li>\n<li><strong>文件历史</strong> — 写入前记录文件状态到历史中，支持后续的 diff 查看和撤销</li>\n</ul>\n\n<h3>GlobTool：高速文件搜索</h3>\n<p>GlobTool 使用 ripgrep 的文件匹配引擎（而非 Node.js 的 glob 库）：</p>\n<ul>\n<li>支持标准 glob 模式（<code>**/*.ts</code>、<code>src/**/*.{js,jsx}</code>）</li>\n<li>结果按文件修改时间排序（最近修改的文件排在前面）</li>\n<li>性能远超 Node.js 原生 glob，处理大型仓库（10万+ 文件）依然快速</li>\n</ul>\n\n<h3>GrepTool：多模式搜索</h3>\n<p>GrepTool 是 ripgrep 的包装器，提供三种输出模式：</p>\n<ul>\n<li><code>content</code> — 显示匹配行的完整内容，支持 -A/-B/-C 上下文行</li>\n<li><code>files_with_matches</code> — 只显示包含匹配的文件路径（默认模式）</li>\n<li><code>count</code> — 显示每个文件的匹配次数</li>\n</ul>\n\n<h3>LSPTool：代码智能</h3>\n<p>LSPTool 通过 Language Server Protocol 提供 IDE 级别的代码智能：</p>\n<ul>\n<li><code>goToDefinition</code> — 跳转到符号定义</li>\n<li><code>findReferences</code> — 查找所有引用</li>\n<li><code>hover</code> — 获取悬停信息（类型、文档）</li>\n<li><code>documentSymbol</code> — 获取文件中的所有符号</li>\n<li><code>workspaceSymbol</code> — 全工作区符号搜索</li>\n<li><code>goToImplementation</code> — 查找接口实现</li>\n<li><code>prepareCallHierarchy</code> / <code>incomingCalls</code> / <code>outgoingCalls</code> — 调用层次分析</li>\n</ul>\n\n<h3>其他工具</h3>\n<p>除上述核心工具外，Claude Code 还包含：</p>\n<ul>\n<li><strong>WebFetchTool</strong> — 获取网页内容并转换为 Markdown</li>\n<li><strong>WebSearchTool</strong> — Web 搜索并返回结构化结果</li>\n<li><strong>NotebookEditTool</strong> — 编辑 Jupyter Notebook 单元格</li>\n<li><strong>PowerShellTool</strong> — Windows PowerShell 执行（特性门控）</li>\n<li>总计 <strong>42+</strong> 种工具，覆盖开发工作流的各个环节</li>\n</ul>\n',architecture:'\n<h2>内置工具架构</h2>\n\n<h3>工具分类矩阵</h3>\n<div style="overflow-x:auto">\n<table>\n<thead><tr><th>工具</th><th>只读?</th><th>并发安全?</th><th>复杂度</th><th>关键特性</th></tr></thead>\n<tbody>\n<tr><td>BashTool</td><td>视命令</td><td>否</td><td>极高（20+ 文件）</td><td>AST 分析、沙箱、命令分类</td></tr>\n<tr><td>FileEditTool</td><td>否</td><td>否</td><td>中</td><td>唯一性检测、diff 计算</td></tr>\n<tr><td>FileReadTool</td><td>是</td><td>是</td><td>中</td><td>多格式支持、图片处理</td></tr>\n<tr><td>FileWriteTool</td><td>否</td><td>否</td><td>低</td><td>编码检测、行尾检测</td></tr>\n<tr><td>GlobTool</td><td>是</td><td>是</td><td>低</td><td>ripgrep 引擎、mod-time 排序</td></tr>\n<tr><td>GrepTool</td><td>是</td><td>是</td><td>低</td><td>三种输出模式、上下文行</td></tr>\n<tr><td>LSPTool</td><td>是</td><td>是</td><td>中</td><td>8 种 LSP 操作</td></tr>\n<tr><td>WebFetchTool</td><td>是</td><td>是</td><td>中</td><td>HTML→Markdown 转换</td></tr>\n<tr><td>WebSearchTool</td><td>是</td><td>是</td><td>低</td><td>结构化搜索结果</td></tr>\n</tbody>\n</table>\n</div>\n\n<h3>BashTool 内部架构</h3>\n<p>BashTool 的 20+ 文件按职责分层：</p>\n<ol>\n<li><strong>入口层</strong>：BashTool.ts — call() 方法，统筹整个执行流程</li>\n<li><strong>分析层</strong>：treeSitterAnalysis.ts — AST 解析和命令语义理解</li>\n<li><strong>分类层</strong>：commandClassification.ts — 命令类别判断</li>\n<li><strong>安全层</strong>：shouldUseSandbox.ts — 沙箱决策逻辑</li>\n<li><strong>执行层</strong>：processExecution.ts — 子进程管理和输出捕获</li>\n<li><strong>检测层</strong>：sedDetection.ts — sed 编辑检测和建议</li>\n</ol>\n\n<h3>FileReadTool 图片处理管线</h3>\n<ol>\n<li><strong>格式检测</strong> → 通过文件扩展名和 magic bytes 判断类型</li>\n<li><strong>尺寸检测</strong> → 读取图片元数据获取宽\xd7高</li>\n<li><strong>Resize 决策</strong> → 超过阈值（如 2048px）则缩小</li>\n<li><strong>降采样</strong> → 减少颜色深度以减小体积</li>\n<li><strong>Base64 编码</strong> → 转换为 API 兼容的 base64 字符串</li>\n<li><strong>嵌入消息</strong> → 作为 image content block 发送给 Claude</li>\n</ol>\n\n<h3>设计决策</h3>\n<blockquote>\n<p><strong>为什么 BashTool 要用 Tree-sitter 而不是正则表达式？</strong></p>\n<p>Shell 命令的语法极其复杂：嵌套引号、命令替换、进程替换、here-doc 等。正则表达式无法可靠地解析 <code>echo "$(cat file | grep \'pattern\')" > output</code> 这样的命令。Tree-sitter 提供了完整的 AST，可以准确识别每个子命令、每个重定向、每个替换。虽然增加了依赖和复杂度，但这是安全性的基础。</p>\n</blockquote>\n',codeExamples:[{title:"BashTool 命令分类与 AST 分析",language:"typescript",code:"// tools/BashTool/commandClassification.ts — 简化版\n\n// 命令分类常量\nexport const BASH_SEARCH_COMMANDS = new Set([\n  'grep', 'rg', 'find', 'fd', 'ag', 'ack',\n  'locate', 'which', 'whereis', 'type',\n])\n\nexport const BASH_READ_COMMANDS = new Set([\n  'cat', 'head', 'tail', 'less', 'more',\n  'wc', 'file', 'stat', 'md5sum', 'sha256sum',\n])\n\nexport const BASH_LIST_COMMANDS = new Set([\n  'ls', 'tree', 'du', 'df', 'find', 'exa',\n])\n\nexport const BASH_SILENT_COMMANDS = new Set([\n  'cd', 'pwd', 'echo', 'printf', 'true', 'false',\n  'test', '[', '[[',\n])\n\n// treeSitterAnalysis.ts — AST 分析\nimport Parser from 'tree-sitter'\nimport Bash from 'tree-sitter-bash'\n\nexport function analyzeCommand(command: string): CommandAnalysis {\n  const parser = new Parser()\n  parser.setLanguage(Bash)\n  const tree = parser.parse(command)\n\n  const analysis: CommandAnalysis = {\n    commands: [],       // 所有命令名\n    hasRedirection: false,\n    hasPipe: false,\n    hasCommandSubstitution: false,\n    hasDestructiveOp: false,\n    isReadOnly: true,\n  }\n\n  // 遍历 AST 节点\n  function visit(node: Parser.SyntaxNode) {\n    switch (node.type) {\n      case 'command':\n        const cmdName = node.firstChild?.text ?? ''\n        analysis.commands.push(cmdName)\n        if (!isReadOnlyCommand(cmdName)) {\n          analysis.isReadOnly = false\n        }\n        break\n\n      case 'redirected_statement':\n      case 'file_redirect':\n        analysis.hasRedirection = true\n        analysis.isReadOnly = false // 重定向意味着写操作\n        break\n\n      case 'pipeline':\n        analysis.hasPipe = true\n        break\n\n      case 'command_substitution':\n        analysis.hasCommandSubstitution = true\n        break\n    }\n\n    for (const child of node.children) {\n      visit(child)\n    }\n  }\n\n  visit(tree.rootNode)\n  return analysis\n}\n\nfunction isReadOnlyCommand(cmd: string): boolean {\n  return BASH_SEARCH_COMMANDS.has(cmd)\n    || BASH_READ_COMMANDS.has(cmd)\n    || BASH_LIST_COMMANDS.has(cmd)\n    || BASH_SILENT_COMMANDS.has(cmd)\n}"},{title:"FileEditTool 精确替换逻辑",language:"typescript",code:"// tools/FileEditTool.ts — 简化版\nimport { z } from 'zod'\nimport { readFile, writeFile } from 'fs/promises'\nimport { createPatch } from 'diff'\n\nexport class FileEditTool implements Tool {\n  name = 'Edit'\n\n  inputSchema = z.object({\n    file_path: z.string().describe('要编辑的文件绝对路径'),\n    old_string: z.string().describe('要替换的文本'),\n    new_string: z.string().describe('替换后的新文本'),\n    replace_all: z.boolean().optional().default(false)\n      .describe('是否替换所有匹配项'),\n  })\n\n  async call(\n    input: z.infer<typeof this.inputSchema>,\n    context: ToolUseContext\n  ): Promise<ToolResult> {\n    const { file_path, old_string, new_string, replace_all } = input\n\n    // 读取文件\n    const content = await readFile(file_path, 'utf-8')\n\n    // 唯一性检测\n    if (!replace_all) {\n      const matchCount = countOccurrences(content, old_string)\n\n      if (matchCount === 0) {\n        return {\n          data: '错误: old_string 在文件中未找到。\\n' +\n            '请确认要替换的文本与文件内容完全匹配（包括缩进和空白）。',\n        }\n      }\n\n      if (matchCount > 1) {\n        // 非唯一匹配：提示用户提供更多上下文\n        const locations = findMatchLocations(content, old_string)\n        return {\n          data: '错误: old_string 在文件中匹配到 ' + matchCount + ' 处。\\n' +\n            '匹配位置: 行 ' + locations.join(', ') + '\\n' +\n            '请提供更多上下文使 old_string 唯一，或使用 replace_all=true。',\n        }\n      }\n    }\n\n    // 执行替换\n    const newContent = replace_all\n      ? content.replaceAll(old_string, new_string)\n      : content.replace(old_string, new_string)\n\n    // 计算 diff（用于 UI 展示）\n    const diff = createPatch(file_path, content, newContent)\n\n    // 记录文件历史\n    context.readFileState.recordEdit(file_path, content)\n\n    // 写入文件\n    await writeFile(file_path, newContent, 'utf-8')\n\n    return {\n      data: '文件已更新: ' + file_path + '\\n\\n' + diff,\n    }\n  }\n\n  isReadOnly() { return false }\n  isConcurrencySafe() { return false }\n  isDestructive() { return false }\n}\n\nfunction countOccurrences(text: string, search: string): number {\n  let count = 0\n  let pos = 0\n  while ((pos = text.indexOf(search, pos)) !== -1) {\n    count++\n    pos += search.length\n  }\n  return count\n}"},{title:"FileReadTool 多格式支持",language:"typescript",code:"// tools/FileReadTool.ts — 简化版\nimport { readFile, stat } from 'fs/promises'\nimport { extname } from 'path'\nimport sharp from 'sharp'\n\nconst IMAGE_EXTENSIONS = new Set(['.png', '.jpg', '.jpeg', '.gif', '.webp'])\nconst MAX_LINES = 2000\nconst MAX_IMAGE_DIMENSION = 2048\n\nexport class FileReadTool implements Tool {\n  name = 'Read'\n\n  async call(\n    input: { file_path: string; offset?: number; limit?: number; pages?: string },\n    context: ToolUseContext\n  ): Promise<ToolResult> {\n    const ext = extname(input.file_path).toLowerCase()\n\n    // 图片处理管线\n    if (IMAGE_EXTENSIONS.has(ext)) {\n      return this.readImage(input.file_path)\n    }\n\n    // PDF 处理\n    if (ext === '.pdf') {\n      return this.readPDF(input.file_path, input.pages)\n    }\n\n    // Jupyter Notebook 处理\n    if (ext === '.ipynb') {\n      return this.readNotebook(input.file_path)\n    }\n\n    // 文本文件：带行号输出\n    return this.readText(input.file_path, input.offset, input.limit)\n  }\n\n  private async readText(\n    filePath: string,\n    offset = 0,\n    limit = MAX_LINES\n  ): Promise<ToolResult> {\n    const content = await readFile(filePath, 'utf-8')\n    const lines = content.split('\\n')\n\n    const selectedLines = lines.slice(offset, offset + limit)\n    const numbered = selectedLines.map(\n      (line, i) => (offset + i + 1).toString().padStart(6) + '\\t' + line\n    )\n\n    let result = numbered.join('\\n')\n    if (offset + limit < lines.length) {\n      result += '\\n\\n... (文件共 ' + lines.length + ' 行，' +\n        '已显示 ' + (offset + 1) + '-' + (offset + limit) + ' 行)'\n    }\n\n    return { data: result }\n  }\n\n  private async readImage(filePath: string): Promise<ToolResult> {\n    // 1. 读取元数据\n    const metadata = await sharp(filePath).metadata()\n    const { width = 0, height = 0 } = metadata\n\n    // 2. Resize 决策\n    let image = sharp(filePath)\n    if (width > MAX_IMAGE_DIMENSION || height > MAX_IMAGE_DIMENSION) {\n      image = image.resize(MAX_IMAGE_DIMENSION, MAX_IMAGE_DIMENSION, {\n        fit: 'inside',          // 保持比例\n        withoutEnlargement: true, // 不放大\n      })\n    }\n\n    // 3. 转为 base64\n    const buffer = await image.toBuffer()\n    const base64 = buffer.toString('base64')\n\n    return {\n      data: base64,\n      // 作为 image content block 传给 Claude\n    }\n  }\n\n  isReadOnly() { return true }\n  isConcurrencySafe() { return true }\n}"}],interactive:'\n<h2>内置工具互动解析</h2>\n\n<h3>第 1 步：BashTool 的安全层次</h3>\n<p>BashTool 面临一个核心矛盾：它必须足够强大以执行任意命令，又必须足够安全以防止危险操作。解决方案是多层防御：</p>\n<ol>\n<li><strong>第一层：命令分类</strong> — 已知安全的命令（grep、ls、cat）自动放行</li>\n<li><strong>第二层：AST 分析</strong> — 解析命令结构，识别重定向、管道、子命令中的风险</li>\n<li><strong>第三层：权限检查</strong> — 未知命令交给权限系统（用户确认或分类器判断）</li>\n<li><strong>第四层：沙箱执行</strong> — 高风险命令在隔离环境中执行</li>\n</ol>\n<p>这种"层层筛选"的模式确保了：安全命令快速执行，危险命令被充分审查。</p>\n\n<h3>第 2 步：FileEditTool 为什么坚持"唯一匹配"？</h3>\n<p>想象以下场景：</p>\n<pre>\n// 文件中有 3 处 "const x = 1"\nold_string: "const x = 1"\nnew_string: "const x = 2"\n</pre>\n<p>如果直接替换，Claude 可能意外修改了它不打算修改的那一处。唯一匹配要求 Claude 提供足够的上下文：</p>\n<pre>\n// 正确的做法：提供更多上下文使匹配唯一\nold_string: "  // 在 processData 函数中\\n  const x = 1"\nnew_string: "  // 在 processData 函数中\\n  const x = 2"\n</pre>\n<p>这迫使 Claude "知道自己在改哪里"，避免了盲目替换。</p>\n\n<h3>第 3 步：为什么 Glob 和 Grep 不用 Node.js 原生库？</h3>\n<p>ripgrep 的性能优势在大型仓库中尤为明显：</p>\n<ul>\n<li><strong>Node.js glob</strong>：10 万文件搜索耗时 ~5 秒（JS 实现，单线程）</li>\n<li><strong>ripgrep</strong>：10 万文件搜索耗时 ~0.2 秒（Rust 实现，多线程，SIMD 加速）</li>\n<li>在 monorepo（百万+文件）中，差距更加明显</li>\n<li>Claude Code 需要频繁搜索文件，ripgrep 的速度直接影响用户体验</li>\n</ul>\n\n<h3>第 4 步：图片处理为什么要 resize 和降采样？</h3>\n<p>Claude 的多模态能力可以理解图片，但图片大小直接影响 token 成本：</p>\n<ul>\n<li>一张 4096\xd74096 的 PNG 截图可能有 10MB</li>\n<li>Base64 编码后变为 ~13MB 的文本</li>\n<li>发送给 API 会消耗大量 input tokens</li>\n<li>resize 到 2048\xd72048 后质量几乎无损，但体积减小 75%</li>\n<li>对于代码截图，这个尺寸完全足够 Claude 阅读</li>\n</ul>\n\n<h3>第 5 步：LSPTool 的价值</h3>\n<p>为什么 Claude Code 需要 LSP 而不是让 Claude 自己通过 Grep 查找定义？</p>\n<ul>\n<li><strong>精确性</strong>：Grep 搜索 <code>function foo</code> 会匹配注释中的"// function foo"。LSP 通过语言服务器理解代码语义，只返回真正的定义</li>\n<li><strong>跨文件</strong>：<code>findReferences</code> 能找到所有导入和使用点，包括间接引用</li>\n<li><strong>类型信息</strong>：<code>hover</code> 能获取推断出的类型，Grep 做不到</li>\n<li><strong>调用层次</strong>：<code>incomingCalls/outgoingCalls</code> 能构建完整的调用图</li>\n</ul>\n\n<h3>关键设计洞察</h3>\n<ul>\n<li><strong>深度 > 广度</strong>：BashTool 用 20+ 文件深度解决一个问题，而不是用一个文件粗略处理</li>\n<li><strong>安全第一</strong>：命令分类 + AST 分析 + 沙箱，三层防御保护用户文件系统</li>\n<li><strong>格式适配</strong>：FileReadTool 不是"读文件"，而是"让 Claude 理解文件内容"</li>\n<li><strong>原生工具</strong>：选择 ripgrep 而非 JS 库，选择 Tree-sitter 而非正则，体现了"用正确的工具做正确的事"</li>\n</ul>\n'},c09:{content:'\n<h2>计划与工作流工具概述</h2>\n<blockquote>除了直接操作文件和代码的工具之外，Claude Code 还有一组"元工具"——它们不直接修改代码，而是帮助 Claude 规划工作、与用户交互、管理任务、加载技能。这些工具构成了 Claude Code 的"工作流引擎"，使其从简单的代码生成器进化为一个能自主规划和执行复杂任务的 AI 代理。</blockquote>\n\n<h3>Plan 模式：探索与执行的分离</h3>\n<p>Claude Code 支持在两种模式之间切换：</p>\n<ul>\n<li><strong>正常模式（Normal）</strong> — Claude 可以使用所有工具，包括写文件、执行命令等有副作用的操作</li>\n<li><strong>计划模式（Plan）</strong> — Claude 只能使用只读工具（读文件、搜索、Grep），不能修改任何内容</li>\n</ul>\n\n<h4>EnterPlanModeTool</h4>\n<p>当 Claude 遇到复杂任务时，它可以调用 <code>EnterPlanModeTool</code> 进入计划模式：</p>\n<ul>\n<li>系统自动禁用所有非只读工具</li>\n<li>Claude 可以自由探索代码库，理解架构和依赖关系</li>\n<li>不用担心误操作——物理上无法执行写操作</li>\n</ul>\n\n<h4>ExitPlanModeV2Tool</h4>\n<p>计划完成后，Claude 调用 <code>ExitPlanModeV2Tool</code> 退出计划模式：</p>\n<ul>\n<li>关键特性：<code>allowedPrompts</code> 参数——Claude 可以声明"我打算执行的操作清单"</li>\n<li>这个清单显示给用户，让用户在 Claude 开始执行前审查计划</li>\n<li>退出后恢复所有工具的可用性</li>\n</ul>\n\n<h3>SkillTool：技能加载系统</h3>\n<p>SkillTool 是 Claude Code 的"能力扩展机制"。技能（Skill）本质上是预定义的提示词模板，可以注入到对话中增强 Claude 在特定领域的表现。</p>\n\n<h4>技能来源</h4>\n<ul>\n<li><strong>用户自定义技能</strong> — 存储在 <code>.claude/skills/</code> 目录中，用户或团队编写</li>\n<li><strong>内置技能</strong> — 与 Claude Code 一起打包发布的 <code>bundled/</code> 技能</li>\n</ul>\n\n<h4>17 个内置技能</h4>\n<p>Claude Code 自带 17 个内置技能，覆盖常见的开发工作流：</p>\n<ul>\n<li><strong>batch</strong> — 批量处理：对多个文件或目标执行相同操作</li>\n<li><strong>claudeApi</strong> — Claude API 使用指南：帮助生成调用 Anthropic API 的代码</li>\n<li><strong>claudeInChrome</strong> — Chrome 扩展开发辅助</li>\n<li><strong>debug</strong> — 调试辅助：分析错误堆栈、建议调试策略</li>\n<li><strong>keybindings</strong> — 快捷键配置辅助</li>\n<li><strong>loop</strong> — 循环执行：定时重复执行某个操作</li>\n<li><strong>remember</strong> — 记忆管理：将信息写入 CLAUDE.md 以持久化</li>\n<li><strong>scheduleRemoteAgents</strong> — 远程代理调度</li>\n<li><strong>simplify</strong> — 代码简化：审查并简化复杂代码</li>\n<li><strong>skillify</strong> — 技能创建：帮助用户编写新技能</li>\n<li><strong>stuck</strong> — 困境突破：当 Claude 陷入循环时提供新思路</li>\n<li><strong>updateConfig</strong> — 配置更新辅助</li>\n<li><strong>verify</strong> — 验证工具：运行测试和检查以验证变更</li>\n</ul>\n\n<h4>技能执行机制</h4>\n<p>当 Claude 调用 SkillTool 时：</p>\n<ol>\n<li>根据技能名称查找技能定义文件</li>\n<li>读取技能的提示词模板</li>\n<li>将提示词注入到子对话中</li>\n<li>在子对话上下文中执行技能逻辑</li>\n<li>将结果返回主对话</li>\n</ol>\n\n<h3>ToolSearchTool：延迟工具发现</h3>\n<p>并非所有工具都需要在每次对话开始时就发送给 Claude。一些不常用的工具被标记为 <code>shouldDefer: true</code>，表示它们可以被延迟加载：</p>\n<ul>\n<li>这些工具在初始的 <code>tools</code> 数组中不出现</li>\n<li>当 Claude 需要时，调用 <code>ToolSearchTool</code> 搜索可用工具</li>\n<li>搜索基于 <code>searchHint</code> 关键词匹配</li>\n<li>找到后，工具被添加到当前会话的工具池中</li>\n<li>这减少了每次 API 调用的 token 数（工具定义也消耗 input tokens）</li>\n</ul>\n\n<h3>AskUserQuestionTool：结构化用户交互</h3>\n<p>当 Claude 需要用户输入时，它不是简单地在回复中提问，而是通过 <code>AskUserQuestionTool</code> 发起结构化的交互：</p>\n<ul>\n<li><strong>question</strong> — 向用户展示的问题文本</li>\n<li><strong>options</strong> — 可选项列表（如 ["是", "否", "跳过"]），渲染为按钮</li>\n<li><strong>multiSelect</strong> — 是否允许多选</li>\n<li><strong>preview</strong> — 预览内容（如即将应用的 diff）</li>\n</ul>\n<p>这提供了比纯文本对话更丰富的交互体验。</p>\n\n<h3>TodoWriteTool：任务跟踪</h3>\n<p>对于复杂的多步骤任务，Claude 可以使用 <code>TodoWriteTool</code> 创建和管理任务列表：</p>\n<ul>\n<li>创建带有 subject 和 description 的任务项</li>\n<li>标记任务状态：pending → in_progress → completed</li>\n<li>设置任务间的依赖关系（blocks/blockedBy）</li>\n<li>任务列表对用户可见，展示当前进度</li>\n</ul>\n<p>这使得 Claude 在处理复杂任务时更加透明和有组织。</p>\n\n<h3>BriefTool：文件上传支持</h3>\n<p><code>BriefTool</code> 支持用户通过文件上传/附件的方式向 Claude 提供信息：</p>\n<ul>\n<li>支持拖拽文件到终端</li>\n<li>支持粘贴剪贴板内容</li>\n<li>文件内容被转换为对话中的消息</li>\n</ul>\n',architecture:'\n<h2>计划与工作流工具架构</h2>\n\n<h3>核心模块</h3>\n<div style="overflow-x:auto">\n<table>\n<thead><tr><th>工具</th><th>职责</th><th>模式影响</th></tr></thead>\n<tbody>\n<tr><td>EnterPlanModeTool</td><td>进入计划模式（只读探索）</td><td>禁用写操作工具</td></tr>\n<tr><td>ExitPlanModeV2Tool</td><td>退出计划模式（带操作清单）</td><td>恢复所有工具</td></tr>\n<tr><td>SkillTool</td><td>加载和执行技能</td><td>注入提示词到子对话</td></tr>\n<tr><td>ToolSearchTool</td><td>搜索延迟加载的工具</td><td>添加工具到当前池</td></tr>\n<tr><td>AskUserQuestionTool</td><td>结构化用户交互</td><td>暂停等待用户输入</td></tr>\n<tr><td>TodoWriteTool</td><td>任务列表管理</td><td>展示进度给用户</td></tr>\n<tr><td>BriefTool</td><td>文件附件支持</td><td>文件内容注入对话</td></tr>\n</tbody>\n</table>\n</div>\n\n<h3>Plan 模式状态机</h3>\n<ul>\n<li><strong>Normal Mode</strong>\n  <ul>\n  <li>所有工具可用</li>\n  <li>Claude 调用 EnterPlanModeTool →</li>\n  </ul>\n</li>\n<li><strong>Plan Mode</strong>\n  <ul>\n  <li>仅只读工具可用（Read, Glob, Grep, LSP...）</li>\n  <li>Claude 自由探索代码库</li>\n  <li>Claude 调用 ExitPlanModeV2Tool(allowedPrompts) →</li>\n  </ul>\n</li>\n<li><strong>Back to Normal Mode</strong>\n  <ul>\n  <li>所有工具恢复</li>\n  <li>allowedPrompts 展示给用户</li>\n  <li>Claude 按计划执行</li>\n  </ul>\n</li>\n</ul>\n\n<h3>SkillTool 执行流程</h3>\n<ol>\n<li><strong>查找技能</strong> → 搜索 .claude/skills/ 和 bundled/ 目录</li>\n<li><strong>读取定义</strong> → 解析技能文件中的提示词模板</li>\n<li><strong>注入上下文</strong> → 将提示词注入子对话的系统提示中</li>\n<li><strong>执行子对话</strong> → 延迟 require(queryLoop) 启动子 QueryLoop</li>\n<li><strong>返回结果</strong> → 子对话的输出作为 ToolResult 返回主对话</li>\n</ol>\n\n<h3>工具延迟加载策略</h3>\n<ul>\n<li><strong>始终加载</strong>：BashTool, FileReadTool, FileEditTool, FileWriteTool, GlobTool, GrepTool（核心工具，几乎每次都用）</li>\n<li><strong>延迟加载（shouldDefer=true）</strong>：NotebookEditTool, WebSearchTool, LSPTool 等（按需发现）</li>\n<li><strong>触发条件</strong>：Claude 调用 ToolSearchTool，或系统检测到相关文件类型</li>\n</ul>\n\n<h3>设计决策</h3>\n<blockquote>\n<p><strong>为什么 Plan 模式要在工具层面禁用而不是提示词层面？</strong></p>\n<p>仅靠提示词"请不要修改文件"是不可靠的——模型可能在复杂推理中忘记这个约束。在工具层面禁用意味着即使 Claude 尝试调用 FileWriteTool，工具系统也会返回错误。这是"能力限制"而非"行为建议"，提供了硬性保证。</p>\n</blockquote>\n',codeExamples:[{title:"Plan 模式切换实现",language:"typescript",code:"// tools/EnterPlanModeTool.ts — 简化版\nexport class EnterPlanModeTool implements Tool {\n  name = 'EnterPlanMode'\n\n  inputSchema = z.object({\n    reason: z.string().optional()\n      .describe('进入计划模式的原因'),\n  })\n\n  description() {\n    return '进入计划模式。在此模式下你只能使用只读工具来探索' +\n      '代码库，不能修改任何文件。适用于需要先理解代码结构再动手的场景。'\n  }\n\n  async call(\n    input: { reason?: string },\n    context: ToolUseContext\n  ): Promise<ToolResult> {\n    // 切换全局模式为 Plan\n    context.setAppState(state => ({\n      ...state,\n      permissionMode: 'plan' as PermissionMode,\n    }))\n\n    return {\n      data: '已进入计划模式。你现在只能使用只读工具（Read, Glob, Grep, LSP 等）。' +\n        '请充分探索代码库，制定完整的实施计划后再退出。',\n    }\n  }\n\n  isReadOnly() { return true }\n  isConcurrencySafe() { return true }\n}\n\n// tools/ExitPlanModeV2Tool.ts — 简化版\nexport class ExitPlanModeV2Tool implements Tool {\n  name = 'ExitPlanMode'\n\n  inputSchema = z.object({\n    plan: z.string().describe('制定的实施计划'),\n    allowedPrompts: z.array(z.string()).optional()\n      .describe('计划执行的操作清单，展示给用户审查'),\n  })\n\n  async call(\n    input: { plan: string; allowedPrompts?: string[] },\n    context: ToolUseContext\n  ): Promise<ToolResult> {\n    // 恢复正常模式\n    context.setAppState(state => ({\n      ...state,\n      permissionMode: 'default' as PermissionMode,\n    }))\n\n    // 构建计划摘要\n    let summary = '计划已制定:\\n\\n' + input.plan\n    if (input.allowedPrompts?.length) {\n      summary += '\\n\\n计划执行的操作:\\n'\n      summary += input.allowedPrompts\n        .map((p, i) => (i + 1) + '. ' + p)\n        .join('\\n')\n    }\n\n    return {\n      data: summary,\n      // contextModifier 可以将计划注入后续的系统提示词\n      contextModifier: (ctx) => ({\n        ...ctx,\n        activePlan: input.plan,\n      }),\n    }\n  }\n\n  // 只在 Plan 模式下可用\n  isEnabled() {\n    return getAppState().permissionMode === 'plan'\n  }\n}"},{title:"SkillTool 技能加载与执行",language:"typescript",code:"// tools/SkillTool.ts — 简化版\nimport { readdir, readFile } from 'fs/promises'\nimport { join } from 'path'\n\ninterface SkillDefinition {\n  name: string\n  description: string\n  prompt: string\n  source: 'user' | 'bundled'\n}\n\nexport class SkillTool implements Tool {\n  name = 'Skill'\n\n  inputSchema = z.object({\n    skill: z.string().describe('要执行的技能名称'),\n    args: z.string().optional().describe('传给技能的参数'),\n  })\n\n  description() {\n    return '执行一个预定义的技能。技能提供特定领域的专业能力。'\n  }\n\n  async call(\n    input: { skill: string; args?: string },\n    context: ToolUseContext\n  ): Promise<ToolResult> {\n    // 1. 查找技能\n    const skill = await this.findSkill(input.skill, context)\n    if (!skill) {\n      const available = await this.listSkills(context)\n      return {\n        data: '未找到技能 \"' + input.skill + '\"。可用技能:\\n' +\n          available.map(s => '  - ' + s.name + ': ' + s.description).join('\\n'),\n      }\n    }\n\n    // 2. 构建技能提示词\n    const skillPrompt = this.buildPrompt(skill, input.args)\n\n    // 3. 延迟加载 queryLoop（避免循环依赖）\n    const { runSubConversation } = require('../services/queryLoop')\n\n    // 4. 在子对话中执行技能\n    const result = await runSubConversation({\n      systemPromptModifier: (basePrompt: string) =>\n        basePrompt + '\\n\\n' + skillPrompt,\n      messages: context.messages,\n      tools: context.options.tools,\n      abortSignal: context.abortController.signal,\n    })\n\n    return {\n      data: result.output,\n      contextModifier: skill.contextModifier,\n    }\n  }\n\n  private async findSkill(\n    name: string,\n    context: ToolUseContext\n  ): Promise<SkillDefinition | null> {\n    // 先搜索用户自定义技能\n    const userSkillPath = join(context.options.cwd, '.claude/skills', name)\n    try {\n      const content = await readFile(userSkillPath + '.md', 'utf-8')\n      return { name, prompt: content, description: '', source: 'user' }\n    } catch {}\n\n    // 再搜索内置技能\n    const bundledSkills = await this.getBundledSkills()\n    return bundledSkills.find(s =>\n      s.name === name || s.name.includes(name)\n    ) ?? null\n  }\n\n  private async getBundledSkills(): Promise<SkillDefinition[]> {\n    // 17 个内置技能\n    return [\n      { name: 'batch', description: '批量处理多个文件', prompt: '...', source: 'bundled' },\n      { name: 'debug', description: '调试辅助', prompt: '...', source: 'bundled' },\n      { name: 'simplify', description: '代码简化', prompt: '...', source: 'bundled' },\n      { name: 'verify', description: '验证变更', prompt: '...', source: 'bundled' },\n      { name: 'stuck', description: '困境突破', prompt: '...', source: 'bundled' },\n      // ... 共 17 个\n    ]\n  }\n}"},{title:"ToolSearchTool 延迟发现与 AskUserQuestionTool",language:"typescript",code:"// tools/ToolSearchTool.ts — 简化版\nexport class ToolSearchTool implements Tool {\n  name = 'ToolSearch'\n\n  inputSchema = z.object({\n    query: z.string().describe('搜索关键词'),\n  })\n\n  description() {\n    return '搜索可用但未加载的工具。' +\n      '某些工具（如 NotebookEdit、WebSearch）' +\n      '不是默认加载的，需要通过搜索发现。'\n  }\n\n  async call(\n    input: { query: string },\n    context: ToolUseContext\n  ): Promise<ToolResult> {\n    // 获取所有延迟加载的工具\n    const deferredTools = getAllBaseTools().filter(\n      t => t.shouldDefer === true\n    )\n\n    // 关键词匹配\n    const matches = deferredTools.filter(tool => {\n      const searchText = [\n        tool.name,\n        tool.searchHint ?? '',\n        tool.description(),\n      ].join(' ').toLowerCase()\n\n      return input.query.toLowerCase().split(/\\s+/).some(\n        keyword => searchText.includes(keyword)\n      )\n    })\n\n    if (matches.length === 0) {\n      return { data: '未找到匹配 \"' + input.query + '\" 的工具。' }\n    }\n\n    // 将找到的工具添加到当前会话\n    context.setAppState(state => ({\n      ...state,\n      additionalTools: [\n        ...(state.additionalTools ?? []),\n        ...matches,\n      ],\n    }))\n\n    return {\n      data: '找到 ' + matches.length + ' 个工具:\\n' +\n        matches.map(t =>\n          '  - ' + t.name + ': ' + t.description().slice(0, 100)\n        ).join('\\n') +\n        '\\n\\n这些工具已添加到当前会话，可以直接使用。',\n    }\n  }\n}\n\n// tools/AskUserQuestionTool.ts — 简化版\nexport class AskUserQuestionTool implements Tool {\n  name = 'AskUserQuestion'\n\n  inputSchema = z.object({\n    question: z.string().describe('向用户提出的问题'),\n    options: z.array(z.string()).optional()\n      .describe('选项列表，渲染为按钮'),\n    multiSelect: z.boolean().optional()\n      .describe('是否允许多选'),\n    preview: z.string().optional()\n      .describe('预览内容（如 diff）'),\n  })\n\n  async call(\n    input: {\n      question: string\n      options?: string[]\n      multiSelect?: boolean\n      preview?: string\n    },\n    context: ToolUseContext\n  ): Promise<ToolResult> {\n    // 等待用户通过 UI 回复\n    // 这会暂停 QueryLoop 直到用户输入\n    const userResponse = await waitForUserInput({\n      prompt: input.question,\n      options: input.options,\n      multiSelect: input.multiSelect,\n      preview: input.preview,\n    })\n\n    return {\n      data: userResponse.text,\n      newMessages: [{\n        role: 'user',\n        content: userResponse.text,\n      }],\n    }\n  }\n\n  isReadOnly() { return true }\n  isConcurrencySafe() { return false }\n}"}],interactive:'\n<h2>计划与工作流工具互动解析</h2>\n\n<h3>第 1 步：为什么需要 Plan 模式？</h3>\n<p>考虑一个复杂任务："重构这个项目的认证系统"。如果 Claude 直接开始修改代码，可能会：</p>\n<ul>\n<li>在不了解全貌的情况下修改了关键文件</li>\n<li>遗漏了某些依赖该模块的地方</li>\n<li>做出与项目惯例不一致的设计决策</li>\n</ul>\n<p>Plan 模式解决了这个问题：</p>\n<ol>\n<li>Claude 先进入 Plan 模式，只能读和搜索</li>\n<li>它浏览认证相关的所有文件，理解架构</li>\n<li>它搜索所有调用认证 API 的地方，了解影响范围</li>\n<li>制定完整计划后退出 Plan 模式</li>\n<li>按计划有序地修改代码</li>\n</ol>\n<p>这模拟了优秀程序员的工作方式：先理解，后动手。</p>\n\n<h3>第 2 步：Skill 系统的设计哲学</h3>\n<p>为什么用"注入提示词"而不是"编写代码"来实现技能？</p>\n<ul>\n<li><strong>灵活性</strong>：任何人都可以用自然语言编写技能，不需要编程知识</li>\n<li><strong>组合性</strong>：技能可以使用所有已有工具，不需要重新实现功能</li>\n<li><strong>可维护性</strong>：修改技能只需要编辑一个 .md 文件，不需要重新编译</li>\n<li><strong>安全性</strong>：技能在同一个权限体系下运行，不会绕过安全检查</li>\n</ul>\n\n<h3>第 3 步：延迟工具加载的经济学</h3>\n<p>每个工具的 JSON Schema 定义大约消耗 200-500 input tokens。42 个工具就是 8400-21000 tokens。</p>\n<ul>\n<li>如果用户只需要文件编辑，加载 WebSearchTool 和 NotebookEditTool 是浪费</li>\n<li>延迟加载可以将初始工具数减少到 ~20 个，节省约 50% 的工具定义 tokens</li>\n<li>在长对话中（每轮都发送工具定义），累积节省可达数万 tokens</li>\n</ul>\n\n<h3>第 4 步：AskUserQuestionTool vs 直接在回复中提问</h3>\n<p>Claude 可以在回复中直接写"请告诉我你想要哪种方案"，但 AskUserQuestionTool 提供了更好的体验：</p>\n<ul>\n<li><strong>选项按钮</strong>：用户可以点击而不是打字，减少输入错误</li>\n<li><strong>多选支持</strong>：用户可以同时选择多个选项</li>\n<li><strong>预览</strong>：在用户做决定之前展示变更的效果</li>\n<li><strong>结构化</strong>：用户的回复被结构化为 ToolResult，而不是需要 Claude 解析的自然语言</li>\n</ul>\n\n<h3>第 5 步：TodoWriteTool 的透明性价值</h3>\n<p>当 Claude 处理一个"把项目从 JavaScript 迁移到 TypeScript"这样的大任务时：</p>\n<ul>\n<li>Claude 创建 15 个子任务（如"迁移 utils/", "迁移 components/", "更新 tsconfig"...）</li>\n<li>用户可以在 UI 中看到任务列表和进度</li>\n<li>Claude 标记完成的任务，用户知道还剩多少工作</li>\n<li>如果会话中断，任务列表帮助 Claude 快速回忆进度</li>\n</ul>\n<p>这种透明性建立了用户对 AI 代理的信任——用户始终知道 Claude 在做什么、还要做什么。</p>\n\n<h3>关键设计洞察</h3>\n<ul>\n<li><strong>能力控制 > 行为建议</strong>：Plan 模式在工具层面限制能力，而非依赖提示词约束</li>\n<li><strong>提示词即代码</strong>：Skill 系统用自然语言模板代替代码，降低了扩展门槛</li>\n<li><strong>按需加载</strong>：延迟工具发现平衡了能力完备性和 token 成本</li>\n<li><strong>结构化交互</strong>：AskUserQuestionTool 将自然语言对话升级为结构化工具调用</li>\n<li><strong>透明执行</strong>：TodoWriteTool 让 AI 代理的工作过程对用户可见</li>\n</ul>\n'},c10:{content:'\n<h2>权限系统概述</h2>\n<blockquote>Claude Code 的权限系统是安全性的最后一道防线。它决定了 Claude 能做什么、不能做什么、需要用户确认后才能做什么。这套系统由 5 层决策逻辑、多种权限模式、命令分类器、沙箱适配器和交互式确认对话框构成。每一次工具调用都要经过这套系统的审查。</blockquote>\n\n<h3>5 层权限决策</h3>\n<p>当一个工具调用到达权限系统时，它按照以下 5 层顺序被评估，第一个做出决策的层"获胜"：</p>\n\n<h4>第 1 层：全局拒绝规则（Blanket Deny Rules）</h4>\n<p>最高优先级。这些规则定义了无论如何都不允许的操作：</p>\n<ul>\n<li>例如：禁止访问 <code>/etc/shadow</code>、禁止执行 <code>rm -rf /</code></li>\n<li>这些规则由系统硬编码，用户无法覆盖</li>\n<li>如果匹配，立即返回"拒绝"，后续层不再评估</li>\n</ul>\n\n<h4>第 2 层：始终允许规则（Always-Allow Rules）</h4>\n<p>用户配置的自动允许规则（在 <code>settings.json</code> 中定义）：</p>\n<ul>\n<li>例如：<code>"always_allow": ["Read", "Glob", "Grep"]</code> 自动允许所有只读工具</li>\n<li>Bash 命令可以使用模式匹配：<code>"Bash(npm test*)"</code> 允许所有以 <code>npm test</code> 开头的命令</li>\n<li>如果匹配，立即返回"允许"，跳过后续层</li>\n</ul>\n\n<h4>第 3 层：始终拒绝规则（Always-Deny Rules）</h4>\n<p>用户配置的自动拒绝规则：</p>\n<ul>\n<li>例如：<code>"always_deny": ["Bash(curl*)", "Bash(wget*)"]</code> 禁止网络下载命令</li>\n<li>优先级低于 always-allow，所以如果同一工具同时在两个列表中，always-allow 获胜</li>\n</ul>\n\n<h4>第 4 层：工具自身权限检查（Tool checkPermissions()）</h4>\n<p>每个工具实现自己的 <code>checkPermissions()</code> 方法：</p>\n<ul>\n<li>BashTool 会分析命令内容，判断是否需要用户确认</li>\n<li>FileWriteTool 检查目标路径是否在工作目录内</li>\n<li>这一层可以返回"允许"、"拒绝"或"需要用户确认"</li>\n</ul>\n\n<h4>第 5 层：分类器 / 用户确认（Classifier / User Prompt）</h4>\n<p>如果前 4 层都没有做出明确决策，系统进入最终判断：</p>\n<ul>\n<li>在默认模式下，弹出交互式对话框让用户确认</li>\n<li>在自动模式（YOLO/Auto）下，由分类器自动判断</li>\n<li>用户确认对话框显示工具名称、输入参数和预期行为</li>\n</ul>\n\n<h3>PermissionMode：权限模式</h3>\n<p>系统支持 4 种权限模式，决定了第 5 层的行为：</p>\n<ul>\n<li><strong>default</strong> — 默认模式。未通过前 4 层的调用会弹出用户确认对话框</li>\n<li><strong>plan</strong> — 计划模式。只读工具自动允许，写操作工具直接拒绝</li>\n<li><strong>bypassPermissions</strong> — 绕过权限。所有工具自动允许（仅用于内部测试）</li>\n<li><strong>auto</strong> — 自动模式（YOLO 模式）。由分类器自动判断，不打扰用户</li>\n</ul>\n\n<h3>Bash 分类器（bashClassifier.ts）</h3>\n<p>BashTool 的权限判断比其他工具更复杂，因为 shell 命令的风险差异极大。<code>bashClassifier.ts</code> 专门负责 Bash 命令的安全分类：</p>\n<ul>\n<li>结合命令的 AST 分析结果（来自 Tree-sitter）</li>\n<li>判断命令是否涉及文件修改、网络访问、进程管理等高风险操作</li>\n<li>为每个命令生成安全评分和风险标签</li>\n<li>根据评分决定是否需要用户确认</li>\n</ul>\n\n<h3>YOLO/Auto 模式分类器（yoloClassifier.ts）</h3>\n<p>在自动模式下，<code>yoloClassifier.ts</code> 负责在不打扰用户的情况下做安全判断：</p>\n<ul>\n<li>分析整个对话上下文（transcript-level），而不只是单个工具调用</li>\n<li>考虑 Claude 为什么要执行这个操作（基于对话历史）</li>\n<li>对明显安全的操作（如用户刚要求的文件编辑）自动放行</li>\n<li>对可疑操作（如未预期的 <code>rm</code> 命令）仍然拒绝</li>\n</ul>\n\n<h3>拒绝追踪与回退提示（Denial Tracking）</h3>\n<p>当分类器多次拒绝 Claude 的工具调用时，系统会触发回退逻辑：</p>\n<ul>\n<li><code>shouldFallbackToPrompting()</code> 检测重复拒绝的模式</li>\n<li>如果同类操作被连续拒绝 3 次以上，回退到用户提示模式</li>\n<li>这避免了分类器过于保守导致 Claude 无法完成任务</li>\n</ul>\n\n<h3>沙箱适配器（sandbox-adapter.ts）</h3>\n<p>对于需要在隔离环境中执行的操作，<code>sandbox-adapter.ts</code> 提供了统一的沙箱接口：</p>\n<ul>\n<li>抽象不同平台的沙箱实现（macOS sandbox-exec、Linux namespaces 等）</li>\n<li>限制文件系统访问范围（只能访问工作目录和显式挂载的路径）</li>\n<li>限制网络访问</li>\n<li>限制进程能力（不允许提权操作）</li>\n</ul>\n\n<h3>useCanUseTool React Hook</h3>\n<p><code>useCanUseTool</code> 是前端侧的权限集成点，连接后端权限系统和 UI 交互：</p>\n<ul>\n<li>当工具调用需要用户确认时，该 hook 触发一个交互式对话框</li>\n<li>对话框显示：工具名称、命令内容、预期行为、风险提示</li>\n<li>用户可以选择"允许"、"拒绝"、"始终允许此类操作"</li>\n<li>支持 coordinator/swarm 模式下的多 Agent 权限管理</li>\n</ul>\n\n<h3>Shell 规则匹配</h3>\n<p>Bash 命令的权限规则支持模式匹配（Pattern Matching）：</p>\n<ul>\n<li><code>Bash(git *)</code> — 匹配所有以 <code>git</code> 开头的命令</li>\n<li><code>Bash(npm run *)</code> — 匹配所有 <code>npm run</code> 命令</li>\n<li><code>Bash(cd *)</code> — 匹配所有目录切换命令</li>\n<li>模式使用 glob 语法，<code>*</code> 匹配任意字符序列</li>\n<li>匹配在 AST 解析后进行，确保不会被引号嵌套等语法技巧绕过</li>\n</ul>\n\n<h3>PermissionDecisionReason</h3>\n<p>每个权限决策都附带原因说明，用于审计和调试：</p>\n<ul>\n<li><code>classifier</code> — 由分类器自动判断</li>\n<li><code>hook</code> — 由钩子拦截</li>\n<li><code>rule</code> — 由配置规则匹配</li>\n<li><code>subcommandResults</code> — 基于子命令分析结果</li>\n<li><code>mode</code> — 由当前权限模式决定</li>\n<li><code>sandboxOverride</code> — 沙箱覆盖</li>\n<li><code>workingDir</code> — 工作目录检查</li>\n<li><code>safetyCheck</code> — 安全检查</li>\n</ul>\n',architecture:'\n<h2>权限系统架构</h2>\n\n<h3>核心模块</h3>\n<div style="overflow-x:auto">\n<table>\n<thead><tr><th>模块</th><th>职责</th><th>关键函数/类型</th></tr></thead>\n<tbody>\n<tr><td>utils/permissions/</td><td>权限决策引擎</td><td>hasPermissionsToUseTool()</td></tr>\n<tr><td>bashClassifier.ts</td><td>Bash 命令安全分类</td><td>classifyBashCommand()</td></tr>\n<tr><td>yoloClassifier.ts</td><td>自动模式安全分类</td><td>classifyTranscript()</td></tr>\n<tr><td>sandbox-adapter.ts</td><td>沙箱接口适配</td><td>createSandbox(), runInSandbox()</td></tr>\n<tr><td>useCanUseTool</td><td>UI 权限对话框</td><td>React Hook + Dialog</td></tr>\n</tbody>\n</table>\n</div>\n\n<h3>5 层权限决策流程</h3>\n<ol>\n<li><strong>Layer 1: Blanket Deny</strong> → 系统硬编码的绝对禁止规则\n  <ul><li>匹配 → <span style="color:#e74c3c">DENY</span>（不可覆盖）</li></ul>\n</li>\n<li><strong>Layer 2: Always-Allow</strong> → 用户配置的自动允许\n  <ul><li>匹配 → <span style="color:#27ae60">ALLOW</span></li></ul>\n</li>\n<li><strong>Layer 3: Always-Deny</strong> → 用户配置的自动拒绝\n  <ul><li>匹配 → <span style="color:#e74c3c">DENY</span></li></ul>\n</li>\n<li><strong>Layer 4: Tool checkPermissions()</strong> → 工具自身的检查逻辑\n  <ul><li>allow/deny/ask → 传递对应决策</li></ul>\n</li>\n<li><strong>Layer 5: Classifier / User Prompt</strong> → 最终裁定\n  <ul>\n  <li>default 模式 → 弹窗让用户确认</li>\n  <li>auto 模式 → yoloClassifier 自动判断</li>\n  <li>plan 模式 → 写操作直接拒绝</li>\n  </ul>\n</li>\n</ol>\n\n<h3>PermissionMode 行为矩阵</h3>\n<div style="overflow-x:auto">\n<table>\n<thead><tr><th>模式</th><th>只读工具</th><th>写操作工具</th><th>未知风险工具</th></tr></thead>\n<tbody>\n<tr><td>default</td><td>自动允许</td><td>用户确认</td><td>用户确认</td></tr>\n<tr><td>plan</td><td>自动允许</td><td>直接拒绝</td><td>直接拒绝</td></tr>\n<tr><td>auto</td><td>自动允许</td><td>分类器判断</td><td>分类器判断</td></tr>\n<tr><td>bypassPermissions</td><td>自动允许</td><td>自动允许</td><td>自动允许</td></tr>\n</tbody>\n</table>\n</div>\n\n<h3>拒绝追踪与回退</h3>\n<ul>\n<li><strong>追踪</strong>：记录每次分类器拒绝的工具名和原因</li>\n<li><strong>检测</strong>：shouldFallbackToPrompting() 检查连续拒绝次数</li>\n<li><strong>回退</strong>：超过阈值（3次），切换到用户确认模式</li>\n<li><strong>重置</strong>：用户做出决策后，重置计数器</li>\n</ul>\n\n<h3>设计决策</h3>\n<blockquote>\n<p><strong>为什么权限系统是 5 层而不是更简单的 allow/deny 二元决策？</strong></p>\n<p>单一层级无法满足不同场景的需求：系统级安全需要硬编码拒绝（第 1 层），用户偏好需要可配置规则（第 2/3 层），工具特有逻辑需要自定义检查（第 4 层），兜底策略需要分类器或人工确认（第 5 层）。每一层解决一个独立的关注点，组合在一起形成了既安全又灵活的权限体系。</p>\n</blockquote>\n',codeExamples:[{title:"5 层权限决策引擎",language:"typescript",code:"// utils/permissions/hasPermissionsToUseTool.ts — 简化版\n\nexport type PermissionDecision = {\n  allowed: boolean\n  reason: PermissionDecisionReason\n  message?: string\n}\n\nexport type PermissionDecisionReason =\n  | 'classifier'\n  | 'hook'\n  | 'rule'\n  | 'subcommandResults'\n  | 'mode'\n  | 'sandboxOverride'\n  | 'workingDir'\n  | 'safetyCheck'\n\nexport async function hasPermissionsToUseTool(\n  tool: Tool,\n  input: unknown,\n  context: ToolPermissionContext\n): Promise<PermissionDecision> {\n  // ===== 第 1 层：全局拒绝规则 =====\n  const blanketDeny = checkBlanketDenyRules(tool, input)\n  if (blanketDeny) {\n    return {\n      allowed: false,\n      reason: 'safetyCheck',\n      message: blanketDeny.message,\n    }\n  }\n\n  // ===== 第 2 层：始终允许规则 =====\n  if (context.alwaysAllowRules.length > 0) {\n    const allowMatch = matchPermissionRule(\n      tool, input, context.alwaysAllowRules\n    )\n    if (allowMatch) {\n      return { allowed: true, reason: 'rule' }\n    }\n  }\n\n  // ===== 第 3 层：始终拒绝规则 =====\n  if (context.alwaysDenyRules.length > 0) {\n    const denyMatch = matchPermissionRule(\n      tool, input, context.alwaysDenyRules\n    )\n    if (denyMatch) {\n      return {\n        allowed: false,\n        reason: 'rule',\n        message: '被拒绝规则阻止: ' + denyMatch.pattern,\n      }\n    }\n  }\n\n  // ===== 第 4 层：工具自身权限检查 =====\n  const toolPermResult = await tool.checkPermissions(input, context)\n  if (toolPermResult.decided) {\n    return {\n      allowed: toolPermResult.allowed,\n      reason: toolPermResult.reason,\n      message: toolPermResult.message,\n    }\n  }\n\n  // ===== 第 5 层：根据模式决定 =====\n  switch (context.mode) {\n    case 'plan':\n      // 计划模式：只读工具允许，其余拒绝\n      return tool.isReadOnly()\n        ? { allowed: true, reason: 'mode' }\n        : { allowed: false, reason: 'mode', message: '计划模式下不允许写操作' }\n\n    case 'bypassPermissions':\n      return { allowed: true, reason: 'mode' }\n\n    case 'auto':\n      // 自动模式：使用分类器\n      return classifyForAutoMode(tool, input, context)\n\n    case 'default':\n    default:\n      // 默认模式：需要用户确认\n      return {\n        allowed: false,\n        reason: 'classifier',\n        message: 'needsUserConfirmation',\n      }\n  }\n}\n\nfunction checkBlanketDenyRules(\n  tool: Tool,\n  input: unknown\n): { message: string } | null {\n  // 硬编码的绝对禁止规则\n  if (tool.name === 'Bash') {\n    const cmd = (input as { command: string }).command\n    if (cmd.includes('rm -rf /') && !cmd.includes('rm -rf /tmp')) {\n      return { message: '禁止执行可能删除根目录的命令' }\n    }\n  }\n  return null\n}"},{title:"Bash 分类器与 YOLO 分类器",language:"typescript",code:"// bashClassifier.ts — Bash 命令安全分类\nexport interface BashClassification {\n  riskLevel: 'safe' | 'moderate' | 'dangerous'\n  category: string\n  requiresConfirmation: boolean\n  explanation: string\n}\n\nexport function classifyBashCommand(\n  command: string,\n  astAnalysis: CommandAnalysis\n): BashClassification {\n  // 已知安全命令\n  if (astAnalysis.isReadOnly && !astAnalysis.hasRedirection) {\n    return {\n      riskLevel: 'safe',\n      category: 'read-only',\n      requiresConfirmation: false,\n      explanation: '只读命令，无副作用',\n    }\n  }\n\n  // 已知危险命令模式\n  const dangerousPatterns = [\n    { pattern: /rms+(-rf?|--recursive)/, category: 'destructive-delete' },\n    { pattern: /chmods+777/, category: 'insecure-permissions' },\n    { pattern: />s*/etc//, category: 'system-file-write' },\n    { pattern: /curl.*|s*(bash|sh)/, category: 'remote-code-execution' },\n    { pattern: /gits+pushs+.*--force/, category: 'force-push' },\n  ]\n\n  for (const { pattern, category } of dangerousPatterns) {\n    if (pattern.test(command)) {\n      return {\n        riskLevel: 'dangerous',\n        category,\n        requiresConfirmation: true,\n        explanation: '检测到高风险操作: ' + category,\n      }\n    }\n  }\n\n  // 有写操作但不是已知危险的\n  if (!astAnalysis.isReadOnly || astAnalysis.hasRedirection) {\n    return {\n      riskLevel: 'moderate',\n      category: 'write-operation',\n      requiresConfirmation: true,\n      explanation: '命令可能修改文件系统',\n    }\n  }\n\n  return {\n    riskLevel: 'safe',\n    category: 'unknown-safe',\n    requiresConfirmation: false,\n    explanation: '未检测到风险',\n  }\n}\n\n// yoloClassifier.ts — 自动模式分类\nexport async function classifyForAutoMode(\n  tool: Tool,\n  input: unknown,\n  context: ToolPermissionContext\n): Promise<PermissionDecision> {\n  // 只读工具：始终允许\n  if (tool.isReadOnly()) {\n    return { allowed: true, reason: 'classifier' }\n  }\n\n  // Bash 命令：使用专用分类器\n  if (tool.name === 'Bash') {\n    const cmd = (input as { command: string }).command\n    const analysis = analyzeCommand(cmd)\n    const classification = classifyBashCommand(cmd, analysis)\n\n    if (classification.riskLevel === 'dangerous') {\n      return {\n        allowed: false,\n        reason: 'classifier',\n        message: classification.explanation,\n      }\n    }\n\n    // 中等风险：检查拒绝追踪\n    if (classification.riskLevel === 'moderate') {\n      if (shouldFallbackToPrompting(tool.name, context)) {\n        return {\n          allowed: false,\n          reason: 'classifier',\n          message: 'needsUserConfirmation',\n        }\n      }\n      return { allowed: true, reason: 'classifier' }\n    }\n  }\n\n  // 文件写操作：检查是否在工作目录内\n  if (tool.name === 'Edit' || tool.name === 'Write') {\n    const filePath = (input as { file_path: string }).file_path\n    if (isWithinWorkingDir(filePath, context)) {\n      return { allowed: true, reason: 'workingDir' }\n    }\n    return {\n      allowed: false,\n      reason: 'workingDir',\n      message: '文件不在工作目录内',\n    }\n  }\n\n  // 默认：允许\n  return { allowed: true, reason: 'classifier' }\n}"},{title:"Shell 规则匹配与用户确认 Hook",language:"typescript",code:"// 权限规则匹配系统\n\ninterface PermissionRule {\n  pattern: string // \"Bash(git *)\", \"Read\", \"Edit\"\n}\n\nexport function matchPermissionRule(\n  tool: Tool,\n  input: unknown,\n  rules: PermissionRule[]\n): PermissionRule | null {\n  for (const rule of rules) {\n    if (matchSingleRule(tool, input, rule)) {\n      return rule\n    }\n  }\n  return null\n}\n\nfunction matchSingleRule(\n  tool: Tool,\n  input: unknown,\n  rule: PermissionRule\n): boolean {\n  // 简单工具名匹配: \"Read\", \"Glob\"\n  if (!rule.pattern.includes('(')) {\n    return tool.name === rule.pattern\n  }\n\n  // 带参数的匹配: \"Bash(npm test*)\"\n  const match = rule.pattern.match(/^(\\w+)\\((.+)\\)$/)\n  if (!match) return false\n\n  const [, toolName, commandPattern] = match\n  if (tool.name !== toolName) return false\n\n  // 对 Bash 工具，匹配命令内容\n  if (toolName === 'Bash') {\n    const command = (input as { command: string }).command\n    return globMatch(command.trim(), commandPattern)\n  }\n\n  // 对文件工具，匹配文件路径\n  if (toolName === 'Read' || toolName === 'Edit' || toolName === 'Write') {\n    const filePath = (input as { file_path: string }).file_path\n    return globMatch(filePath, commandPattern)\n  }\n\n  return false\n}\n\n// glob 匹配：* 匹配任意字符序列\nfunction globMatch(text: string, pattern: string): boolean {\n  // 转义正则特殊字符，然后将 glob 通配符转换为正则\n  const escaped = escapeRegExp(pattern)\n  const regexStr = '^' + escaped\n    .replace(/\\\\\\*/g, '.*')  // glob * → regex .*\n    .replace(/\\\\\\?/g, '.')   // glob ? → regex .\n    + '$'\n  return new RegExp(regexStr).test(text)\n}\n\nfunction escapeRegExp(str: string): string {\n  return str.replace(/[.*+?^$\\\\|()\\[\\]]/g, '\\\\$&')\n}\n\n// 拒绝追踪与回退\nconst denialCounts = new Map<string, number>()\n\nexport function shouldFallbackToPrompting(\n  toolName: string,\n  context: ToolPermissionContext\n): boolean {\n  const key = toolName\n  const count = denialCounts.get(key) ?? 0\n  return count >= 3 // 连续拒绝 3 次后回退到用户确认\n}\n\nexport function trackDenial(toolName: string): void {\n  const count = denialCounts.get(toolName) ?? 0\n  denialCounts.set(toolName, count + 1)\n}\n\nexport function resetDenials(toolName: string): void {\n  denialCounts.delete(toolName)\n}\n\n// useCanUseTool — React Hook (简化版)\nexport function useCanUseTool(toolCall: ToolUseBlock) {\n  const [decision, setDecision] = useState<PermissionDecision | null>(null)\n  const [showDialog, setShowDialog] = useState(false)\n\n  useEffect(() => {\n    async function check() {\n      const result = await hasPermissionsToUseTool(\n        toolCall.tool, toolCall.input, getPermissionContext()\n      )\n\n      if (result.message === 'needsUserConfirmation') {\n        setShowDialog(true) // 弹出确认对话框\n      } else {\n        setDecision(result)\n      }\n    }\n    check()\n  }, [toolCall])\n\n  const handleUserDecision = (\n    allowed: boolean,\n    alwaysAllow: boolean\n  ) => {\n    setDecision({ allowed, reason: 'classifier' })\n    if (alwaysAllow) {\n      // 将规则添加到 always-allow 列表\n      addAlwaysAllowRule(toolCall.tool.name, toolCall.input)\n    }\n    setShowDialog(false)\n  }\n\n  return { decision, showDialog, handleUserDecision }\n}"}],interactive:'\n<h2>权限系统互动解析</h2>\n\n<h3>第 1 步：理解 5 层决策的必要性</h3>\n<p>让我们用一个具体例子走过 5 层决策：Claude 调用 <code>Bash("git push --force origin main")</code></p>\n<ol>\n<li><strong>第 1 层（全局拒绝）</strong>：虽然危险，但不在硬编码禁止列表中 → 通过</li>\n<li><strong>第 2 层（始终允许）</strong>：用户配置了 <code>Bash(git *)</code> → 匹配！但等等...这真的安全吗？</li>\n</ol>\n<p>这就是 5 层设计的问题：如果用户配置了过于宽泛的 always-allow 规则，<code>git push --force</code> 会被自动允许。Claude Code 的文档强调：<strong>不要使用过于宽泛的规则</strong>。正确的做法是 <code>Bash(git status*)</code>、<code>Bash(git log*)</code> 这样的精确规则。</p>\n\n<h3>第 2 步：Bash 分类器的深度分析</h3>\n<p>Bash 分类器不是简单的黑名单匹配，而是理解命令语义：</p>\n<ul>\n<li><code>cat file.txt</code> → safe（只读）</li>\n<li><code>cat file.txt > output.txt</code> → moderate（有重定向，可能覆盖文件）</li>\n<li><code>cat file.txt | grep pattern</code> → safe（管道不产生副作用）</li>\n<li><code>$(cat file.txt)</code> → moderate（命令替换，结果可能被执行）</li>\n</ul>\n<p>关键洞察：同一个 <code>cat</code> 命令在不同上下文中有不同的风险等级。这就是为什么需要 AST 分析而不是简单的命令名匹配。</p>\n\n<h3>第 3 步：YOLO 模式的安全边界</h3>\n<p>Auto/YOLO 模式并不是"允许一切"。它的核心原则是：</p>\n<ul>\n<li><strong>自动允许</strong>：与用户明确要求一致的操作（"帮我编辑 config.ts" → 允许编辑 config.ts）</li>\n<li><strong>自动拒绝</strong>：明显危险的操作（rm -rf、curl | bash）</li>\n<li><strong>回退确认</strong>：不确定的操作连续被拒绝 3 次后，切换到人工确认</li>\n</ul>\n<p>这意味着即使在 YOLO 模式下，用户仍然有安全保护。分类器的目标是"像一个有经验的开发者那样判断"。</p>\n\n<h3>第 4 步：沙箱的隔离机制</h3>\n<p>沙箱为不信任的命令提供了一个隔离的执行环境：</p>\n<ul>\n<li><strong>文件系统隔离</strong>：命令只能看到工作目录及其子目录，无法访问 ~/.ssh、/etc 等</li>\n<li><strong>网络隔离</strong>：可选地禁止网络访问</li>\n<li><strong>进程隔离</strong>：命令无法提升权限或影响其他进程</li>\n<li><strong>使用场景</strong>：执行不受信任的构建脚本、运行测试等</li>\n</ul>\n\n<h3>第 5 步：用户确认对话框的设计</h3>\n<p>权限确认对话框提供了三个选项：</p>\n<ul>\n<li><strong>"允许"</strong> — 本次允许，下次同类操作仍需确认</li>\n<li><strong>"拒绝"</strong> — 本次拒绝，Claude 收到错误信息并调整策略</li>\n<li><strong>"始终允许"</strong> — 将此操作模式添加到 always-allow 规则，后续自动放行</li>\n</ul>\n<p>这种渐进式信任模型让用户可以：</p>\n<ol>\n<li>初始保持谨慎（每次都确认）</li>\n<li>逐渐建立信任（对常用操作设置"始终允许"）</li>\n<li>最终达到高效状态（大多数操作自动放行，只有异常操作需要确认）</li>\n</ol>\n\n<h3>关键设计洞察</h3>\n<ul>\n<li><strong>纵深防御</strong>：5 层决策确保即使某一层被绕过，后续层仍能拦截</li>\n<li><strong>可配置性</strong>：用户可以通过规则精确控制权限边界</li>\n<li><strong>渐进信任</strong>：从"全部确认"到"大部分自动"的平滑过渡</li>\n<li><strong>安全分类</strong>：Bash 分类器理解命令语义，而非简单的模式匹配</li>\n<li><strong>回退机制</strong>：分类器不确定时回退到人工确认，避免过度阻塞</li>\n</ul>\n'},c11:{content:"\n    <h2>11.1 Hook 类型与触发时机</h2>\n    <p>Claude Code 的 Hook 系统提供了 <strong>6 种生命周期钩子</strong>，允许开发者在关键节点注入自定义逻辑。这些钩子覆盖了从会话启动到工具调用、停止、上下文压缩的完整生命周期：</p>\n    <ul>\n      <li><strong>SessionStart</strong> — 会话初始化时触发，用于环境准备、配置加载</li>\n      <li><strong>PreToolUse</strong> — 工具调用前触发，可拦截、修改或拒绝工具调用</li>\n      <li><strong>PostToolUse</strong> — 工具调用后触发，可处理结果、记录日志</li>\n      <li><strong>Stop</strong> — 代理停止时触发，用于清理资源、保存状态</li>\n      <li><strong>PreCompact</strong> — 上下文压缩前触发，可自定义压缩策略</li>\n      <li><strong>PostCompact</strong> — 上下文压缩后触发，可恢复关键信息</li>\n    </ul>\n    <p>每个 Hook 返回的结果遵循 <code>hookResponseSchema</code>，包含 <code>ok</code>（是否成功）和 <code>reason</code>（失败原因）两个字段，提供统一的错误处理接口。</p>\n\n    <h2>11.2 三种执行方式</h2>\n    <p>Hook 系统支持三种不同的执行方式，适用于不同的使用场景：</p>\n    <ul>\n      <li><strong>execAgentHook</strong> — 使用 Claude 作为子代理执行 Hook，适合需要 AI 推理的复杂逻辑。会启动一个独立的代理实例来处理 Hook 逻辑</li>\n      <li><strong>execHttpHook</strong> — 通过 HTTP Webhook 调用外部服务，适合与现有基础设施集成。支持超时控制和错误重试</li>\n      <li><strong>execPromptHook</strong> — 基于 Prompt 的执行方式，将 Hook 逻辑嵌入到当前对话上下文中执行</li>\n    </ul>\n\n    <h2>11.3 核心辅助模块</h2>\n    <p><strong>hookHelpers.ts</strong> 提供了关键的辅助功能：</p>\n    <ul>\n      <li><code>addArgumentsToPrompt</code> — 支持 <code>$ARGUMENTS</code>、<code>$0</code>、<code>$1</code> 等变量替换，将运行时参数注入到 Hook Prompt 中</li>\n      <li><code>registerStructuredOutputEnforcement</code> — 注册结构化输出强制机制，确保 Hook 返回符合预期的 JSON Schema</li>\n    </ul>\n    <p><strong>hookEvents.ts</strong> 负责 Hook 事件分发，将系统事件路由到对应的 Hook 处理器。<strong>hooksConfigManager.ts</strong> 管理 Hook 配置，支持从多个来源（项目配置、用户配置、企业配置）聚合 Hook 定义。</p>\n\n    <h2>11.4 Skill 与异步注册</h2>\n    <p><strong>registerFrontmatterHooks.ts</strong> 从 Skill 的 frontmatter 中解析并注册 Hook 定义，使得 Skill 可以声明式地定义自己的生命周期行为。<strong>registerSkillHooks.ts</strong> 处理 Skill 级别的 Hook 注册逻辑。</p>\n    <p><strong>AsyncHookRegistry</strong> 提供异步 Hook 注册和执行能力，支持动态添加和移除 Hook 处理器，适合运行时需要动态调整 Hook 行为的场景。</p>\n    <p><strong>sessionHooks.ts</strong> 维护会话范围的 Hook 状态，确保每个会话拥有独立的 Hook 上下文，避免跨会话干扰。</p>\n  ",architecture:'\n    <div class="arch-diagram">\n      <h3>Hook 生命周期架构图</h3>\n      <div class="arch-flow">\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px; margin-bottom: 16px;">\n          <h4 style="color: #e94560;">Hook 事件触发层</h4>\n          <div style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 8px;">\n            <div class="arch-box" style="background: #16213e; padding: 10px; border-radius: 8px; text-align: center; border: 1px solid #e94560;">SessionStart</div>\n            <div class="arch-box" style="background: #16213e; padding: 10px; border-radius: 8px; text-align: center; border: 1px solid #e94560;">PreToolUse</div>\n            <div class="arch-box" style="background: #16213e; padding: 10px; border-radius: 8px; text-align: center; border: 1px solid #e94560;">PostToolUse</div>\n            <div class="arch-box" style="background: #16213e; padding: 10px; border-radius: 8px; text-align: center; border: 1px solid #e94560;">Stop</div>\n            <div class="arch-box" style="background: #16213e; padding: 10px; border-radius: 8px; text-align: center; border: 1px solid #e94560;">PreCompact</div>\n            <div class="arch-box" style="background: #16213e; padding: 10px; border-radius: 8px; text-align: center; border: 1px solid #e94560;">PostCompact</div>\n          </div>\n        </div>\n        <div style="text-align: center; font-size: 24px; color: #e94560;">&#x2193;</div>\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px; margin-bottom: 16px;">\n          <h4 style="color: #0f3460;">hookEvents.ts 事件分发</h4>\n          <p style="color: #aaa; font-size: 14px;">解析配置 &#x2192; 匹配事件类型 &#x2192; 路由到执行器</p>\n        </div>\n        <div style="text-align: center; font-size: 24px; color: #0f3460;">&#x2193;</div>\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px; margin-bottom: 16px;">\n          <h4 style="color: #533483;">三种执行引擎</h4>\n          <div style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 8px;">\n            <div class="arch-box" style="background: #16213e; padding: 12px; border-radius: 8px; text-align: center; border: 1px solid #533483;">\n              <div style="font-weight: bold;">execAgentHook</div>\n              <div style="font-size: 12px; color: #aaa;">Claude 子代理</div>\n            </div>\n            <div class="arch-box" style="background: #16213e; padding: 12px; border-radius: 8px; text-align: center; border: 1px solid #533483;">\n              <div style="font-weight: bold;">execHttpHook</div>\n              <div style="font-size: 12px; color: #aaa;">HTTP Webhook</div>\n            </div>\n            <div class="arch-box" style="background: #16213e; padding: 12px; border-radius: 8px; text-align: center; border: 1px solid #533483;">\n              <div style="font-weight: bold;">execPromptHook</div>\n              <div style="font-size: 12px; color: #aaa;">Prompt 注入</div>\n            </div>\n          </div>\n        </div>\n        <div style="text-align: center; font-size: 24px; color: #533483;">&#x2193;</div>\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px;">\n          <h4 style="color: #48c9b0;">hookHelpers.ts 结果处理</h4>\n          <p style="color: #aaa; font-size: 14px;">hookResponseSchema { ok, reason } &#x2192; 统一返回格式</p>\n        </div>\n      </div>\n    </div>\n  ',codeExamples:[{title:"Hook 定义与配置结构",language:"typescript",code:"// hookResponseSchema: Hook 统一返回结构\nconst hookResponseSchema = {\n  type: 'object',\n  properties: {\n    ok: { type: 'boolean', description: 'Hook 是否执行成功' },\n    reason: { type: 'string', description: '失败原因描述' }\n  },\n  required: ['ok']\n};\n\n// Hook 配置定义示例 (settings.json)\nconst hookConfig = {\n  hooks: {\n    PreToolUse: [\n      {\n        // 匹配特定工具\n        matcher: 'Bash',\n        // 使用 prompt 方式执行\n        type: 'prompt',\n        prompt: '检查即将执行的命令是否安全: $ARGUMENTS'\n      },\n      {\n        matcher: '*',\n        type: 'http',\n        url: 'https://audit.example.com/hook',\n        timeout: 5000\n      }\n    ],\n    PostToolUse: [\n      {\n        matcher: 'Write',\n        type: 'prompt',\n        prompt: '验证写入的文件内容是否符合规范'\n      }\n    ],\n    SessionStart: [\n      {\n        type: 'command',\n        command: 'echo \"Session initialized at $(date)\"'\n      }\n    ]\n  }\n};"},{title:"Hook 执行流程与参数替换",language:"typescript",code:"// addArgumentsToPrompt: 参数替换引擎\nfunction addArgumentsToPrompt(\n  prompt: string,\n  args: string[]\n): string {\n  let result = prompt;\n  // 替换 $ARGUMENTS 为完整参数字符串\n  result = result.replace(\n    /\\$ARGUMENTS/g,\n    args.join(' ')\n  );\n  // 替换 $0, $1, $2... 为对应位置参数\n  for (let i = 0; i < args.length; i++) {\n    result = result.replace(\n      new RegExp('\\\\$' + i, 'g'),\n      args[i]\n    );\n  }\n  return result;\n}\n\n// Hook 事件分发核心逻辑\nasync function dispatchHookEvent(\n  event: HookEvent,\n  context: HookContext\n): Promise<HookResponse[]> {\n  const configs = hooksConfigManager\n    .getHooksForEvent(event.type);\n  const results: HookResponse[] = [];\n\n  for (const config of configs) {\n    // 检查 matcher 是否匹配当前工具\n    if (config.matcher && !matchTool(\n      config.matcher, event.toolName\n    )) continue;\n\n    let result: HookResponse;\n    switch (config.type) {\n      case 'agent':\n        result = await execAgentHook(config, context);\n        break;\n      case 'http':\n        result = await execHttpHook(config, context);\n        break;\n      case 'prompt':\n        result = await execPromptHook(config, context);\n        break;\n    }\n    results.push(result);\n    // 如果任何 Hook 返回不通过则中断\n    if (!result.ok) break;\n  }\n  return results;\n}"},{title:"AsyncHookRegistry 与 Skill 注册",language:"typescript",code:"// AsyncHookRegistry: 异步 Hook 注册表\nclass AsyncHookRegistry {\n  private hooks: Map<string, HookHandler[]> = new Map();\n\n  register(event: string, handler: HookHandler): void {\n    const handlers = this.hooks.get(event) || [];\n    handlers.push(handler);\n    this.hooks.set(event, handlers);\n  }\n\n  unregister(event: string, handler: HookHandler): void {\n    const handlers = this.hooks.get(event) || [];\n    this.hooks.set(\n      event,\n      handlers.filter(h => h !== handler)\n    );\n  }\n\n  async execute(\n    event: string,\n    context: HookContext\n  ): Promise<HookResponse[]> {\n    const handlers = this.hooks.get(event) || [];\n    return Promise.all(\n      handlers.map(h => h(context))\n    );\n  }\n}\n\n// registerFrontmatterHooks: 从 Skill frontmatter 注册\nfunction registerFrontmatterHooks(\n  skill: SkillDefinition,\n  registry: AsyncHookRegistry\n): void {\n  const frontmatter = skill.frontmatter;\n  if (!frontmatter?.hooks) return;\n\n  for (const [event, hookDefs] of\n    Object.entries(frontmatter.hooks)\n  ) {\n    for (const def of hookDefs) {\n      registry.register(event, async (ctx) => {\n        // 使用 skill 上下文执行 hook\n        return execPromptHook({\n          prompt: addArgumentsToPrompt(\n            def.prompt, ctx.args || []\n          ),\n          skillContext: skill.name\n        }, ctx);\n      });\n    }\n  }\n}\n\n// registerStructuredOutputEnforcement\nfunction registerStructuredOutputEnforcement(\n  registry: AsyncHookRegistry\n): void {\n  registry.register('PostToolUse', async (ctx) => {\n    if (!ctx.expectedSchema) return { ok: true };\n    const valid = validateSchema(\n      ctx.result, ctx.expectedSchema\n    );\n    return {\n      ok: valid,\n      reason: valid ? undefined\n        : 'Output does not match expected schema'\n    };\n  });\n}"}],interactive:'\n    <h3>Hook 生命周期交互式演练</h3>\n    <div class="interactive-demo">\n      <div class="step" data-step="1">\n        <h4>Step 1: Hook 事件触发</h4>\n        <p>当用户请求执行 Bash 命令时，系统首先触发 <code>PreToolUse</code> 事件。事件包含工具名称（Bash）、参数（命令内容）和当前上下文。</p>\n        <div class="code-highlight">\n          <pre>// 系统内部触发流程\nhookEvents.dispatch({\n  type: \'PreToolUse\',\n  toolName: \'Bash\',\n  args: [\'rm -rf /tmp/cache\'],\n  context: currentSession\n});</pre>\n        </div>\n      </div>\n      <div class="step" data-step="2">\n        <h4>Step 2: 配置匹配与路由</h4>\n        <p>hooksConfigManager 查找所有匹配 PreToolUse + Bash 的 Hook 配置。支持通配符 <code>*</code> 匹配所有工具，也支持精确匹配特定工具名。</p>\n        <div class="code-highlight">\n          <pre>// 匹配逻辑\nmatcher: \'Bash\'  // 精确匹配 Bash 工具\nmatcher: \'*\'     // 匹配所有工具\nmatcher: \'Write|Edit\'  // 匹配多个工具</pre>\n        </div>\n      </div>\n      <div class="step" data-step="3">\n        <h4>Step 3: 执行引擎选择</h4>\n        <p>根据配置的 type 字段选择执行引擎。execPromptHook 适合简单检查，execAgentHook 适合复杂推理，execHttpHook 适合外部集成。</p>\n      </div>\n      <div class="step" data-step="4">\n        <h4>Step 4: 结果处理</h4>\n        <p>所有 Hook 返回统一的 <code>{ ok, reason }</code> 结构。如果任一 Hook 返回 <code>ok: false</code>，则工具调用被拦截，reason 会展示给用户解释拦截原因。</p>\n        <div class="code-highlight">\n          <pre>// 拦截示例\n{ ok: false, reason: \'检测到危险命令: rm -rf，已拦截执行\' }\n// 通过示例\n{ ok: true }</pre>\n        </div>\n      </div>\n    </div>\n  '},c12:{content:"\n    <h2>12.1 五种传输协议</h2>\n    <p>Claude Code 的 MCP（Model Context Protocol）集成支持 <strong>5 种传输协议</strong>，覆盖从本地进程到远程服务的全场景通信需求：</p>\n    <ul>\n      <li><strong>StdioClientTransport</strong> — 通过子进程的 stdin/stdout 通信，最常用的本地 MCP 服务器连接方式。启动一个子进程并通过标准输入输出交换 JSON-RPC 消息</li>\n      <li><strong>SSEClientTransport</strong> — 基于 Server-Sent Events 的 HTTP 长连接，适合远程 MCP 服务器。客户端通过 GET 请求建立 SSE 连接接收消息，通过 POST 发送请求</li>\n      <li><strong>StreamableHTTPClientTransport</strong> — 基于可流式 HTTP 的传输，支持双向流通信，是 SSE 的升级版本</li>\n      <li><strong>WebSocketTransport</strong> — 基于 WebSocket 的全双工通信，适合需要低延迟双向交互的场景</li>\n      <li><strong>SdkControlClientTransport</strong> — SDK 控制传输，用于 Claude Code SDK 内部进程间通信</li>\n    </ul>\n\n    <h2>12.2 客户端核心逻辑</h2>\n    <p><strong>client.ts</strong> 是 MCP 集成的核心模块，提供三个关键函数：</p>\n    <ul>\n      <li><code>connectToServer()</code> — 根据配置选择传输协议，建立与 MCP 服务器的连接。处理连接超时、重连和错误恢复</li>\n      <li><code>fetchToolsForClient()</code> — 从已连接的 MCP 服务器获取工具列表，并将 MCP 工具定义转换为 Claude Code 内部的 Tool 对象格式。这个转换过程包括参数映射、权限标记和描述本地化</li>\n      <li><code>prefetchAllMcpResources()</code> — 预加载所有 MCP 服务器的资源（工具列表、资源模板等），减少运行时的延迟</li>\n    </ul>\n\n    <h2>12.3 配置管理与聚合</h2>\n    <p><strong>config.ts</strong> 负责 MCP 配置的解析和聚合：</p>\n    <ul>\n      <li><code>parseMcpConfig()</code> — 解析单个 MCP 服务器配置，验证必要字段</li>\n      <li><code>getClaudeCodeMcpConfigs()</code> — 从多个来源聚合 MCP 配置：项目 <code>.mcp.json</code>、用户全局配置、企业管理员配置、Skill 定义的 MCP 服务器</li>\n      <li><code>filterMcpServersByPolicy()</code> — 根据组织策略过滤 MCP 服务器，企业管理员可以设置允许列表和黑名单</li>\n    </ul>\n\n    <h2>12.4 OAuth 认证与错误处理</h2>\n    <p><strong>auth.ts</strong> 实现了完整的 OAuth 认证流程，支持 XAA IDP 登录。当 MCP 服务器需要认证时，系统会自动引导用户完成 OAuth 授权流程，获取并缓存访问令牌。</p>\n    <p>MCP 集成定义了自定义错误层次结构：</p>\n    <ul>\n      <li><strong>McpAuthError</strong> — 认证失败，需要用户重新授权</li>\n      <li><strong>McpSessionExpiredError</strong> — 会话过期，需要重新建立连接</li>\n      <li><strong>McpToolCallError</strong> — 工具调用失败，包含错误详情和重试建议</li>\n    </ul>\n\n    <h2>12.5 MCPTool 包装器与权限控制</h2>\n    <p><strong>MCPTool</strong> 包装器将 MCP 服务器暴露的工具封装为 Claude Code 标准工具接口。<strong>MCPConnectionManager.tsx</strong> 是 React 组件，提供连接状态可视化和管理界面。通道权限系统通过 allowlists 控制哪些 MCP 工具可以被使用。</p>\n  ",architecture:'\n    <div class="arch-diagram">\n      <h3>MCP 集成架构图</h3>\n      <div class="arch-flow">\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px; margin-bottom: 16px;">\n          <h4 style="color: #e94560;">配置聚合层</h4>\n          <div style="display: grid; grid-template-columns: repeat(4, 1fr); gap: 8px;">\n            <div class="arch-box" style="background: #16213e; padding: 10px; border-radius: 8px; text-align: center; border: 1px solid #e94560; font-size: 13px;">.mcp.json</div>\n            <div class="arch-box" style="background: #16213e; padding: 10px; border-radius: 8px; text-align: center; border: 1px solid #e94560; font-size: 13px;">用户全局配置</div>\n            <div class="arch-box" style="background: #16213e; padding: 10px; border-radius: 8px; text-align: center; border: 1px solid #e94560; font-size: 13px;">企业配置</div>\n            <div class="arch-box" style="background: #16213e; padding: 10px; border-radius: 8px; text-align: center; border: 1px solid #e94560; font-size: 13px;">Skill MCP</div>\n          </div>\n          <p style="text-align: center; color: #aaa; margin-top: 8px; font-size: 13px;">getClaudeCodeMcpConfigs() &#x2192; filterMcpServersByPolicy()</p>\n        </div>\n        <div style="text-align: center; font-size: 24px; color: #e94560;">&#x2193;</div>\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px; margin-bottom: 16px;">\n          <h4 style="color: #0f3460;">传输协议层</h4>\n          <div style="display: grid; grid-template-columns: repeat(5, 1fr); gap: 6px;">\n            <div class="arch-box" style="background: #16213e; padding: 8px; border-radius: 8px; text-align: center; border: 1px solid #0f3460; font-size: 11px;">Stdio</div>\n            <div class="arch-box" style="background: #16213e; padding: 8px; border-radius: 8px; text-align: center; border: 1px solid #0f3460; font-size: 11px;">SSE</div>\n            <div class="arch-box" style="background: #16213e; padding: 8px; border-radius: 8px; text-align: center; border: 1px solid #0f3460; font-size: 11px;">StreamableHTTP</div>\n            <div class="arch-box" style="background: #16213e; padding: 8px; border-radius: 8px; text-align: center; border: 1px solid #0f3460; font-size: 11px;">WebSocket</div>\n            <div class="arch-box" style="background: #16213e; padding: 8px; border-radius: 8px; text-align: center; border: 1px solid #0f3460; font-size: 11px;">SdkControl</div>\n          </div>\n        </div>\n        <div style="text-align: center; font-size: 24px; color: #0f3460;">&#x2193;</div>\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px; margin-bottom: 16px;">\n          <h4 style="color: #533483;">client.ts 核心连接</h4>\n          <div style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 8px;">\n            <div style="background: #16213e; padding: 10px; border-radius: 8px; text-align: center; border: 1px solid #533483; font-size: 12px;">connectToServer()</div>\n            <div style="background: #16213e; padding: 10px; border-radius: 8px; text-align: center; border: 1px solid #533483; font-size: 12px;">fetchToolsForClient()</div>\n            <div style="background: #16213e; padding: 10px; border-radius: 8px; text-align: center; border: 1px solid #533483; font-size: 12px;">prefetchAllMcpResources()</div>\n          </div>\n        </div>\n        <div style="text-align: center; font-size: 24px; color: #533483;">&#x2193;</div>\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px;">\n          <h4 style="color: #48c9b0;">MCPTool 包装器</h4>\n          <p style="color: #aaa; font-size: 14px;">MCP Tool &#x2192; Claude Code Tool 对象 &#x2192; 权限检查 &#x2192; 工具调用</p>\n        </div>\n      </div>\n    </div>\n  ',codeExamples:[{title:"MCP 客户端连接与工具获取",language:"typescript",code:"// connectToServer: 根据配置选择传输协议并连接\nasync function connectToServer(\n  config: McpServerConfig\n): Promise<McpClient> {\n  let transport: Transport;\n\n  switch (config.transport) {\n    case 'stdio':\n      transport = new StdioClientTransport({\n        command: config.command,\n        args: config.args || [],\n        env: config.env\n      });\n      break;\n    case 'sse':\n      transport = new SSEClientTransport(\n        new URL(config.url),\n        { headers: config.headers }\n      );\n      break;\n    case 'streamable-http':\n      transport = new StreamableHTTPClientTransport(\n        new URL(config.url)\n      );\n      break;\n    case 'websocket':\n      transport = new WebSocketTransport(config.url);\n      break;\n    default:\n      throw new Error(\n        'Unsupported transport: ' + config.transport\n      );\n  }\n\n  const client = new McpClient();\n  await client.connect(transport);\n  return client;\n}\n\n// fetchToolsForClient: MCP 工具转换为内部格式\nasync function fetchToolsForClient(\n  client: McpClient,\n  serverName: string\n): Promise<Tool[]> {\n  const mcpTools = await client.listTools();\n  return mcpTools.map(tool => ({\n    name: serverName + ':' + tool.name,\n    description: tool.description,\n    inputSchema: tool.inputSchema,\n    // MCPTool 包装器处理实际调用\n    execute: async (input) => {\n      return client.callTool(tool.name, input);\n    }\n  }));\n}"},{title:"MCP 配置解析与聚合",language:"typescript",code:"// parseMcpConfig: 解析单个 MCP 配置\nfunction parseMcpConfig(\n  raw: Record<string, unknown>\n): McpServerConfig {\n  return {\n    name: String(raw.name),\n    transport: raw.transport as TransportType || 'stdio',\n    command: raw.command as string,\n    args: (raw.args as string[]) || [],\n    env: raw.env as Record<string, string>,\n    url: raw.url as string,\n    headers: raw.headers as Record<string, string>,\n    authType: raw.authType as string\n  };\n}\n\n// getClaudeCodeMcpConfigs: 聚合所有来源的配置\nasync function getClaudeCodeMcpConfigs(): Promise<\n  McpServerConfig[]\n> {\n  const configs: McpServerConfig[] = [];\n\n  // 1. 项目级 .mcp.json\n  const projectConfig = await loadJsonFile(\n    '.mcp.json'\n  );\n  if (projectConfig?.mcpServers) {\n    for (const [name, cfg] of\n      Object.entries(projectConfig.mcpServers)\n    ) {\n      configs.push(parseMcpConfig({ name, ...cfg }));\n    }\n  }\n\n  // 2. 用户全局配置\n  const userConfig = await loadUserMcpConfig();\n  configs.push(...userConfig);\n\n  // 3. 企业管理员配置\n  const enterpriseConfig = await loadEnterpriseMcpConfig();\n  configs.push(...enterpriseConfig);\n\n  // 4. 策略过滤\n  return filterMcpServersByPolicy(configs);\n}\n\n// filterMcpServersByPolicy: 企业策略过滤\nfunction filterMcpServersByPolicy(\n  configs: McpServerConfig[]\n): McpServerConfig[] {\n  const policy = getOrganizationPolicy();\n  if (!policy?.mcpAllowlist) return configs;\n\n  return configs.filter(c =>\n    policy.mcpAllowlist.includes(c.name) ||\n    policy.mcpAllowlist.includes('*')\n  );\n}"},{title:"OAuth 认证与自定义错误处理",language:"typescript",code:"// OAuth 认证流程\nasync function handleMcpAuth(\n  serverConfig: McpServerConfig\n): Promise<string> {\n  if (serverConfig.authType !== 'oauth') {\n    return '';\n  }\n\n  // 检查缓存的 token\n  const cached = await getTokenCache(serverConfig.name);\n  if (cached && !isTokenExpired(cached)) {\n    return cached.accessToken;\n  }\n\n  // 发起 OAuth 流程\n  const authUrl = buildAuthUrl({\n    clientId: serverConfig.oauth?.clientId,\n    redirectUri: 'http://localhost:' + getRandomPort(),\n    scope: serverConfig.oauth?.scope || 'read'\n  });\n\n  // 启动本地服务器接收回调\n  const code = await waitForAuthCallback(authUrl);\n\n  // 交换 access token\n  const token = await exchangeCodeForToken(\n    code, serverConfig\n  );\n  await setTokenCache(serverConfig.name, token);\n  return token.accessToken;\n}\n\n// 自定义错误层次结构\nclass McpAuthError extends Error {\n  constructor(serverName: string, detail: string) {\n    super('MCP Auth Error [' + serverName + ']: ' + detail);\n    this.name = 'McpAuthError';\n  }\n}\n\nclass McpSessionExpiredError extends Error {\n  constructor(serverName: string) {\n    super('MCP session expired: ' + serverName);\n    this.name = 'McpSessionExpiredError';\n  }\n}\n\nclass McpToolCallError extends Error {\n  public readonly toolName: string;\n  public readonly retryable: boolean;\n\n  constructor(\n    toolName: string,\n    message: string,\n    retryable: boolean = false\n  ) {\n    super('MCP tool call failed [' + toolName + ']: '\n      + message);\n    this.name = 'McpToolCallError';\n    this.toolName = toolName;\n    this.retryable = retryable;\n  }\n}"}],interactive:'\n    <h3>MCP 连接交互式演练</h3>\n    <div class="interactive-demo">\n      <div class="step" data-step="1">\n        <h4>Step 1: 配置发现与聚合</h4>\n        <p>系统启动时，<code>getClaudeCodeMcpConfigs()</code> 从四个来源收集 MCP 配置：项目 <code>.mcp.json</code>、用户全局配置、企业配置和 Skill 定义。所有配置经过去重和策略过滤后合并。</p>\n        <div class="code-highlight">\n          <pre>// .mcp.json 示例\n{\n  "mcpServers": {\n    "github": {\n      "transport": "stdio",\n      "command": "npx",\n      "args": ["-y", "@modelcontextprotocol/server-github"]\n    }\n  }\n}</pre>\n        </div>\n      </div>\n      <div class="step" data-step="2">\n        <h4>Step 2: 传输协议选择与连接</h4>\n        <p><code>connectToServer()</code> 根据配置中的 transport 字段选择对应的传输实现。Stdio 模式会启动一个子进程；SSE 模式会建立 HTTP 长连接；WebSocket 模式会建立全双工连接。</p>\n      </div>\n      <div class="step" data-step="3">\n        <h4>Step 3: 工具发现与注册</h4>\n        <p><code>fetchToolsForClient()</code> 调用 MCP 服务器的 <code>tools/list</code> 方法获取工具定义，然后将每个 MCP 工具转换为 Claude Code 的 Tool 接口。工具名称会加上服务器名前缀避免冲突。</p>\n        <div class="code-highlight">\n          <pre>// MCP 工具名转换\n"read_file" &#x2192; "github:read_file"\n"search"    &#x2192; "github:search"</pre>\n        </div>\n      </div>\n      <div class="step" data-step="4">\n        <h4>Step 4: 认证与错误恢复</h4>\n        <p>如果 MCP 服务器需要 OAuth 认证，系统会自动启动认证流程。连接断开时，MCPConnectionManager 会根据错误类型决定是重试连接还是提示用户重新授权。</p>\n      </div>\n    </div>\n  '},c13:{content:"\n    <h2>13.1 CLAUDE.md 多层加载</h2>\n    <p>Claude Code 的内存系统从多个位置加载 <strong>CLAUDE.md</strong> 文件，形成分层的上下文配置体系：</p>\n    <ul>\n      <li><strong>项目级</strong> — 项目根目录的 <code>CLAUDE.md</code>，包含项目特定的指令、架构说明和编码规范</li>\n      <li><strong>用户级</strong> — <code>~/.claude/CLAUDE.md</code>，包含用户个人偏好和通用指令</li>\n      <li><strong>企业级</strong> — 组织管理员配置的 CLAUDE.md，强制所有成员遵循的规范</li>\n    </ul>\n    <p>多层 CLAUDE.md 按优先级合并，企业级覆盖用户级，项目级具有最高优先级用于项目特定配置。</p>\n\n    <h2>13.2 MEMORY.md 系统</h2>\n    <p><strong>memdir/memdir.ts</strong> 实现了 MEMORY.md 内存系统，用于持久化跨会话的关键信息：</p>\n    <ul>\n      <li><code>ENTRYPOINT_NAME</code> — 入口文件名标识</li>\n      <li><code>MAX_ENTRYPOINT_LINES = 200</code> — 入口文件最大行数限制</li>\n      <li><code>MAX_ENTRYPOINT_BYTES = 25000</code> — 入口文件最大字节数限制</li>\n      <li><code>truncateEntrypointContent</code> — 当内容超出限制时的截断策略，保留最重要的信息</li>\n    </ul>\n\n    <h2>13.3 内存类型分类</h2>\n    <p>系统将内存分为四种类型，每种类型有不同的生命周期和用途：</p>\n    <ul>\n      <li><strong>user</strong> — 用户级内存，跨项目持久化的个人偏好和习惯</li>\n      <li><strong>feedback</strong> — 反馈内存，来自用户纠正和指导的学习记录</li>\n      <li><strong>project</strong> — 项目内存，项目特定的上下文信息和决策记录</li>\n      <li><strong>reference</strong> — 参考内存，外部文档和知识的引用</li>\n    </ul>\n    <p><strong>findRelevantMemories.ts</strong> 通过语义搜索在海量内存中找到与当前任务最相关的记忆片段。</p>\n\n    <h2>13.4 四级压缩级联</h2>\n    <p>Claude Code 实现了 <strong>4 级上下文压缩策略</strong>，从轻量到重量级逐步释放上下文空间：</p>\n    <ol>\n      <li><strong>snipCompact</strong> — 激进的旧消息移除（需启用 HISTORY_SNIP 特性标志）。直接删除最早的对话消息，释放大量空间但丢失历史上下文</li>\n      <li><strong>microCompact</strong> — 轻量级工具结果裁剪（始终运行）。保留工具调用记录但截断过长的输出结果，如大文件内容、长日志输出</li>\n      <li><strong>contextCollapse</strong> — 读时投影压缩（需启用 CONTEXT_COLLAPSE 特性标志）。不修改实际消息，而是在读取时动态折叠不活跃的消息段</li>\n      <li><strong>autoCompact</strong> — 完整压缩，使用分叉代理生成对话摘要。这是最彻底的压缩方式，会启动一个子代理来总结当前对话的关键信息</li>\n    </ol>\n\n    <h2>13.5 压缩后恢复与内存提取</h2>\n    <p><strong>compact.ts</strong> 中定义了关键常量：</p>\n    <ul>\n      <li><code>POST_COMPACT_MAX_FILES_TO_RESTORE = 5</code> — 压缩后最多恢复 5 个最近文件的上下文</li>\n      <li><code>POST_COMPACT_TOKEN_BUDGET = 50000</code> — 压缩后文件恢复的 Token 预算上限</li>\n    </ul>\n    <p><strong>sessionMemoryCompact.ts</strong> 在压缩后从对话中提取关键记忆，保存到 MEMORY.md 中，确保重要信息不会因压缩而丢失。<strong>extractMemories</strong> 服务负责智能识别值得记忆的信息片段。</p>\n  ",architecture:'\n    <div class="arch-diagram">\n      <h3>内存与上下文管理架构图</h3>\n      <div class="arch-flow">\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px; margin-bottom: 16px;">\n          <h4 style="color: #e94560;">CLAUDE.md 多层加载</h4>\n          <div style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 8px;">\n            <div style="background: #16213e; padding: 12px; border-radius: 8px; text-align: center; border: 1px solid #e94560;">\n              <div style="font-weight: bold;">企业级</div>\n              <div style="font-size: 12px; color: #aaa;">组织强制规范</div>\n            </div>\n            <div style="background: #16213e; padding: 12px; border-radius: 8px; text-align: center; border: 1px solid #e94560;">\n              <div style="font-weight: bold;">用户级</div>\n              <div style="font-size: 12px; color: #aaa;">~/.claude/CLAUDE.md</div>\n            </div>\n            <div style="background: #16213e; padding: 12px; border-radius: 8px; text-align: center; border: 1px solid #e94560;">\n              <div style="font-weight: bold;">项目级</div>\n              <div style="font-size: 12px; color: #aaa;">./CLAUDE.md</div>\n            </div>\n          </div>\n        </div>\n        <div style="text-align: center; font-size: 24px; color: #e94560;">&#x2193;</div>\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px; margin-bottom: 16px;">\n          <h4 style="color: #0f3460;">MEMORY.md + 内存类型系统</h4>\n          <div style="display: grid; grid-template-columns: repeat(4, 1fr); gap: 8px;">\n            <div style="background: #16213e; padding: 8px; border-radius: 8px; text-align: center; border: 1px solid #0f3460; font-size: 12px;">user</div>\n            <div style="background: #16213e; padding: 8px; border-radius: 8px; text-align: center; border: 1px solid #0f3460; font-size: 12px;">feedback</div>\n            <div style="background: #16213e; padding: 8px; border-radius: 8px; text-align: center; border: 1px solid #0f3460; font-size: 12px;">project</div>\n            <div style="background: #16213e; padding: 8px; border-radius: 8px; text-align: center; border: 1px solid #0f3460; font-size: 12px;">reference</div>\n          </div>\n          <p style="text-align: center; color: #aaa; margin-top: 8px; font-size: 13px;">findRelevantMemories() 语义搜索</p>\n        </div>\n        <div style="text-align: center; font-size: 24px; color: #0f3460;">&#x2193;</div>\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px; margin-bottom: 16px;">\n          <h4 style="color: #533483;">四级压缩级联</h4>\n          <div style="display: flex; align-items: center; gap: 8px; flex-wrap: wrap;">\n            <div style="background: #2d1b4e; padding: 10px 14px; border-radius: 8px; border: 1px solid #533483; font-size: 13px;">\n              <strong>L1</strong> snipCompact\n            </div>\n            <span style="color: #533483;">&#x2192;</span>\n            <div style="background: #2d1b4e; padding: 10px 14px; border-radius: 8px; border: 1px solid #533483; font-size: 13px;">\n              <strong>L2</strong> microCompact\n            </div>\n            <span style="color: #533483;">&#x2192;</span>\n            <div style="background: #2d1b4e; padding: 10px 14px; border-radius: 8px; border: 1px solid #533483; font-size: 13px;">\n              <strong>L3</strong> contextCollapse\n            </div>\n            <span style="color: #533483;">&#x2192;</span>\n            <div style="background: #2d1b4e; padding: 10px 14px; border-radius: 8px; border: 1px solid #533483; font-size: 13px;">\n              <strong>L4</strong> autoCompact\n            </div>\n          </div>\n        </div>\n        <div style="text-align: center; font-size: 24px; color: #533483;">&#x2193;</div>\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px;">\n          <h4 style="color: #48c9b0;">压缩后恢复</h4>\n          <p style="color: #aaa; font-size: 14px;">sessionMemoryCompact &#x2192; extractMemories &#x2192; MEMORY.md 持久化</p>\n          <p style="color: #aaa; font-size: 12px;">MAX_FILES_TO_RESTORE=5 | TOKEN_BUDGET=50000</p>\n        </div>\n      </div>\n    </div>\n  ',codeExamples:[{title:"MEMORY.md 系统与内存类型",language:"typescript",code:"// memdir.ts 核心常量与逻辑\nconst ENTRYPOINT_NAME = 'MEMORY.md';\nconst MAX_ENTRYPOINT_LINES = 200;\nconst MAX_ENTRYPOINT_BYTES = 25000;\n\n// 截断入口内容，保留最重要的信息\nfunction truncateEntrypointContent(\n  content: string\n): string {\n  const lines = content.split('\\n');\n  if (lines.length <= MAX_ENTRYPOINT_LINES &&\n      Buffer.byteLength(content) <= MAX_ENTRYPOINT_BYTES) {\n    return content;\n  }\n\n  // 优先保留最近添加的内存（文件末尾）\n  let truncated = lines\n    .slice(-MAX_ENTRYPOINT_LINES)\n    .join('\\n');\n\n  // 字节数检查\n  while (Buffer.byteLength(truncated) > MAX_ENTRYPOINT_BYTES) {\n    const parts = truncated.split('\\n');\n    parts.shift(); // 移除最旧的一行\n    truncated = parts.join('\\n');\n  }\n  return truncated;\n}\n\n// 内存类型定义\ntype MemoryType = 'user' | 'feedback' | 'project' | 'reference';\n\ninterface MemoryEntry {\n  type: MemoryType;\n  content: string;\n  timestamp: number;\n  source: string;  // 来源标识\n  relevanceScore?: number;\n}\n\n// findRelevantMemories: 语义搜索相关内存\nasync function findRelevantMemories(\n  query: string,\n  memories: MemoryEntry[],\n  topK: number = 10\n): Promise<MemoryEntry[]> {\n  const scored = memories.map(m => ({\n    ...m,\n    relevanceScore: computeSimilarity(query, m.content)\n  }));\n  scored.sort((a, b) =>\n    (b.relevanceScore || 0) - (a.relevanceScore || 0)\n  );\n  return scored.slice(0, topK);\n}"},{title:"四级压缩级联实现",language:"typescript",code:"// 压缩后恢复常量\nconst POST_COMPACT_MAX_FILES_TO_RESTORE = 5;\nconst POST_COMPACT_TOKEN_BUDGET = 50000;\n\n// L1: snipCompact - 激进的旧消息移除\nfunction snipCompact(\n  messages: Message[],\n  targetTokens: number\n): Message[] {\n  if (!isFeatureEnabled('HISTORY_SNIP')) {\n    return messages;\n  }\n  // 从最旧的消息开始删除\n  const result = [...messages];\n  let currentTokens = countTokens(result);\n  while (currentTokens > targetTokens && result.length > 2) {\n    result.shift();\n    currentTokens = countTokens(result);\n  }\n  return result;\n}\n\n// L2: microCompact - 轻量级工具结果裁剪（始终运行）\nfunction microCompact(messages: Message[]): Message[] {\n  return messages.map(msg => {\n    if (msg.type !== 'tool_result') return msg;\n    // 截断过长的工具输出\n    if (msg.content.length > 10000) {\n      return {\n        ...msg,\n        content: msg.content.slice(0, 5000)\n          + '\\n... [已截断] ...\\n'\n          + msg.content.slice(-2000)\n      };\n    }\n    return msg;\n  });\n}\n\n// L4: autoCompact - 使用分叉代理生成摘要\nasync function autoCompact(\n  messages: Message[],\n  context: CompactContext\n): Promise<CompactResult> {\n  // 启动子代理进行摘要\n  const summary = await forkAgent({\n    prompt: '请总结以下对话的关键信息...',\n    messages: messages,\n    maxTokens: 2000\n  });\n\n  // 压缩后恢复最近文件\n  const recentFiles = getRecentlyAccessedFiles(messages)\n    .slice(0, POST_COMPACT_MAX_FILES_TO_RESTORE);\n\n  return {\n    summary: summary,\n    restoredFiles: recentFiles,\n    tokenBudget: POST_COMPACT_TOKEN_BUDGET\n  };\n}"},{title:"会话内存提取与持久化",language:"typescript",code:"// sessionMemoryCompact.ts: 压缩后内存提取\nasync function sessionMemoryCompact(\n  messages: Message[],\n  existingMemories: MemoryEntry[]\n): Promise<MemoryEntry[]> {\n  // 使用 AI 提取值得记忆的信息\n  const extracted = await extractMemories(messages);\n\n  // 过滤重复内存\n  const newMemories = extracted.filter(\n    mem => !existingMemories.some(\n      existing => isSimilar(existing.content, mem.content)\n    )\n  );\n\n  // 写入 MEMORY.md\n  const memoryContent = newMemories\n    .map(m => formatMemoryEntry(m))\n    .join('\\n');\n\n  await appendToMemoryFile(memoryContent);\n  return [...existingMemories, ...newMemories];\n}\n\n// extractMemories: 智能记忆提取服务\nasync function extractMemories(\n  messages: Message[]\n): Promise<MemoryEntry[]> {\n  const memories: MemoryEntry[] = [];\n\n  // 提取用户纠正（feedback 类型）\n  for (const msg of messages) {\n    if (isUserCorrection(msg)) {\n      memories.push({\n        type: 'feedback',\n        content: msg.content,\n        timestamp: Date.now(),\n        source: 'user_correction'\n      });\n    }\n  }\n\n  // 提取项目发现（project 类型）\n  const projectInsights = await analyzeForInsights(\n    messages\n  );\n  for (const insight of projectInsights) {\n    memories.push({\n      type: 'project',\n      content: insight,\n      timestamp: Date.now(),\n      source: 'auto_extract'\n    });\n  }\n\n  return memories;\n}\n\n// CLAUDE.md 多层加载\nasync function loadClaudeMd(): Promise<string> {\n  const layers: string[] = [];\n\n  // 企业级（最低优先级）\n  const enterprise = await loadEnterpriseClaude();\n  if (enterprise) layers.push(enterprise);\n\n  // 用户级\n  const user = await loadFile('~/.claude/CLAUDE.md');\n  if (user) layers.push(user);\n\n  // 项目级（最高优先级）\n  const project = await loadFile('./CLAUDE.md');\n  if (project) layers.push(project);\n\n  return layers.join('\\n---\\n');\n}"}],interactive:'\n    <h3>上下文压缩交互式演练</h3>\n    <div class="interactive-demo">\n      <div class="step" data-step="1">\n        <h4>Step 1: 上下文空间监控</h4>\n        <p>系统持续监控当前上下文窗口的 Token 使用量。当使用量接近模型上下文窗口限制时（如 200K tokens 中已使用 180K），触发压缩流程。</p>\n        <div class="code-highlight">\n          <pre>// Token 监控\ncurrentTokens: 182,400 / 200,000\nusage: 91.2%  // 超过阈值，触发压缩</pre>\n        </div>\n      </div>\n      <div class="step" data-step="2">\n        <h4>Step 2: 级联压缩执行</h4>\n        <p>压缩按级联顺序执行。<strong>microCompact</strong> 始终运行，截断过长的工具输出。如果仍然空间不足，根据特性标志决定是否启用 snipCompact 和 contextCollapse。最后才使用 autoCompact 进行完整压缩。</p>\n        <div class="code-highlight">\n          <pre>L2 microCompact: 182K &#x2192; 165K tokens  // 截断工具输出\nL3 contextCollapse: 165K &#x2192; 140K tokens  // 折叠不活跃段\nL4 autoCompact: 140K &#x2192; 45K tokens  // AI 摘要压缩</pre>\n        </div>\n      </div>\n      <div class="step" data-step="3">\n        <h4>Step 3: 内存提取与保存</h4>\n        <p>压缩过程中，<code>sessionMemoryCompact</code> 从即将被压缩的消息中提取关键记忆。用户的纠正被标记为 feedback 类型，项目发现被标记为 project 类型，确保重要信息不丢失。</p>\n      </div>\n      <div class="step" data-step="4">\n        <h4>Step 4: 压缩后恢复</h4>\n        <p>autoCompact 完成后，系统恢复最近 5 个文件的上下文（受 50000 Token 预算限制），确保代理能继续当前工作。摘要包含关键决策、文件修改记录和待完成任务。</p>\n      </div>\n    </div>\n  '},c14:{content:"\n    <h2>14.1 AgentTool 与子代理生成</h2>\n    <p>Claude Code 的子代理系统通过 <strong>AgentTool.tsx</strong> 提供统一的子代理生成入口。AgentTool 是一个特殊的工具，允许主代理在需要时创建独立的子代理来执行特定任务。每个子代理拥有独立的上下文窗口、工具集合和执行环境。</p>\n    <p>子代理通过 <strong>AgentDefinition</strong> 接口定义：</p>\n    <ul>\n      <li><code>name</code> — 代理名称，用于识别和日志记录</li>\n      <li><code>prompt</code> — 代理的系统提示词，定义其角色和行为</li>\n      <li><code>tools</code> — 允许使用的工具列表（可限制以保障安全）</li>\n      <li><code>maxTurns</code> — 最大交互轮次限制</li>\n      <li><code>mcpServers</code> — 代理专属的 MCP 服务器配置</li>\n    </ul>\n\n    <h2>14.2 runAgent 核心执行流程</h2>\n    <p><strong>runAgent.ts</strong> 是子代理执行的核心模块，实现了完整的代理运行循环：</p>\n    <ol>\n      <li><strong>创建子代理上下文</strong> — 初始化独立的消息历史、工具注册表和权限范围</li>\n      <li><strong>构建代理提示词</strong> — 将 AgentDefinition 的 prompt 与系统指令、上下文信息合并</li>\n      <li><strong>过滤工具集</strong> — 根据代理定义限制可用工具，例如探索代理只能使用只读工具</li>\n      <li><strong>递归调用 query()</strong> — 进入代理主循环，与 Claude 模型交互执行任务</li>\n      <li><strong>产出进度</strong> — 通过 yield 机制实时报告子代理执行进度</li>\n      <li><strong>返回摘要</strong> — 任务完成后生成执行摘要返回给父代理</li>\n    </ol>\n\n    <h2>14.3 分叉代理与缓存共享</h2>\n    <p><strong>forkSubagent.ts</strong> 实现了基于进程分叉的子代理生成。通过 fork 机制，子代理可以共享父进程的 KV 缓存和模型连接，大幅减少冷启动时间。这对于需要频繁创建子代理的场景（如多文件处理）尤为重要。</p>\n\n    <h2>14.4 自定义与内置代理</h2>\n    <p><strong>loadAgentsDir.ts</strong> 从 <code>.claude/agents/</code> 目录加载用户自定义代理定义。系统同时内置了多个专用代理：</p>\n    <ul>\n      <li><strong>exploreAgent</strong> — 快速代码库探索代理，使用只读工具集快速理解代码结构</li>\n      <li><strong>planAgent</strong> — 实现规划代理，制定详细的实现方案</li>\n      <li><strong>generalPurposeAgent</strong> — 通用多步骤任务代理，处理不适合其他专用代理的任务</li>\n      <li><strong>claudeCodeGuideAgent</strong> — Claude Code 使用指导代理</li>\n      <li><strong>statuslineSetup</strong> — 状态栏配置代理</li>\n      <li><strong>verificationAgent</strong> — 验证代理，用于验证任务执行结果</li>\n    </ul>\n\n    <h2>14.5 代理 MCP 与状态管理</h2>\n    <p>代理的 MCP 配置支持两种引用方式：</p>\n    <ul>\n      <li><strong>字符串引用</strong> — 共享父代理的 MCP 连接，多个子代理复用同一连接，减少资源消耗</li>\n      <li><strong>内联定义</strong> — 代理专属的 MCP 服务器，随代理生命周期创建和销毁</li>\n    </ul>\n    <p><strong>resumeAgent.ts</strong> 处理代理状态恢复，当代理因错误中断时可以从检查点恢复执行。<strong>agentColorManager.ts</strong> 为每个活跃代理分配唯一的显示颜色，方便在终端中区分不同代理的输出。<strong>SendMessageTool</strong> 实现代理间消息传递，允许子代理向父代理或兄弟代理发送消息。</p>\n  ",architecture:'\n    <div class="arch-diagram">\n      <h3>子代理系统架构图</h3>\n      <div class="arch-flow">\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px; margin-bottom: 16px;">\n          <h4 style="color: #e94560;">主代理 (Parent Agent)</h4>\n          <div style="display: flex; justify-content: center; gap: 16px;">\n            <div style="background: #16213e; padding: 12px 20px; border-radius: 8px; border: 1px solid #e94560;">AgentTool.tsx</div>\n            <div style="background: #16213e; padding: 12px 20px; border-radius: 8px; border: 1px solid #e94560;">SendMessageTool</div>\n          </div>\n        </div>\n        <div style="text-align: center; font-size: 24px; color: #e94560;">&#x2193;</div>\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px; margin-bottom: 16px;">\n          <h4 style="color: #0f3460;">runAgent.ts 执行引擎</h4>\n          <div style="display: flex; flex-wrap: wrap; gap: 8px; justify-content: center;">\n            <div style="background: #16213e; padding: 8px 12px; border-radius: 6px; border: 1px solid #0f3460; font-size: 13px;">创建上下文</div>\n            <span style="color: #0f3460; line-height: 36px;">&#x2192;</span>\n            <div style="background: #16213e; padding: 8px 12px; border-radius: 6px; border: 1px solid #0f3460; font-size: 13px;">构建提示词</div>\n            <span style="color: #0f3460; line-height: 36px;">&#x2192;</span>\n            <div style="background: #16213e; padding: 8px 12px; border-radius: 6px; border: 1px solid #0f3460; font-size: 13px;">过滤工具</div>\n            <span style="color: #0f3460; line-height: 36px;">&#x2192;</span>\n            <div style="background: #16213e; padding: 8px 12px; border-radius: 6px; border: 1px solid #0f3460; font-size: 13px;">query() 循环</div>\n            <span style="color: #0f3460; line-height: 36px;">&#x2192;</span>\n            <div style="background: #16213e; padding: 8px 12px; border-radius: 6px; border: 1px solid #0f3460; font-size: 13px;">返回摘要</div>\n          </div>\n        </div>\n        <div style="text-align: center; font-size: 24px; color: #0f3460;">&#x2193;</div>\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px; margin-bottom: 16px;">\n          <h4 style="color: #533483;">内置代理集合</h4>\n          <div style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 8px;">\n            <div style="background: #2d1b4e; padding: 10px; border-radius: 8px; text-align: center; border: 1px solid #533483; font-size: 12px;">\n              <div style="font-weight: bold;">exploreAgent</div>\n              <div style="color: #aaa;">代码库探索</div>\n            </div>\n            <div style="background: #2d1b4e; padding: 10px; border-radius: 8px; text-align: center; border: 1px solid #533483; font-size: 12px;">\n              <div style="font-weight: bold;">planAgent</div>\n              <div style="color: #aaa;">实现规划</div>\n            </div>\n            <div style="background: #2d1b4e; padding: 10px; border-radius: 8px; text-align: center; border: 1px solid #533483; font-size: 12px;">\n              <div style="font-weight: bold;">generalPurpose</div>\n              <div style="color: #aaa;">通用多步骤</div>\n            </div>\n            <div style="background: #2d1b4e; padding: 10px; border-radius: 8px; text-align: center; border: 1px solid #533483; font-size: 12px;">\n              <div style="font-weight: bold;">guideAgent</div>\n              <div style="color: #aaa;">使用指导</div>\n            </div>\n            <div style="background: #2d1b4e; padding: 10px; border-radius: 8px; text-align: center; border: 1px solid #533483; font-size: 12px;">\n              <div style="font-weight: bold;">verificationAgent</div>\n              <div style="color: #aaa;">结果验证</div>\n            </div>\n            <div style="background: #2d1b4e; padding: 10px; border-radius: 8px; text-align: center; border: 1px solid #533483; font-size: 12px;">\n              <div style="font-weight: bold;">自定义代理</div>\n              <div style="color: #aaa;">.claude/agents/</div>\n            </div>\n          </div>\n        </div>\n        <div style="text-align: center; font-size: 24px; color: #533483;">&#x2193;</div>\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px;">\n          <h4 style="color: #48c9b0;">MCP 连接策略 &amp; 状态管理</h4>\n          <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 12px;">\n            <div style="background: #16213e; padding: 10px; border-radius: 8px; border: 1px solid #48c9b0; font-size: 13px;">\n              <strong>字符串引用:</strong> 共享父连接\n            </div>\n            <div style="background: #16213e; padding: 10px; border-radius: 8px; border: 1px solid #48c9b0; font-size: 13px;">\n              <strong>内联定义:</strong> 代理专属连接\n            </div>\n          </div>\n        </div>\n      </div>\n    </div>\n  ',codeExamples:[{title:"AgentDefinition 与 AgentTool",language:"typescript",code:"// AgentDefinition: 代理定义接口\ninterface AgentDefinition {\n  name: string;\n  prompt: string;\n  tools?: string[];        // 允许的工具列表\n  maxTurns?: number;       // 最大交互轮次\n  mcpServers?: McpConfig;  // MCP 配置\n  model?: string;          // 使用的模型\n}\n\n// 内置代理定义示例\nconst exploreAgent: AgentDefinition = {\n  name: 'exploreAgent',\n  prompt: '你是一个代码库探索专家。使用只读工具快速理解'\n    + '代码结构、找到关键文件和理解架构设计。'\n    + '不要修改任何文件。',\n  tools: ['Read', 'Glob', 'Grep', 'LSP', 'Bash'],\n  maxTurns: 15\n};\n\nconst planAgent: AgentDefinition = {\n  name: 'planAgent',\n  prompt: '你是一个实现规划专家。分析需求后制定详细的'\n    + '实现方案，包括文件修改计划、测试策略和风险评估。',\n  tools: ['Read', 'Glob', 'Grep', 'LSP'],\n  maxTurns: 10\n};\n\n// AgentTool: 子代理生成工具\nconst AgentTool = {\n  name: 'Agent',\n  description: '创建子代理来执行复杂的多步骤任务',\n  inputSchema: {\n    type: 'object',\n    properties: {\n      agent: { type: 'string', description: '代理名称' },\n      task: { type: 'string', description: '任务描述' }\n    },\n    required: ['agent', 'task']\n  },\n  execute: async (input: AgentToolInput) => {\n    const definition = resolveAgent(input.agent);\n    return runAgent(definition, input.task);\n  }\n};"},{title:"runAgent 核心执行循环",language:"typescript",code:"// runAgent.ts: 子代理核心执行流程\nasync function* runAgent(\n  definition: AgentDefinition,\n  task: string\n): AsyncGenerator<AgentProgress, AgentResult> {\n  // 1. 创建子代理上下文\n  const context = createSubagentContext({\n    parentContext: getCurrentContext(),\n    agentName: definition.name\n  });\n\n  // 2. 构建代理提示词\n  const systemPrompt = buildAgentPrompt(\n    definition.prompt,\n    context\n  );\n\n  // 3. 过滤工具集\n  const tools = filterTools(\n    getAllTools(),\n    definition.tools  // 只保留允许的工具\n  );\n\n  // 4. 初始化消息\n  const messages: Message[] = [\n    { role: 'user', content: task }\n  ];\n\n  // 5. 代理执行循环\n  let turns = 0;\n  while (turns < (definition.maxTurns || 20)) {\n    // 递归调用 query()\n    const response = await query({\n      systemPrompt,\n      messages,\n      tools,\n      model: definition.model\n    });\n\n    // 产出进度更新\n    yield {\n      type: 'progress',\n      agent: definition.name,\n      turn: turns,\n      content: response.content\n    };\n\n    // 检查是否完成\n    if (!response.toolCalls || response.toolCalls.length === 0) {\n      break;\n    }\n\n    // 执行工具调用并追加消息\n    for (const call of response.toolCalls) {\n      const result = await executeTool(call, context);\n      messages.push(\n        { role: 'assistant', content: response.content },\n        { role: 'tool', content: result }\n      );\n    }\n    turns++;\n  }\n\n  // 6. 返回执行摘要\n  return {\n    agent: definition.name,\n    summary: messages[messages.length - 1].content,\n    turnsUsed: turns\n  };\n}"},{title:"forkSubagent 与自定义代理加载",language:"typescript",code:"// forkSubagent.ts: 基于进程分叉的子代理\nasync function forkSubagent(\n  definition: AgentDefinition,\n  task: string\n): Promise<AgentResult> {\n  // 分叉当前进程，共享 KV 缓存\n  const child = fork(process.argv[1], {\n    env: {\n      ...process.env,\n      AGENT_MODE: 'subagent',\n      AGENT_NAME: definition.name,\n      AGENT_TASK: task,\n      // 共享父进程的缓存路径\n      CACHE_DIR: getCacheDir()\n    }\n  });\n\n  return new Promise((resolve, reject) => {\n    child.on('message', (msg: AgentMessage) => {\n      if (msg.type === 'result') {\n        resolve(msg.data);\n      }\n    });\n    child.on('error', reject);\n    child.on('exit', (code) => {\n      if (code !== 0) {\n        reject(new Error(\n          'Subagent exited with code ' + code\n        ));\n      }\n    });\n  });\n}\n\n// loadAgentsDir.ts: 加载自定义代理\nasync function loadAgentsDir(\n  dir: string\n): Promise<AgentDefinition[]> {\n  const agentsPath = join(dir, '.claude', 'agents');\n  const files = await readdir(agentsPath).catch(() => []);\n  const agents: AgentDefinition[] = [];\n\n  for (const file of files) {\n    if (!file.endsWith('.md')) continue;\n    const content = await readFile(\n      join(agentsPath, file), 'utf-8'\n    );\n    // 解析 frontmatter 获取代理配置\n    const { frontmatter, body } = parseFrontmatter(content);\n    agents.push({\n      name: frontmatter.name || basename(file, '.md'),\n      prompt: body,\n      tools: frontmatter.tools,\n      maxTurns: frontmatter.maxTurns,\n      mcpServers: frontmatter.mcpServers\n    });\n  }\n  return agents;\n}\n\n// agentColorManager.ts: 代理颜色分配\nclass AgentColorManager {\n  private colors = [\n    '#e94560', '#0f3460', '#533483',\n    '#48c9b0', '#f39c12', '#3498db'\n  ];\n  private assignments = new Map<string, string>();\n\n  getColor(agentName: string): string {\n    if (!this.assignments.has(agentName)) {\n      const idx = this.assignments.size % this.colors.length;\n      this.assignments.set(agentName, this.colors[idx]);\n    }\n    return this.assignments.get(agentName)!;\n  }\n}"}],interactive:'\n    <h3>子代理执行交互式演练</h3>\n    <div class="interactive-demo">\n      <div class="step" data-step="1">\n        <h4>Step 1: 主代理决定委派任务</h4>\n        <p>当主代理判断当前任务需要独立探索代码库时，调用 AgentTool 创建一个 exploreAgent 子代理。主代理提供明确的任务描述。</p>\n        <div class="code-highlight">\n          <pre>// 主代理调用 AgentTool\nAgentTool.execute({\n  agent: \'exploreAgent\',\n  task: \'分析 src/services/ 目录的架构设计，找到所有服务间的依赖关系\'\n})</pre>\n        </div>\n      </div>\n      <div class="step" data-step="2">\n        <h4>Step 2: runAgent 初始化子代理</h4>\n        <p>runAgent 创建独立的执行上下文，构建针对探索任务优化的提示词，并将工具集限制为只读工具（Read、Glob、Grep、LSP）。子代理无法修改任何文件。</p>\n        <div class="code-highlight">\n          <pre>// 工具过滤\n允许的工具: Read, Glob, Grep, LSP, Bash\n禁止的工具: Write, Edit, NotebookEdit（只读模式）</pre>\n        </div>\n      </div>\n      <div class="step" data-step="3">\n        <h4>Step 3: 子代理自主执行</h4>\n        <p>子代理进入独立的执行循环，自主使用工具探索代码库。每个交互轮次会通过 yield 向父代理报告进度。子代理最多执行 15 轮（exploreAgent 的 maxTurns 限制）。</p>\n      </div>\n      <div class="step" data-step="4">\n        <h4>Step 4: 结果摘要返回</h4>\n        <p>子代理完成探索后，生成结构化的执行摘要返回给主代理。摘要包含发现的架构信息、关键文件列表和依赖关系图。主代理据此继续后续工作。</p>\n        <div class="code-highlight">\n          <pre>// 返回结果\n{\n  agent: \'exploreAgent\',\n  summary: \'发现 12 个服务模块，核心依赖链...\',\n  turnsUsed: 8\n}</pre>\n        </div>\n      </div>\n    </div>\n  '},c15:{content:"\n    <h2>15.1 TeamCreateTool 与 TeamDeleteTool</h2>\n    <p>Claude Code 的多代理协作系统通过 <strong>TeamCreateTool</strong> 和 <strong>TeamDeleteTool</strong> 提供团队管理能力：</p>\n    <ul>\n      <li><strong>TeamCreateTool</strong> — 创建并行工作团队。协调者代理（Coordinator）可以定义团队的组成、每个成员的任务和协作策略。团队成员以并行方式执行各自的任务，共享必要的上下文和权限</li>\n      <li><strong>TeamDeleteTool</strong> — 删除团队中的工作者。当某个工作者完成任务或不再需要时，协调者可以通过此工具回收资源</li>\n    </ul>\n    <p>团队创建后，每个成员都是一个独立的代理实例，拥有独立的上下文窗口和工具集，但通过共享内存机制实现信息同步。</p>\n\n    <h2>15.2 Swarm 编排架构</h2>\n    <p><strong>utils/swarm/</strong> 目录包含 22+ 个文件，构成了完整的 Swarm 编排系统：</p>\n    <h3>三种执行后端</h3>\n    <ul>\n      <li><strong>ITermBackend</strong> — iTerm2 终端后端，每个工作者在独立的 iTerm 标签页中运行，适合 macOS 开发环境下的可视化调试</li>\n      <li><strong>TmuxBackend</strong> — Tmux 会话后端，每个工作者在独立的 Tmux 窗格中运行，适合远程服务器和 CI/CD 环境</li>\n      <li><strong>InProcessBackend</strong> — 进程内后端，所有工作者在同一进程中运行，通过异步任务切换实现并行。资源开销最小，适合轻量级协作</li>\n    </ul>\n\n    <h2>15.3 Swarm 核心模块</h2>\n    <ul>\n      <li><strong>inProcessRunner.ts</strong> — 进程内队友执行器，管理工作者的生命周期、任务分配和结果收集</li>\n      <li><strong>permissionSync.ts</strong> — 权限同步模块，确保所有工作者共享一致的权限状态。当一个工作者获得用户授权后，其他工作者自动继承该权限</li>\n      <li><strong>teamHelpers.ts</strong> — 团队辅助函数，包括工作者状态查询、负载均衡和健康检查</li>\n      <li><strong>teammateInit.ts</strong> — 队友初始化模块，处理工作者的上下文准备、工具注册和 MCP 连接</li>\n      <li><strong>teammateModel.ts</strong> — 队友模型选择，根据任务类型和复杂度为每个工作者选择最合适的 AI 模型</li>\n      <li><strong>teammatePromptAddendum.ts</strong> — 队友附加提示词，为工作者注入协作相关的额外指令</li>\n    </ul>\n\n    <h2>15.4 协调者模式</h2>\n    <p><strong>coordinator/coordinatorMode.ts</strong> 实现了多代理编排的协调者模式。协调者负责：</p>\n    <ul>\n      <li>分析任务并拆解为可并行执行的子任务</li>\n      <li>创建工作者团队并分配任务</li>\n      <li>监控工作者进度，处理失败和重试</li>\n      <li>汇总所有工作者的结果，生成最终输出</li>\n    </ul>\n\n    <h2>15.5 团队内存同步与安全</h2>\n    <p><strong>services/teamMemorySync/</strong> 提供团队级别的内存同步服务：</p>\n    <ul>\n      <li>工作者之间共享关键发现和上下文信息</li>\n      <li>防止重复工作和冲突操作</li>\n      <li>内置秘密扫描（Secret Scanning），防止敏感信息在工作者之间泄露</li>\n    </ul>\n    <p><strong>tasks/InProcessTeammateTask/</strong> 定义了进程内队友的任务类型，管理任务的创建、执行和完成生命周期。</p>\n  ",architecture:'\n    <div class="arch-diagram">\n      <h3>Teams/Swarm 多代理协作架构图</h3>\n      <div class="arch-flow">\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px; margin-bottom: 16px;">\n          <h4 style="color: #e94560;">协调者层 (Coordinator)</h4>\n          <div style="display: flex; justify-content: center; gap: 16px;">\n            <div style="background: #16213e; padding: 12px 16px; border-radius: 8px; border: 2px solid #e94560;">\n              <div style="font-weight: bold;">coordinatorMode.ts</div>\n              <div style="font-size: 12px; color: #aaa;">任务拆解 &amp; 编排</div>\n            </div>\n            <div style="background: #16213e; padding: 12px 16px; border-radius: 8px; border: 1px solid #e94560;">TeamCreateTool</div>\n            <div style="background: #16213e; padding: 12px 16px; border-radius: 8px; border: 1px solid #e94560;">TeamDeleteTool</div>\n          </div>\n        </div>\n        <div style="text-align: center; font-size: 24px; color: #e94560;">&#x2193;</div>\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px; margin-bottom: 16px;">\n          <h4 style="color: #0f3460;">Swarm 编排层 (utils/swarm/)</h4>\n          <div style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 8px; margin-bottom: 12px;">\n            <div style="background: #16213e; padding: 10px; border-radius: 8px; text-align: center; border: 1px solid #0f3460;">\n              <div style="font-weight: bold; font-size: 13px;">ITermBackend</div>\n              <div style="font-size: 11px; color: #aaa;">iTerm2 标签页</div>\n            </div>\n            <div style="background: #16213e; padding: 10px; border-radius: 8px; text-align: center; border: 1px solid #0f3460;">\n              <div style="font-weight: bold; font-size: 13px;">TmuxBackend</div>\n              <div style="font-size: 11px; color: #aaa;">Tmux 窗格</div>\n            </div>\n            <div style="background: #16213e; padding: 10px; border-radius: 8px; text-align: center; border: 1px solid #0f3460;">\n              <div style="font-weight: bold; font-size: 13px;">InProcessBackend</div>\n              <div style="font-size: 11px; color: #aaa;">进程内异步</div>\n            </div>\n          </div>\n          <div style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 8px;">\n            <div style="background: #0d1b2a; padding: 8px; border-radius: 6px; text-align: center; font-size: 12px; border: 1px solid #1b4965;">teammateInit</div>\n            <div style="background: #0d1b2a; padding: 8px; border-radius: 6px; text-align: center; font-size: 12px; border: 1px solid #1b4965;">teammateModel</div>\n            <div style="background: #0d1b2a; padding: 8px; border-radius: 6px; text-align: center; font-size: 12px; border: 1px solid #1b4965;">teammatePrompt</div>\n          </div>\n        </div>\n        <div style="text-align: center; font-size: 24px; color: #0f3460;">&#x2193;</div>\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px; margin-bottom: 16px;">\n          <h4 style="color: #533483;">工作者层 (Workers)</h4>\n          <div style="display: flex; justify-content: center; gap: 12px; flex-wrap: wrap;">\n            <div style="background: #2d1b4e; padding: 12px 16px; border-radius: 8px; border: 1px solid #533483; min-width: 100px; text-align: center;">\n              <div style="font-weight: bold;">Worker A</div>\n              <div style="font-size: 11px; color: #aaa;">前端重构</div>\n            </div>\n            <div style="background: #2d1b4e; padding: 12px 16px; border-radius: 8px; border: 1px solid #533483; min-width: 100px; text-align: center;">\n              <div style="font-weight: bold;">Worker B</div>\n              <div style="font-size: 11px; color: #aaa;">后端 API</div>\n            </div>\n            <div style="background: #2d1b4e; padding: 12px 16px; border-radius: 8px; border: 1px solid #533483; min-width: 100px; text-align: center;">\n              <div style="font-weight: bold;">Worker C</div>\n              <div style="font-size: 11px; color: #aaa;">测试编写</div>\n            </div>\n          </div>\n        </div>\n        <div style="text-align: center; font-size: 24px; color: #533483;">&#x2193;</div>\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px;">\n          <h4 style="color: #48c9b0;">共享层</h4>\n          <div style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 8px;">\n            <div style="background: #16213e; padding: 10px; border-radius: 8px; text-align: center; border: 1px solid #48c9b0; font-size: 13px;">\n              <div style="font-weight: bold;">permissionSync</div>\n              <div style="font-size: 11px; color: #aaa;">权限同步</div>\n            </div>\n            <div style="background: #16213e; padding: 10px; border-radius: 8px; text-align: center; border: 1px solid #48c9b0; font-size: 13px;">\n              <div style="font-weight: bold;">teamMemorySync</div>\n              <div style="font-size: 11px; color: #aaa;">内存同步</div>\n            </div>\n            <div style="background: #16213e; padding: 10px; border-radius: 8px; text-align: center; border: 1px solid #48c9b0; font-size: 13px;">\n              <div style="font-weight: bold;">secretScanning</div>\n              <div style="font-size: 11px; color: #aaa;">秘密扫描</div>\n            </div>\n          </div>\n        </div>\n      </div>\n    </div>\n  ',codeExamples:[{title:"TeamCreateTool 与协调者模式",language:"typescript",code:"// TeamCreateTool: 创建并行工作团队\nconst TeamCreateTool = {\n  name: 'TeamCreate',\n  description: '创建一个并行工作团队来执行多个子任务',\n  inputSchema: {\n    type: 'object',\n    properties: {\n      workers: {\n        type: 'array',\n        items: {\n          type: 'object',\n          properties: {\n            name: { type: 'string' },\n            task: { type: 'string' },\n            tools: {\n              type: 'array',\n              items: { type: 'string' }\n            }\n          }\n        }\n      },\n      backend: {\n        type: 'string',\n        enum: ['iterm', 'tmux', 'in-process']\n      }\n    }\n  },\n  execute: async (input: TeamCreateInput) => {\n    const backend = selectBackend(input.backend);\n    const workers = await Promise.all(\n      input.workers.map(w =>\n        backend.spawnWorker({\n          name: w.name,\n          task: w.task,\n          tools: w.tools\n        })\n      )\n    );\n    return { teamId: generateId(), workers };\n  }\n};\n\n// coordinatorMode.ts: 协调者编排模式\nasync function coordinatorMode(\n  task: string,\n  context: CoordinatorContext\n): Promise<CoordinatorResult> {\n  // 1. 分析任务，拆解为子任务\n  const subtasks = await analyzeAndDecompose(task);\n\n  // 2. 创建工作者团队\n  const team = await TeamCreateTool.execute({\n    workers: subtasks.map(st => ({\n      name: st.name,\n      task: st.description,\n      tools: st.requiredTools\n    })),\n    backend: context.preferredBackend || 'in-process'\n  });\n\n  // 3. 监控进度\n  const results = await monitorTeam(team.teamId);\n\n  // 4. 汇总结果\n  return {\n    summary: aggregateResults(results),\n    teamId: team.teamId\n  };\n}"},{title:"Swarm 执行后端与进程内运行器",language:"typescript",code:"// 三种执行后端接口\ninterface SwarmBackend {\n  spawnWorker(config: WorkerConfig): Promise<Worker>;\n  killWorker(workerId: string): Promise<void>;\n  getWorkerStatus(workerId: string): WorkerStatus;\n}\n\n// InProcessBackend: 进程内后端\nclass InProcessBackend implements SwarmBackend {\n  private workers = new Map<string, InProcessWorker>();\n\n  async spawnWorker(\n    config: WorkerConfig\n  ): Promise<Worker> {\n    const worker = new InProcessWorker(config);\n    this.workers.set(worker.id, worker);\n\n    // 初始化队友\n    await teammateInit(worker, {\n      tools: config.tools,\n      model: teammateModel.select(config.task),\n      prompt: config.task\n        + '\\n'\n        + teammatePromptAddendum.get()\n    });\n\n    // 异步启动执行\n    worker.run().catch(err => {\n      worker.status = 'failed';\n      worker.error = err;\n    });\n\n    return worker;\n  }\n\n  async killWorker(workerId: string): Promise<void> {\n    const worker = this.workers.get(workerId);\n    if (worker) {\n      await worker.abort();\n      this.workers.delete(workerId);\n    }\n  }\n\n  getWorkerStatus(workerId: string): WorkerStatus {\n    const worker = this.workers.get(workerId);\n    return worker?.status || 'unknown';\n  }\n}\n\n// TmuxBackend: Tmux 会话后端\nclass TmuxBackend implements SwarmBackend {\n  async spawnWorker(\n    config: WorkerConfig\n  ): Promise<Worker> {\n    // 创建 Tmux 窗格\n    const paneId = await execCommand(\n      'tmux split-window -h -P -F \"#{pane_id}\"'\n    );\n    // 在窗格中启动 Claude Code 子进程\n    await execCommand(\n      'tmux send-keys -t ' + paneId\n      + ' \"claude --agent-mode --task '\n      + escapeShell(config.task) + '\" Enter'\n    );\n    return { id: paneId, status: 'running' };\n  }\n\n  async killWorker(workerId: string): Promise<void> {\n    await execCommand('tmux kill-pane -t ' + workerId);\n  }\n\n  getWorkerStatus(workerId: string): WorkerStatus {\n    // 检查 Tmux 窗格是否存在\n    return 'running';\n  }\n}"},{title:"权限同步与团队内存",language:"typescript",code:"// permissionSync.ts: 工作者间权限同步\nclass PermissionSync {\n  private sharedPermissions = new Map<string, boolean>();\n  private listeners: PermissionListener[] = [];\n\n  // 当一个工作者获得权限时，广播给所有工作者\n  grantPermission(\n    workerId: string,\n    permission: string\n  ): void {\n    this.sharedPermissions.set(permission, true);\n    // 通知所有其他工作者\n    for (const listener of this.listeners) {\n      listener.onPermissionGranted(permission);\n    }\n  }\n\n  // 检查权限是否已被任一工作者获得\n  hasPermission(permission: string): boolean {\n    return this.sharedPermissions.get(permission) || false;\n  }\n\n  subscribe(listener: PermissionListener): void {\n    this.listeners.push(listener);\n  }\n}\n\n// teamMemorySync: 团队内存同步服务\nclass TeamMemorySync {\n  private sharedMemory: SharedMemoryEntry[] = [];\n\n  // 工作者共享发现\n  shareDiscovery(\n    workerId: string,\n    discovery: string\n  ): void {\n    // 秘密扫描：检查是否包含敏感信息\n    if (containsSecrets(discovery)) {\n      console.warn(\n        'Secret detected in worker ' + workerId\n        + ' output, redacting...'\n      );\n      discovery = redactSecrets(discovery);\n    }\n\n    this.sharedMemory.push({\n      workerId,\n      content: discovery,\n      timestamp: Date.now()\n    });\n  }\n\n  // 获取所有共享内存\n  getSharedMemories(): SharedMemoryEntry[] {\n    return [...this.sharedMemory];\n  }\n\n  // 检查是否有工作者已经处理过某个文件\n  isFileBeingProcessed(filePath: string): boolean {\n    return this.sharedMemory.some(\n      m => m.content.includes(filePath)\n        && m.content.includes('processing')\n    );\n  }\n}\n\n// teammatePromptAddendum.ts: 队友附加指令\nconst teammatePromptAddendum = {\n  get(): string {\n    return [\n      '你是团队中的一个工作者。',\n      '与其他工作者协作完成任务。',\n      '通过共享内存报告重要发现。',\n      '避免修改其他工作者正在处理的文件。',\n      '完成任务后生成简洁的结果摘要。'\n    ].join('\\n');\n  }\n};"}],interactive:'\n    <h3>多代理协作交互式演练</h3>\n    <div class="interactive-demo">\n      <div class="step" data-step="1">\n        <h4>Step 1: 协调者分析任务</h4>\n        <p>用户请求："重构整个认证模块，更新 API、前端组件和测试"。协调者分析任务后，识别出三个可并行执行的子任务。</p>\n        <div class="code-highlight">\n          <pre>// 任务拆解结果\nsubtasks: [\n  { name: \'api-worker\', task: \'重构认证 API 端点\' },\n  { name: \'frontend-worker\', task: \'更新前端认证组件\' },\n  { name: \'test-worker\', task: \'编写新的测试用例\' }\n]</pre>\n        </div>\n      </div>\n      <div class="step" data-step="2">\n        <h4>Step 2: 创建工作者团队</h4>\n        <p>TeamCreateTool 根据环境选择执行后端。本地开发使用 InProcessBackend，CI 环境使用 TmuxBackend。每个工作者被分配独立的工具集和上下文。</p>\n        <div class="code-highlight">\n          <pre>// InProcessBackend 模式\nWorker A [api-worker]:      工具: Read, Write, Bash, Grep\nWorker B [frontend-worker]: 工具: Read, Write, Glob, LSP\nWorker C [test-worker]:     工具: Read, Write, Bash</pre>\n        </div>\n      </div>\n      <div class="step" data-step="3">\n        <h4>Step 3: 并行执行与同步</h4>\n        <p>三个工作者并行执行各自任务。permissionSync 确保权限共享——当 api-worker 获得写入 src/auth/ 的权限后，其他工作者自动继承。teamMemorySync 共享关键发现，防止冲突。</p>\n        <div class="code-highlight">\n          <pre>// 权限同步\napi-worker 获得权限: Write src/auth/api.ts\n&#x2192; frontend-worker 自动继承 Write 权限\n&#x2192; test-worker 自动继承 Write 权限\n\n// 内存同步\napi-worker 共享: "API 接口签名已变更为..."\n&#x2192; frontend-worker 读取并调整调用方式\n&#x2192; test-worker 读取并更新测试断言</pre>\n        </div>\n      </div>\n      <div class="step" data-step="4">\n        <h4>Step 4: 结果汇总</h4>\n        <p>所有工作者完成后，协调者汇总结果。包括修改的文件列表、测试执行结果和潜在的冲突检测。最终生成完整的重构报告返回给用户。</p>\n        <div class="code-highlight">\n          <pre>// 汇总报告\n{\n  filesModified: 12,\n  testsAdded: 8,\n  testsPassed: \'8/8\',\n  conflicts: 0,\n  summary: \'认证模块重构完成...\'\n}</pre>\n        </div>\n      </div>\n    </div>\n  '},c16:{content:'\n    <h2>16.1 五种任务类型</h2>\n    <p>Claude Code 的 <strong>tasks/</strong> 目录定义了五种核心任务类型，覆盖从主线程到后台处理的全部场景：</p>\n    <ul>\n      <li><strong>LocalMainSessionTask</strong> — 主线程任务，运行在用户当前的 REPL 会话中。这是最常见的任务类型，直接响应用户输入并产生可见输出</li>\n      <li><strong>LocalAgentTask</strong> — 基于代理的任务，拥有独立的上下文窗口和工具集。通常由 AgentTool 创建，用于处理需要隔离执行环境的子任务</li>\n      <li><strong>LocalShellTask</strong> — 后台 Shell 任务，在独立的终端进程中执行长时间运行的命令（如构建、测试），不阻塞主会话</li>\n      <li><strong>InProcessTeammateTask</strong> — 进程内队友任务，与多代理 Swarm 系统配合使用。每个队友作为独立的异步任务在同一进程中运行</li>\n      <li><strong>DreamTask</strong> — 后台"梦境"任务，用于 AI 异步知识整合。当用户空闲时，系统会启动 DreamTask 对对话历史进行反思和总结</li>\n    </ul>\n\n    <h2>16.2 任务管理工具</h2>\n    <p>Claude Code 提供了一套完整的任务管理工具，通过 <strong>ScheduleCronTool/</strong> 和任务 CRUD 工具实现任务的全生命周期管理：</p>\n    <ul>\n      <li><strong>TaskCreateTool</strong> — 创建新任务，接受 subject（简短标题）和 description（详细描述）参数。可选 activeForm 字段用于在进度条中显示</li>\n      <li><strong>TaskUpdateTool</strong> — 更新任务状态和依赖关系。支持修改 status、subject、description、owner，以及通过 addBlocks/addBlockedBy 设置依赖</li>\n      <li><strong>TaskGetTool</strong> — 按 ID 获取任务详情，包括完整描述、状态、依赖关系</li>\n      <li><strong>TaskListTool</strong> — 列出所有任务的摘要视图，包含 id、subject、status、owner、blockedBy</li>\n      <li><strong>TaskStopTool</strong> — 停止正在运行的任务，释放其占用的资源</li>\n    </ul>\n\n    <h2>16.3 任务依赖与状态工作流</h2>\n    <p>任务之间可以通过 <strong>blocks</strong> 和 <strong>blockedBy</strong> 建立依赖关系。被阻塞的任务在其依赖完成之前无法开始执行。</p>\n    <p>状态工作流遵循严格的转换规则：<code>pending → in_progress → completed</code>。任何阶段都可以转为 <code>deleted</code> 状态。只有当任务确实完成所有工作（测试通过、实现完整）时才应标记为 completed。</p>\n\n    <h2>16.4 Cron 调度系统</h2>\n    <p><strong>ScheduleCronTool/</strong> 提供基于 Cron 表达式的定时调度：</p>\n    <ul>\n      <li><strong>CronCreateTool</strong> — 创建定时任务，使用标准 5 字段 Cron 表达式（分 时 日 月 周），在用户本地时区执行</li>\n      <li><strong>CronDeleteTool</strong> — 根据 Job ID 取消已创建的定时任务</li>\n      <li><strong>CronListTool</strong> — 列出所有活跃的定时任务</li>\n    </ul>\n    <p>Cron 任务支持两种模式：<strong>recurring</strong>（循环执行，7 天后自动过期）和 <strong>one-shot</strong>（单次执行后自动删除）。持久性方面分为 <strong>durable</strong>（写入 .claude/scheduled_tasks.json，跨会话保持）和 <strong>session-only</strong>（仅在当前会话存活）。</p>\n\n    <h2>16.5 DreamTask 与自动梦境</h2>\n    <p><strong>services/autoDream/</strong> 实现了 AI "做梦"机制：</p>\n    <ul>\n      <li><strong>consolidationPrompt</strong> — 梦境整合提示词，指导 AI 对对话历史进行反思、提取关键知识点和决策模式</li>\n      <li><strong>consolidationLock</strong> — 梦境锁，防止多个梦境任务同时执行造成资源竞争</li>\n    </ul>\n    <p>当用户空闲且会话达到一定长度时，系统自动启动 DreamTask。它会在后台分析对话上下文，生成知识摘要，为后续交互提供更精准的上下文支持。</p>\n  ',architecture:'\n    <div class="arch-diagram">\n      <h3>任务与调度系统架构图</h3>\n      <div class="arch-flow">\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px; margin-bottom: 16px;">\n          <h4 style="color: #e94560;">任务管理工具层</h4>\n          <div style="display: flex; justify-content: center; gap: 10px; flex-wrap: wrap;">\n            <div style="background: #16213e; padding: 10px 14px; border-radius: 8px; border: 1px solid #e94560; font-size: 13px;">TaskCreateTool</div>\n            <div style="background: #16213e; padding: 10px 14px; border-radius: 8px; border: 1px solid #e94560; font-size: 13px;">TaskUpdateTool</div>\n            <div style="background: #16213e; padding: 10px 14px; border-radius: 8px; border: 1px solid #e94560; font-size: 13px;">TaskGetTool</div>\n            <div style="background: #16213e; padding: 10px 14px; border-radius: 8px; border: 1px solid #e94560; font-size: 13px;">TaskListTool</div>\n            <div style="background: #16213e; padding: 10px 14px; border-radius: 8px; border: 1px solid #e94560; font-size: 13px;">TaskStopTool</div>\n          </div>\n        </div>\n        <div style="text-align: center; font-size: 24px; color: #e94560;">&#x2193;</div>\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px; margin-bottom: 16px;">\n          <h4 style="color: #0f3460;">五种任务类型 (tasks/)</h4>\n          <div style="display: grid; grid-template-columns: repeat(5, 1fr); gap: 8px;">\n            <div style="background: #16213e; padding: 10px; border-radius: 8px; text-align: center; border: 1px solid #0f3460;">\n              <div style="font-weight: bold; font-size: 11px;">LocalMainSession</div>\n              <div style="font-size: 10px; color: #aaa;">主线程</div>\n            </div>\n            <div style="background: #16213e; padding: 10px; border-radius: 8px; text-align: center; border: 1px solid #0f3460;">\n              <div style="font-weight: bold; font-size: 11px;">LocalAgent</div>\n              <div style="font-size: 10px; color: #aaa;">代理隔离</div>\n            </div>\n            <div style="background: #16213e; padding: 10px; border-radius: 8px; text-align: center; border: 1px solid #0f3460;">\n              <div style="font-weight: bold; font-size: 11px;">LocalShell</div>\n              <div style="font-size: 10px; color: #aaa;">后台命令</div>\n            </div>\n            <div style="background: #16213e; padding: 10px; border-radius: 8px; text-align: center; border: 1px solid #0f3460;">\n              <div style="font-weight: bold; font-size: 11px;">InProcessTeammate</div>\n              <div style="font-size: 10px; color: #aaa;">进程内队友</div>\n            </div>\n            <div style="background: #16213e; padding: 10px; border-radius: 8px; text-align: center; border: 1px solid #533483;">\n              <div style="font-weight: bold; font-size: 11px;">DreamTask</div>\n              <div style="font-size: 10px; color: #aaa;">异步梦境</div>\n            </div>\n          </div>\n        </div>\n        <div style="text-align: center; font-size: 24px; color: #0f3460;">&#x2193;</div>\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px; margin-bottom: 16px;">\n          <h4 style="color: #48c9b0;">状态与依赖</h4>\n          <div style="display: flex; justify-content: center; gap: 8px; align-items: center; margin-bottom: 12px;">\n            <div style="background: #2d4a3e; padding: 8px 16px; border-radius: 20px; border: 1px solid #48c9b0; font-size: 13px;">pending</div>\n            <div style="color: #48c9b0; font-size: 18px;">&#x2192;</div>\n            <div style="background: #2d4a3e; padding: 8px 16px; border-radius: 20px; border: 1px solid #f0c040; font-size: 13px;">in_progress</div>\n            <div style="color: #f0c040; font-size: 18px;">&#x2192;</div>\n            <div style="background: #2d4a3e; padding: 8px 16px; border-radius: 20px; border: 1px solid #4ade80; font-size: 13px;">completed</div>\n          </div>\n          <div style="text-align: center; font-size: 12px; color: #aaa;">blocks / blockedBy 依赖关系控制执行顺序</div>\n        </div>\n        <div style="text-align: center; font-size: 24px; color: #533483;">&#x2193;</div>\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px;">\n          <h4 style="color: #533483;">Cron 调度 &amp; AutoDream</h4>\n          <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 12px;">\n            <div style="background: #2d1b4e; padding: 12px; border-radius: 8px; border: 1px solid #533483;">\n              <div style="font-weight: bold; font-size: 13px;">ScheduleCronTool</div>\n              <div style="font-size: 11px; color: #aaa;">Create / Delete / List</div>\n              <div style="font-size: 11px; color: #aaa;">recurring | one-shot</div>\n              <div style="font-size: 11px; color: #aaa;">durable | session-only</div>\n            </div>\n            <div style="background: #2d1b4e; padding: 12px; border-radius: 8px; border: 1px solid #533483;">\n              <div style="font-weight: bold; font-size: 13px;">services/autoDream</div>\n              <div style="font-size: 11px; color: #aaa;">consolidationPrompt</div>\n              <div style="font-size: 11px; color: #aaa;">consolidationLock</div>\n              <div style="font-size: 11px; color: #aaa;">后台知识整合</div>\n            </div>\n          </div>\n        </div>\n      </div>\n    </div>\n  ',codeExamples:[{title:"任务创建与依赖管理",language:"typescript",code:'// TaskCreateTool: 创建任务并设置依赖\ninterface TaskCreateInput {\n  subject: string;       // 简短标题（祈使句式）\n  description: string;   // 详细描述\n  activeForm?: string;   // 进行中时的显示文本\n  metadata?: Record<string, unknown>;\n}\n\ninterface Task {\n  id: string;\n  subject: string;\n  description: string;\n  status: "pending" | "in_progress" | "completed" | "deleted";\n  owner?: string;\n  blocks: string[];      // 此任务完成后才能开始的任务\n  blockedBy: string[];   // 此任务依赖的前置任务\n  activeForm?: string;\n}\n\n// 创建一组有依赖关系的任务\nasync function createTaskPipeline(\n  store: TaskStore\n): Promise<void> {\n  // 任务 1: 基础设施\n  const t1 = store.create({\n    subject: "搭建数据库 Schema",\n    description: "创建用户表、权限表和会话表",\n    activeForm: "搭建数据库 Schema 中"\n  });\n\n  // 任务 2: 依赖任务 1\n  const t2 = store.create({\n    subject: "实现 API 端点",\n    description: "基于 Schema 实现 CRUD API"\n  });\n  store.update(t2.id, {\n    addBlockedBy: [t1.id]\n  });\n\n  // 任务 3: 依赖任务 2\n  const t3 = store.create({\n    subject: "编写集成测试",\n    description: "测试所有 API 端点"\n  });\n  store.update(t3.id, {\n    addBlockedBy: [t2.id]\n  });\n\n  // 开始执行任务 1\n  store.update(t1.id, {\n    status: "in_progress"\n  });\n}'},{title:"Cron 调度系统",language:"typescript",code:'// CronCreateTool: 标准 5 字段 Cron 调度\ninterface CronCreateInput {\n  cron: string;          // "M H DoM Mon DoW"\n  prompt: string;        // 触发时执行的提示词\n  recurring?: boolean;   // 默认 true，false 为单次\n  durable?: boolean;     // 默认 false，true 持久化到磁盘\n}\n\ninterface CronJob {\n  id: string;\n  cron: string;\n  prompt: string;\n  recurring: boolean;\n  durable: boolean;\n  nextFireTime: Date;\n  createdAt: Date;\n  expiresAt: Date;       // 循环任务 7 天后过期\n}\n\n// 调度器核心逻辑\nclass CronScheduler {\n  private jobs: Map<string, CronJob> = new Map();\n  private readonly STORAGE_PATH =\n    ".claude/scheduled_tasks.json";\n\n  create(input: CronCreateInput): CronJob {\n    const job: CronJob = {\n      id: generateId(),\n      cron: input.cron,\n      prompt: input.prompt,\n      recurring: input.recurring !== false,\n      durable: input.durable || false,\n      nextFireTime: computeNextFire(input.cron),\n      createdAt: new Date(),\n      expiresAt: new Date(\n        Date.now() + 7 * 24 * 60 * 60 * 1000\n      )\n    };\n\n    this.jobs.set(job.id, job);\n\n    // 持久化到磁盘\n    if (job.durable) {\n      this.persistToDisk();\n    }\n    return job;\n  }\n\n  // REPL 空闲时检查并触发到期任务\n  async tick(): Promise<void> {\n    const now = new Date();\n    for (const [id, job] of this.jobs) {\n      // 过期检查\n      if (now > job.expiresAt) {\n        this.jobs.delete(id);\n        continue;\n      }\n      // 触发检查\n      if (now >= job.nextFireTime) {\n        await this.fire(job);\n        if (job.recurring) {\n          job.nextFireTime = computeNextFire(job.cron);\n        } else {\n          this.jobs.delete(id);\n        }\n      }\n    }\n  }\n\n  private async fire(job: CronJob): Promise<void> {\n    // 将 prompt 加入 REPL 消息队列\n    enqueuePrompt(job.prompt);\n  }\n\n  private persistToDisk(): void {\n    const durableJobs = [...this.jobs.values()]\n      .filter(j => j.durable);\n    writeFileSync(\n      this.STORAGE_PATH,\n      JSON.stringify(durableJobs, null, 2)\n    );\n  }\n}'},{title:"DreamTask 与自动梦境整合",language:"typescript",code:'// services/autoDream: AI 梦境知识整合\nconst consolidationPrompt = [\n  "回顾本次对话的完整历史。",\n  "提取关键技术决策和发现。",\n  "识别用户的编码偏好和风格。",\n  "总结项目结构和架构特征。",\n  "生成简洁的知识摘要用于后续参考。"\n].join("\\n");\n\n// 梦境锁：防止并发执行\nclass ConsolidationLock {\n  private locked = false;\n  private lockHolder: string | null = null;\n\n  acquire(taskId: string): boolean {\n    if (this.locked) return false;\n    this.locked = true;\n    this.lockHolder = taskId;\n    return true;\n  }\n\n  release(taskId: string): void {\n    if (this.lockHolder === taskId) {\n      this.locked = false;\n      this.lockHolder = null;\n    }\n  }\n}\n\n// DreamTask: 后台知识整合任务\nclass DreamTask {\n  private lock = new ConsolidationLock();\n\n  async execute(\n    conversationHistory: Message[]\n  ): Promise<DreamResult | null> {\n    const taskId = "dream-" + Date.now();\n\n    // 尝试获取锁\n    if (!this.lock.acquire(taskId)) {\n      return null; // 已有梦境任务在执行\n    }\n\n    try {\n      // 构建整合上下文\n      const context = conversationHistory\n        .filter(m => m.role !== "system")\n        .map(m => m.role + ": " + summarize(m.content))\n        .join("\\n");\n\n      // 执行 AI 反思\n      const result = await callModel({\n        system: consolidationPrompt,\n        messages: [\n          { role: "user", content: context }\n        ]\n      });\n\n      // 保存知识摘要\n      return {\n        summary: result.content,\n        timestamp: Date.now(),\n        messageCount: conversationHistory.length\n      };\n    } finally {\n      this.lock.release(taskId);\n    }\n  }\n}\n\n// 自动触发条件\nfunction shouldStartDream(\n  state: AppState\n): boolean {\n  const idleTime = Date.now() - state.lastUserInput;\n  const messageCount = state.conversationLength;\n  // 空闲 2 分钟且对话超过 20 条消息\n  return idleTime > 120_000 && messageCount > 20;\n}'}],interactive:'\n    <h3>任务调度交互式演练</h3>\n    <div class="interactive-demo">\n      <div class="step" data-step="1">\n        <h4>Step 1: 创建任务流水线</h4>\n        <p>用户要求实现一个完整功能。Claude Code 自动创建三个有序任务：数据库 Schema → API 实现 → 集成测试。通过 blockedBy 建立依赖，确保执行顺序。</p>\n        <div class="code-highlight">\n          <pre>Task #1 [pending] "搭建数据库 Schema"\nTask #2 [pending] "实现 API 端点" (blockedBy: #1)\nTask #3 [pending] "编写集成测试" (blockedBy: #2)</pre>\n        </div>\n      </div>\n      <div class="step" data-step="2">\n        <h4>Step 2: 逐步执行与状态流转</h4>\n        <p>Task #1 开始执行，状态变为 in_progress。完成后标记为 completed，Task #2 自动解除阻塞变为可执行状态。</p>\n        <div class="code-highlight">\n          <pre>Task #1 [completed] &#x2714;\nTask #2 [in_progress] &#x2192; 阻塞解除，开始执行\nTask #3 [pending] (blockedBy: #2) 等待中...</pre>\n        </div>\n      </div>\n      <div class="step" data-step="3">\n        <h4>Step 3: 设置 Cron 定时任务</h4>\n        <p>用户请求"每天早上 9 点检查部署状态"。CronCreateTool 创建一个 durable 循环任务，写入磁盘持久化，跨会话保持运行。</p>\n        <div class="code-highlight">\n          <pre>CronJob {\n  id: "cj_abc123",\n  cron: "3 9 * * *",      // 每天 9:03（避开整点）\n  prompt: "检查所有服务的部署状态并汇报",\n  recurring: true,\n  durable: true,           // 持久化到磁盘\n  expiresAt: "7天后"       // 自动过期\n}</pre>\n        </div>\n      </div>\n      <div class="step" data-step="4">\n        <h4>Step 4: 后台梦境整合</h4>\n        <p>用户空闲 2 分钟后，DreamTask 自动启动。在后台分析对话历史，提取知识要点，生成知识摘要。下次用户提问时，这些摘要提供更精准的上下文。</p>\n        <div class="code-highlight">\n          <pre>DreamTask 启动:\n  - 分析 35 条对话消息\n  - 提取 8 个关键技术决策\n  - 识别用户偏好: TypeScript, React, 函数式风格\n  - 生成知识摘要 (512 tokens)\n  - 写入 CLAUDE.md 记忆文件</pre>\n        </div>\n      </div>\n    </div>\n  '},c17:{content:'\n    <h2>17.1 Skills 系统概览</h2>\n    <p>Claude Code 的 <strong>skills/</strong> 系统是一种可扩展的能力注入机制。Skills 本质上是结构化的 Markdown 文件，通过 Frontmatter 声明元数据和钩子，正文部分作为提示词注入到子对话中。</p>\n    <ul>\n      <li><strong>loadSkillsDir.ts</strong> — 从 <code>.claude/skills/</code> 目录加载用户自定义 Skills，支持嵌套目录和热更新</li>\n      <li><strong>bundledSkills.ts</strong> — 内置 Skills 注册表，包含 17+ 个预打包的 Skills</li>\n      <li><strong>SkillTool</strong> — Skills 执行工具，将 Skill 的提示词内容注入到子对话中执行，通过 skill 名称或完全限定名称（如 "plugin:skill-name"）调用</li>\n      <li><strong>mcpSkillBuilders.ts</strong> — 将 MCP 服务器提供的 Prompts 转换为可调用的 Skills</li>\n    </ul>\n\n    <h2>17.2 内置 Skills 清单</h2>\n    <p>Claude Code 预置了 <strong>17+ 个内置 Skills</strong>，涵盖开发工作流的各个方面：</p>\n    <ul>\n      <li><strong>batch</strong> — 批量处理多个文件或任务</li>\n      <li><strong>claudeApi</strong> — Claude API 和 Anthropic SDK 集成指导</li>\n      <li><strong>claudeInChrome</strong> — Chrome 浏览器中使用 Claude 的专用模式</li>\n      <li><strong>debug</strong> — 调试辅助，提供系统状态诊断和日志分析</li>\n      <li><strong>keybindings</strong> — 快捷键配置和自定义</li>\n      <li><strong>loop</strong> — 循环执行：以固定间隔重复运行提示词或命令</li>\n      <li><strong>remember</strong> — 记忆管理：读写 CLAUDE.md 记忆文件</li>\n      <li><strong>scheduleRemoteAgents</strong> — 远程代理调度和管理</li>\n      <li><strong>simplify</strong> — 代码简化：检查变更代码的复用性、质量和效率</li>\n      <li><strong>skillify / skill-creator</strong> — Skill 创建工具：从模板创建新的自定义 Skill</li>\n      <li><strong>stuck</strong> — 卡住检测：当 AI 陷入循环时自动介入</li>\n      <li><strong>updateConfig</strong> — 通过 settings.json 配置 Claude Code 行为</li>\n      <li><strong>verify / verifyContent</strong> — 验证工具：确认代码变更的正确性和完整性</li>\n    </ul>\n\n    <h2>17.3 Frontmatter 钩子注册</h2>\n    <p>Skills 可以在 Markdown Frontmatter 中声明 <strong>PreToolUse</strong> 和 <strong>PostToolUse</strong> 钩子，在工具调用前后自动触发：</p>\n    <ul>\n      <li>Frontmatter 使用 YAML 格式声明钩子匹配规则</li>\n      <li>支持按工具名称、文件模式等条件过滤</li>\n      <li>钩子可以修改工具参数、拦截执行或追加后处理逻辑</li>\n    </ul>\n\n    <h2>17.4 Plugin 系统</h2>\n    <p><strong>utils/plugins/</strong> 目录包含 <strong>43+ 个文件</strong>，构成了完整的插件管理系统：</p>\n    <ul>\n      <li><strong>pluginLoader.ts</strong> — 插件加载器，负责发现、加载和缓存插件</li>\n      <li><strong>installedPluginsManager.ts</strong> — 已安装插件管理器，处理版本化的插件生命周期</li>\n      <li><strong>managedPlugins.ts</strong> — 企业级托管插件，由组织管理员统一配置</li>\n      <li><strong>Plugin Marketplace</strong> — 插件市场集成，支持浏览、安装和自动更新</li>\n      <li><strong>blocklist</strong> — 插件黑名单机制，阻止已知有问题的插件版本</li>\n    </ul>\n\n    <h2>17.5 Slash 命令系统</h2>\n    <p><strong>commands/</strong> 目录定义了 <strong>83+ 个 Slash 命令</strong>，按功能分类：</p>\n    <ul>\n      <li><strong>Git 操作</strong> — /commit, /pr, /review-pr 等版本控制快捷命令</li>\n      <li><strong>会话管理</strong> — /clear, /compact, /resume 等会话控制命令</li>\n      <li><strong>调试工具</strong> — /debug, /doctor, /bug-report 等诊断命令</li>\n      <li><strong>配置管理</strong> — /config, /theme, /model 等设置命令</li>\n      <li><strong>项目工具</strong> — /init, /search, /install 等项目操作命令</li>\n    </ul>\n    <p>每个 Slash 命令可以直接执行逻辑，也可以触发对应的 Skill。当用户输入 "/skill-name" 时，系统会查找匹配的 Skill 并通过 SkillTool 执行。</p>\n  ',architecture:'\n    <div class="arch-diagram">\n      <h3>Skills / Plugins / Commands 架构图</h3>\n      <div class="arch-flow">\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px; margin-bottom: 16px;">\n          <h4 style="color: #e94560;">用户入口层</h4>\n          <div style="display: flex; justify-content: center; gap: 12px;">\n            <div style="background: #16213e; padding: 12px 18px; border-radius: 8px; border: 2px solid #e94560;">\n              <div style="font-weight: bold;">Slash 命令 /xxx</div>\n              <div style="font-size: 11px; color: #aaa;">83+ 个命令</div>\n            </div>\n            <div style="background: #16213e; padding: 12px 18px; border-radius: 8px; border: 2px solid #e94560;">\n              <div style="font-weight: bold;">SkillTool 调用</div>\n              <div style="font-size: 11px; color: #aaa;">AI 自动匹配</div>\n            </div>\n          </div>\n        </div>\n        <div style="text-align: center; font-size: 24px; color: #e94560;">&#x2193;</div>\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px; margin-bottom: 16px;">\n          <h4 style="color: #0f3460;">Skills 引擎</h4>\n          <div style="display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 10px;">\n            <div style="background: #16213e; padding: 12px; border-radius: 8px; border: 1px solid #0f3460;">\n              <div style="font-weight: bold; font-size: 13px;">bundledSkills</div>\n              <div style="font-size: 11px; color: #aaa;">17+ 内置 Skills</div>\n            </div>\n            <div style="background: #16213e; padding: 12px; border-radius: 8px; border: 1px solid #0f3460;">\n              <div style="font-weight: bold; font-size: 13px;">loadSkillsDir</div>\n              <div style="font-size: 11px; color: #aaa;">.claude/skills/ 自定义</div>\n            </div>\n            <div style="background: #16213e; padding: 12px; border-radius: 8px; border: 1px solid #0f3460;">\n              <div style="font-weight: bold; font-size: 13px;">mcpSkillBuilders</div>\n              <div style="font-size: 11px; color: #aaa;">MCP Prompts 转 Skill</div>\n            </div>\n          </div>\n          <div style="margin-top: 10px; background: #0d1b2a; padding: 10px; border-radius: 8px; text-align: center; border: 1px dashed #0f3460;">\n            <div style="font-size: 12px; color: #8ab4f8;">Frontmatter 钩子: PreToolUse / PostToolUse</div>\n          </div>\n        </div>\n        <div style="text-align: center; font-size: 24px; color: #0f3460;">&#x2193;</div>\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px; margin-bottom: 16px;">\n          <h4 style="color: #533483;">Plugins 生态</h4>\n          <div style="display: grid; grid-template-columns: repeat(4, 1fr); gap: 8px;">\n            <div style="background: #2d1b4e; padding: 10px; border-radius: 8px; text-align: center; border: 1px solid #533483; font-size: 12px;">\n              <div style="font-weight: bold;">pluginLoader</div>\n              <div style="font-size: 10px; color: #aaa;">加载 &amp; 缓存</div>\n            </div>\n            <div style="background: #2d1b4e; padding: 10px; border-radius: 8px; text-align: center; border: 1px solid #533483; font-size: 12px;">\n              <div style="font-weight: bold;">installedMgr</div>\n              <div style="font-size: 10px; color: #aaa;">版本管理</div>\n            </div>\n            <div style="background: #2d1b4e; padding: 10px; border-radius: 8px; text-align: center; border: 1px solid #533483; font-size: 12px;">\n              <div style="font-weight: bold;">managedPlugins</div>\n              <div style="font-size: 10px; color: #aaa;">企业托管</div>\n            </div>\n            <div style="background: #2d1b4e; padding: 10px; border-radius: 8px; text-align: center; border: 1px solid #533483; font-size: 12px;">\n              <div style="font-weight: bold;">marketplace</div>\n              <div style="font-size: 10px; color: #aaa;">市场 &amp; 更新</div>\n            </div>\n          </div>\n        </div>\n        <div style="text-align: center; font-size: 24px; color: #533483;">&#x2193;</div>\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px;">\n          <h4 style="color: #48c9b0;">commands/ — Slash 命令注册表</h4>\n          <div style="display: flex; justify-content: center; gap: 8px; flex-wrap: wrap;">\n            <div style="background: #16213e; padding: 6px 12px; border-radius: 16px; border: 1px solid #48c9b0; font-size: 12px;">/commit</div>\n            <div style="background: #16213e; padding: 6px 12px; border-radius: 16px; border: 1px solid #48c9b0; font-size: 12px;">/pr</div>\n            <div style="background: #16213e; padding: 6px 12px; border-radius: 16px; border: 1px solid #48c9b0; font-size: 12px;">/review-pr</div>\n            <div style="background: #16213e; padding: 6px 12px; border-radius: 16px; border: 1px solid #48c9b0; font-size: 12px;">/clear</div>\n            <div style="background: #16213e; padding: 6px 12px; border-radius: 16px; border: 1px solid #48c9b0; font-size: 12px;">/compact</div>\n            <div style="background: #16213e; padding: 6px 12px; border-radius: 16px; border: 1px solid #48c9b0; font-size: 12px;">/debug</div>\n            <div style="background: #16213e; padding: 6px 12px; border-radius: 16px; border: 1px solid #48c9b0; font-size: 12px;">/doctor</div>\n            <div style="background: #16213e; padding: 6px 12px; border-radius: 16px; border: 1px solid #48c9b0; font-size: 12px;">/config</div>\n            <div style="background: #16213e; padding: 6px 12px; border-radius: 16px; border: 1px solid #48c9b0; font-size: 12px;">...80+</div>\n          </div>\n        </div>\n      </div>\n    </div>\n  ',codeExamples:[{title:"Skill 加载与 Frontmatter 解析",language:"typescript",code:'// Skill 文件结构 (.claude/skills/my-skill.md)\n// ---\n// name: my-custom-skill\n// description: 自定义代码审查 Skill\n// hooks:\n//   PreToolUse:\n//     - tool: Write\n//       match: "**/*.ts"\n//   PostToolUse:\n//     - tool: Bash\n// ---\n// 你是一个代码审查专家。在写入 TypeScript 文件前...\n\ninterface SkillDefinition {\n  name: string;\n  description: string;\n  content: string;       // Markdown 正文 = 提示词\n  hooks?: {\n    PreToolUse?: HookConfig[];\n    PostToolUse?: HookConfig[];\n  };\n  source: "bundled" | "user" | "mcp" | "plugin";\n}\n\ninterface HookConfig {\n  tool: string;         // 匹配的工具名\n  match?: string;       // 文件模式（glob）\n}\n\n// loadSkillsDir.ts: 从磁盘加载用户 Skills\nasync function loadSkillsDir(\n  skillsPath: string\n): Promise<SkillDefinition[]> {\n  const files = await glob(skillsPath + "/**/*.md");\n  const skills: SkillDefinition[] = [];\n\n  for (const file of files) {\n    const raw = await readFile(file, "utf-8");\n    const { frontmatter, body } = parseFrontmatter(raw);\n\n    skills.push({\n      name: frontmatter.name || basename(file, ".md"),\n      description: frontmatter.description || "",\n      content: body,\n      hooks: frontmatter.hooks,\n      source: "user"\n    });\n  }\n\n  return skills;\n}\n\n// bundledSkills.ts: 内置 Skills 注册表\nconst bundledSkills: SkillDefinition[] = [\n  {\n    name: "simplify",\n    description: "审查变更代码的质量和效率",\n    content: "检查代码复用性、质量和效率...",\n    source: "bundled"\n  },\n  {\n    name: "loop",\n    description: "以固定间隔循环执行命令",\n    content: "按照指定的时间间隔重复运行...",\n    source: "bundled"\n  }\n  // ... 15+ 更多内置 Skills\n];'},{title:"SkillTool 执行与 Plugin 加载",language:"typescript",code:'// SkillTool: 执行 Skill 的核心工具\nclass SkillTool {\n  private registry: Map<string, SkillDefinition>;\n\n  async execute(input: {\n    skill: string;\n    args?: string;\n  }): Promise<SkillResult> {\n    // 按名称或完全限定名查找 Skill\n    const skill = this.findSkill(input.skill);\n    if (!skill) {\n      throw new Error(\n        "Skill not found: " + input.skill\n      );\n    }\n\n    // 将 Skill 内容注入子对话\n    const subConversation = createSubConversation({\n      systemPrompt: skill.content,\n      userMessage: input.args || "",\n      tools: getAvailableTools()\n    });\n\n    // 执行子对话\n    return await runSubConversation(subConversation);\n  }\n\n  private findSkill(name: string): SkillDefinition | undefined {\n    // 完全限定名: "plugin:skill-name"\n    if (name.includes(":")) {\n      return this.registry.get(name);\n    }\n    // 短名称匹配\n    return this.registry.get(name)\n      || [...this.registry.values()].find(\n        s => s.name === name\n      );\n  }\n}\n\n// pluginLoader.ts: 插件加载与缓存\nclass PluginLoader {\n  private cache = new Map<string, Plugin>();\n\n  async loadPlugin(\n    pluginId: string\n  ): Promise<Plugin> {\n    // 检查缓存\n    if (this.cache.has(pluginId)) {\n      return this.cache.get(pluginId)!;\n    }\n\n    // 从 marketplace 或本地加载\n    const manifest = await this.fetchManifest(pluginId);\n\n    // 黑名单检查\n    if (isBlocklisted(pluginId, manifest.version)) {\n      throw new Error(\n        "Plugin " + pluginId + " is blocklisted"\n      );\n    }\n\n    const plugin = await this.instantiate(manifest);\n    this.cache.set(pluginId, plugin);\n    return plugin;\n  }\n}\n\n// installedPluginsManager.ts: 版本化管理\nclass InstalledPluginsManager {\n  private plugins: Map<string, InstalledPlugin>;\n\n  async install(\n    pluginId: string,\n    version: string\n  ): Promise<void> {\n    const plugin = await PluginLoader.loadPlugin(\n      pluginId\n    );\n    this.plugins.set(pluginId, {\n      ...plugin,\n      installedVersion: version,\n      installedAt: new Date()\n    });\n  }\n\n  async checkUpdates(): Promise<UpdateInfo[]> {\n    const updates: UpdateInfo[] = [];\n    for (const [id, plugin] of this.plugins) {\n      const latest = await fetchLatestVersion(id);\n      if (latest !== plugin.installedVersion) {\n        updates.push({\n          pluginId: id,\n          current: plugin.installedVersion,\n          latest\n        });\n      }\n    }\n    return updates;\n  }\n}'},{title:"Slash 命令注册与路由",language:"typescript",code:'// commands/ 目录: Slash 命令定义\ninterface SlashCommand {\n  name: string;           // 命令名（不含 /）\n  description: string;\n  aliases?: string[];     // 别名\n  category: CommandCategory;\n  execute: (\n    args: string,\n    context: CommandContext\n  ) => Promise<void>;\n}\n\ntype CommandCategory =\n  | "git"\n  | "session"\n  | "debug"\n  | "config"\n  | "project";\n\n// 示例: /commit 命令\nconst commitCommand: SlashCommand = {\n  name: "commit",\n  description: "分析变更并创建 Git 提交",\n  category: "git",\n  execute: async (args, ctx) => {\n    // 触发 commit Skill\n    await ctx.skillTool.execute({\n      skill: "commit",\n      args: args\n    });\n  }\n};\n\n// 命令路由器\nclass CommandRouter {\n  private commands = new Map<string, SlashCommand>();\n\n  register(cmd: SlashCommand): void {\n    this.commands.set(cmd.name, cmd);\n    // 注册别名\n    if (cmd.aliases) {\n      for (const alias of cmd.aliases) {\n        this.commands.set(alias, cmd);\n      }\n    }\n  }\n\n  async route(\n    input: string,\n    ctx: CommandContext\n  ): Promise<boolean> {\n    if (!input.startsWith("/")) return false;\n\n    const parts = input.slice(1).split(" ");\n    const cmdName = parts[0];\n    const args = parts.slice(1).join(" ");\n\n    const cmd = this.commands.get(cmdName);\n    if (!cmd) {\n      // 尝试模糊匹配\n      const fuzzy = this.fuzzyMatch(cmdName);\n      if (fuzzy) {\n        await fuzzy.execute(args, ctx);\n        return true;\n      }\n      return false;\n    }\n\n    await cmd.execute(args, ctx);\n    return true;\n  }\n\n  private fuzzyMatch(\n    name: string\n  ): SlashCommand | undefined {\n    const candidates = [...this.commands.entries()]\n      .filter(([k]) => k.startsWith(name));\n    return candidates.length === 1\n      ? candidates[0][1]\n      : undefined;\n  }\n\n  listByCategory(\n    cat: CommandCategory\n  ): SlashCommand[] {\n    return [...this.commands.values()]\n      .filter(c => c.category === cat);\n  }\n}'}],interactive:'\n    <h3>Skills 与 Plugins 交互式演练</h3>\n    <div class="interactive-demo">\n      <div class="step" data-step="1">\n        <h4>Step 1: 用户输入 Slash 命令</h4>\n        <p>用户在 REPL 中输入 <code>/simplify</code>。CommandRouter 识别这是一个 Slash 命令，查找匹配的内置 Skill。</p>\n        <div class="code-highlight">\n          <pre>用户输入: /simplify\nCommandRouter.route("/simplify")\n  &#x2192; 查找命令: "simplify"\n  &#x2192; 匹配到 bundled Skill: "simplify"\n  &#x2192; 触发 SkillTool.execute({ skill: "simplify" })</pre>\n        </div>\n      </div>\n      <div class="step" data-step="2">\n        <h4>Step 2: Skill 内容注入</h4>\n        <p>SkillTool 读取 simplify Skill 的 Markdown 内容，将其作为系统提示词注入子对话。子对话拥有完整的工具访问能力。</p>\n        <div class="code-highlight">\n          <pre>子对话创建:\n  systemPrompt: "检查变更代码的复用性、质量和效率。\n    找到问题后立即修复..."\n  tools: [Read, Write, Grep, Glob, Bash, LSP]\n  context: 当前 git diff 中的变更文件</pre>\n        </div>\n      </div>\n      <div class="step" data-step="3">\n        <h4>Step 3: Frontmatter 钩子触发</h4>\n        <p>如果 Skill 声明了 PreToolUse 钩子（如匹配 Write 工具 + *.ts 文件），则在每次工具调用前自动执行钩子逻辑。</p>\n        <div class="code-highlight">\n          <pre>Skill 钩子触发:\n  PreToolUse: Write "src/auth/login.ts"\n    &#x2192; 匹配 hooks.PreToolUse[0]: tool=Write, match="**/*.ts"\n    &#x2192; 执行前置检查: 代码风格验证\n    &#x2192; 通过 &#x2714; 继续执行 Write 工具</pre>\n        </div>\n      </div>\n      <div class="step" data-step="4">\n        <h4>Step 4: Plugin 自动更新</h4>\n        <p>InstalledPluginsManager 在启动时检查已安装插件的更新。发现新版本后提示用户，同时检查黑名单确保安全。</p>\n        <div class="code-highlight">\n          <pre>Plugin 更新检查:\n  my-linter-plugin: v1.2.0 &#x2192; v1.3.0 (可更新)\n  code-formatter:   v2.0.1 &#x2192; v2.0.1 (最新)\n  unsafe-plugin:    v0.9.0 &#x2192; BLOCKLISTED (已拦截)\n\n自动更新: my-linter-plugin &#x2192; v1.3.0 &#x2714;</pre>\n        </div>\n      </div>\n    </div>\n  '},c18:{content:"\n    <h2>18.1 AppState 全局状态类型</h2>\n    <p>Claude Code 的状态管理核心是 <strong>state/</strong> 目录中定义的 <strong>AppState</strong> 类型。这是一个包含 <strong>300+ 个字段</strong>的巨型状态对象，按功能域组织为多个区块：</p>\n    <ul>\n      <li><strong>Core</strong> — 核心状态：conversationId, messages, model, currentTool, isLoading, error 等基础运行状态</li>\n      <li><strong>Permissions</strong> — 权限状态：grantedPermissions, deniedPermissions, permissionMode, autoApprovePatterns</li>\n      <li><strong>MCP</strong> — MCP 连接状态：mcpClients, mcpTools, mcpResources, mcpConnectionStatus</li>\n      <li><strong>Plugins</strong> — 插件状态：installedPlugins, pluginConfigs, pluginErrors</li>\n      <li><strong>Tasks</strong> — 任务状态：taskList, activeTask, taskDependencies</li>\n      <li><strong>Agents</strong> — 代理状态：activeAgents, coordinatorState, swarmBackend</li>\n      <li><strong>Speculation</strong> — 推测执行状态：speculationState, specBoundary, writtenPaths</li>\n      <li><strong>Remote</strong> — 远程连接状态：remoteSession, bridgeStatus, jwtToken</li>\n      <li><strong>Kairos</strong> — Kairos 集成状态：kairosEnabled, kairosSession</li>\n      <li><strong>Computer Use</strong> — 计算机操控状态：screenState, appDetection, inputMode</li>\n    </ul>\n\n    <h2>18.2 DeepImmutable 不可变包装</h2>\n    <p>AppState 通过 <strong>DeepImmutable&lt;T&gt;</strong> 类型包装器在类型层面强制不可变性。这是一个递归的 TypeScript 工具类型，将所有属性、嵌套对象和数组都标记为 readonly：</p>\n    <ul>\n      <li>防止直接修改状态字段（编译时报错）</li>\n      <li>强制所有状态更新通过 setAppState 函数进行</li>\n      <li>数组变为 ReadonlyArray，Map 变为 ReadonlyMap</li>\n    </ul>\n\n    <h2>18.3 Store 模式</h2>\n    <p><strong>createStore&lt;T&gt;</strong> 提供了一个极简的 Store 实现，是整个状态管理系统的基础原语：</p>\n    <ul>\n      <li><strong>get()</strong> — 获取当前状态快照</li>\n      <li><strong>set(updater)</strong> — 通过更新函数设置新状态。接受 <code>(prev: T) =&gt; T</code> 或直接值</li>\n      <li><strong>subscribe(listener)</strong> — 订阅状态变更，返回取消订阅函数</li>\n    </ul>\n    <p><strong>AppStateStore.ts</strong> 基于 createStore 创建了全局唯一的应用状态存储实例。</p>\n\n    <h2>18.4 状态变更副作用</h2>\n    <p><strong>onChangeAppState.ts</strong> 注册了一组状态变更监听器，当特定字段变化时触发副作用：</p>\n    <ul>\n      <li>当 model 变更时，重新计算 token 限制和工具可用性</li>\n      <li>当 permissions 变更时，更新工具的权限检查缓存</li>\n      <li>当 mcpClients 变更时，重新注册 MCP 工具</li>\n      <li>当 speculationState 变更时，触发推测执行的启动或中止</li>\n    </ul>\n\n    <h2>18.5 SpeculationState 推测执行状态</h2>\n    <p>推测执行使用专门的状态结构追踪其生命周期：</p>\n    <ul>\n      <li><strong>idle</strong> — 空闲状态，等待触发条件</li>\n      <li><strong>active</strong> — 活跃状态，包含 abort() 中止函数、startTime 启动时间、messagesRef 消息引用、writtenPathsRef 已写入路径集合、boundary 推测边界</li>\n    </ul>\n\n    <h2>18.6 bootstrap/state.ts 与 selectors</h2>\n    <p><strong>bootstrap/state.ts</strong> 提供全局单例状态，不依赖 React 上下文。这使得非 UI 代码（如工具执行、MCP 处理）也能访问应用状态。</p>\n    <p><strong>selectors.ts</strong> 提供派生状态计算，将原始状态转换为业务视图。<strong>teammateViewHelpers.ts</strong> 则为队友显示提供专门的视图状态转换。</p>\n  ",architecture:'\n    <div class="arch-diagram">\n      <h3>状态管理架构图</h3>\n      <div class="arch-flow">\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px; margin-bottom: 16px;">\n          <h4 style="color: #e94560;">AppState — 300+ 字段的全局状态</h4>\n          <div style="display: grid; grid-template-columns: repeat(5, 1fr); gap: 6px;">\n            <div style="background: #16213e; padding: 8px; border-radius: 6px; text-align: center; border: 1px solid #e94560; font-size: 11px;">\n              <div style="font-weight: bold;">Core</div>\n              <div style="color: #aaa;">会话/模型</div>\n            </div>\n            <div style="background: #16213e; padding: 8px; border-radius: 6px; text-align: center; border: 1px solid #e94560; font-size: 11px;">\n              <div style="font-weight: bold;">Permissions</div>\n              <div style="color: #aaa;">权限控制</div>\n            </div>\n            <div style="background: #16213e; padding: 8px; border-radius: 6px; text-align: center; border: 1px solid #e94560; font-size: 11px;">\n              <div style="font-weight: bold;">MCP</div>\n              <div style="color: #aaa;">连接/工具</div>\n            </div>\n            <div style="background: #16213e; padding: 8px; border-radius: 6px; text-align: center; border: 1px solid #e94560; font-size: 11px;">\n              <div style="font-weight: bold;">Tasks</div>\n              <div style="color: #aaa;">任务列表</div>\n            </div>\n            <div style="background: #16213e; padding: 8px; border-radius: 6px; text-align: center; border: 1px solid #e94560; font-size: 11px;">\n              <div style="font-weight: bold;">Agents</div>\n              <div style="color: #aaa;">代理状态</div>\n            </div>\n          </div>\n          <div style="display: grid; grid-template-columns: repeat(5, 1fr); gap: 6px; margin-top: 6px;">\n            <div style="background: #16213e; padding: 8px; border-radius: 6px; text-align: center; border: 1px solid #533483; font-size: 11px;">\n              <div style="font-weight: bold;">Speculation</div>\n              <div style="color: #aaa;">推测执行</div>\n            </div>\n            <div style="background: #16213e; padding: 8px; border-radius: 6px; text-align: center; border: 1px solid #533483; font-size: 11px;">\n              <div style="font-weight: bold;">Remote</div>\n              <div style="color: #aaa;">远程连接</div>\n            </div>\n            <div style="background: #16213e; padding: 8px; border-radius: 6px; text-align: center; border: 1px solid #533483; font-size: 11px;">\n              <div style="font-weight: bold;">Plugins</div>\n              <div style="color: #aaa;">插件配置</div>\n            </div>\n            <div style="background: #16213e; padding: 8px; border-radius: 6px; text-align: center; border: 1px solid #533483; font-size: 11px;">\n              <div style="font-weight: bold;">Kairos</div>\n              <div style="color: #aaa;">集成状态</div>\n            </div>\n            <div style="background: #16213e; padding: 8px; border-radius: 6px; text-align: center; border: 1px solid #533483; font-size: 11px;">\n              <div style="font-weight: bold;">ComputerUse</div>\n              <div style="color: #aaa;">屏幕操控</div>\n            </div>\n          </div>\n        </div>\n        <div style="text-align: center; font-size: 24px; color: #e94560;">&#x2193;</div>\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px; margin-bottom: 16px;">\n          <h4 style="color: #0f3460;">Store 模式</h4>\n          <div style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 12px;">\n            <div style="background: #16213e; padding: 14px; border-radius: 8px; border: 1px solid #0f3460; text-align: center;">\n              <div style="font-weight: bold;">createStore&lt;T&gt;</div>\n              <div style="font-size: 11px; color: #aaa;">get / set / subscribe</div>\n            </div>\n            <div style="background: #16213e; padding: 14px; border-radius: 8px; border: 2px solid #0f3460; text-align: center;">\n              <div style="font-weight: bold;">AppStateStore</div>\n              <div style="font-size: 11px; color: #aaa;">全局单例</div>\n            </div>\n            <div style="background: #16213e; padding: 14px; border-radius: 8px; border: 1px solid #0f3460; text-align: center;">\n              <div style="font-weight: bold;">DeepImmutable</div>\n              <div style="font-size: 11px; color: #aaa;">类型级不可变</div>\n            </div>\n          </div>\n        </div>\n        <div style="text-align: center; font-size: 24px; color: #0f3460;">&#x2193;</div>\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px; margin-bottom: 16px;">\n          <h4 style="color: #48c9b0;">副作用 &amp; 派生</h4>\n          <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 12px;">\n            <div style="background: #16213e; padding: 12px; border-radius: 8px; border: 1px solid #48c9b0;">\n              <div style="font-weight: bold; font-size: 13px;">onChangeAppState</div>\n              <div style="font-size: 11px; color: #aaa;">model &#x2192; token 重算</div>\n              <div style="font-size: 11px; color: #aaa;">permissions &#x2192; 缓存更新</div>\n              <div style="font-size: 11px; color: #aaa;">mcp &#x2192; 工具重注册</div>\n            </div>\n            <div style="background: #16213e; padding: 12px; border-radius: 8px; border: 1px solid #48c9b0;">\n              <div style="font-weight: bold; font-size: 13px;">selectors.ts</div>\n              <div style="font-size: 11px; color: #aaa;">派生状态计算</div>\n              <div style="font-size: 11px; color: #aaa;">teammateViewHelpers</div>\n              <div style="font-size: 11px; color: #aaa;">bootstrap/state.ts</div>\n            </div>\n          </div>\n        </div>\n        <div style="text-align: center; font-size: 24px; color: #48c9b0;">&#x2193;</div>\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px;">\n          <h4 style="color: #f0c040;">SpeculationState</h4>\n          <div style="display: flex; justify-content: center; gap: 12px; align-items: center;">\n            <div style="background: #3a3000; padding: 10px 20px; border-radius: 20px; border: 1px solid #f0c040;">idle</div>\n            <div style="color: #f0c040; font-size: 20px;">&#x21C4;</div>\n            <div style="background: #3a3000; padding: 10px 20px; border-radius: 20px; border: 2px solid #f0c040;">\n              <div>active</div>\n              <div style="font-size: 10px; color: #ddd;">abort | startTime | boundary</div>\n            </div>\n          </div>\n        </div>\n      </div>\n    </div>\n  ',codeExamples:[{title:"createStore 与 AppState 定义",language:"typescript",code:'// DeepImmutable: 递归不可变类型包装\ntype DeepImmutable<T> =\n  T extends Map<infer K, infer V>\n    ? ReadonlyMap<DeepImmutable<K>, DeepImmutable<V>>\n  : T extends Set<infer S>\n    ? ReadonlySet<DeepImmutable<S>>\n  : T extends Array<infer E>\n    ? ReadonlyArray<DeepImmutable<E>>\n  : T extends object\n    ? { readonly [K in keyof T]: DeepImmutable<T[K]> }\n  : T;\n\n// createStore: 极简 Store 原语\ninterface Store<T> {\n  get(): DeepImmutable<T>;\n  set(updater: T | ((prev: T) => T)): void;\n  subscribe(listener: (state: DeepImmutable<T>) => void): () => void;\n}\n\nfunction createStore<T>(initialState: T): Store<T> {\n  let state = initialState;\n  const listeners = new Set<\n    (state: DeepImmutable<T>) => void\n  >();\n\n  return {\n    get() {\n      return state as DeepImmutable<T>;\n    },\n    set(updater) {\n      const next = typeof updater === "function"\n        ? (updater as (prev: T) => T)(state)\n        : updater;\n      if (next !== state) {\n        state = next;\n        const immutable = state as DeepImmutable<T>;\n        for (const listener of listeners) {\n          listener(immutable);\n        }\n      }\n    },\n    subscribe(listener) {\n      listeners.add(listener);\n      return () => listeners.delete(listener);\n    }\n  };\n}\n\n// AppStateStore.ts: 全局状态存储\nconst appStateStore = createStore<AppState>(\n  initialAppState\n);\n\n// 导出便捷函数\nfunction getAppState(): DeepImmutable<AppState> {\n  return appStateStore.get();\n}\n\nfunction setAppState(\n  updater: (prev: AppState) => AppState\n): void {\n  appStateStore.set(updater);\n}'},{title:"AppState 类型定义（精简版）",language:"typescript",code:'// AppState: 300+ 字段的全局状态类型（精简展示）\ninterface AppState {\n  // === Core 核心 ===\n  conversationId: string;\n  messages: Message[];\n  model: ModelName;\n  currentTool: string | null;\n  isLoading: boolean;\n  error: string | null;\n  apiKey: string;\n  tokenUsage: TokenUsage;\n\n  // === Permissions 权限 ===\n  grantedPermissions: Map<string, Permission>;\n  deniedPermissions: Set<string>;\n  permissionMode: "default" | "auto" | "manual";\n  autoApprovePatterns: string[];\n\n  // === MCP ===\n  mcpClients: Map<string, McpClient>;\n  mcpTools: ToolDefinition[];\n  mcpResources: Resource[];\n  mcpConnectionStatus: Map<string, ConnectionStatus>;\n\n  // === Tasks 任务 ===\n  taskList: Task[];\n  activeTask: Task | null;\n\n  // === Agents 代理 ===\n  activeAgents: Agent[];\n  coordinatorState: CoordinatorState | null;\n  swarmBackend: "iterm" | "tmux" | "in-process";\n\n  // === Speculation 推测执行 ===\n  speculationState: SpeculationState;\n\n  // === Remote 远程 ===\n  remoteSession: RemoteSession | null;\n  bridgeStatus: BridgeStatus;\n  jwtToken: string | null;\n\n  // ... 更多字段 (Kairos, ComputerUse, Plugins 等)\n}\n\n// SpeculationState: 推测执行的精确状态\ntype SpeculationState =\n  | { status: "idle" }\n  | {\n      status: "active";\n      abort: () => void;\n      startTime: number;\n      messagesRef: { current: Message[] };\n      writtenPathsRef: { current: Set<string> };\n      boundary: SpeculationBoundary;\n    };'},{title:"onChangeAppState 副作用注册",language:"typescript",code:'// onChangeAppState.ts: 状态变更副作用\nfunction registerStateEffects(\n  store: Store<AppState>\n): void {\n  let prevState = store.get();\n\n  store.subscribe((nextState) => {\n    // model 变更 -> 重算 token 限制\n    if (nextState.model !== prevState.model) {\n      const limits = getModelLimits(nextState.model);\n      store.set(prev => ({\n        ...prev,\n        tokenUsage: {\n          ...prev.tokenUsage,\n          maxTokens: limits.maxOutputTokens,\n          contextWindow: limits.contextWindow\n        }\n      }));\n    }\n\n    // permissions 变更 -> 清除权限缓存\n    if (\n      nextState.grantedPermissions\n        !== prevState.grantedPermissions\n    ) {\n      clearPermissionCache();\n      rebuildToolPermissions(\n        nextState.grantedPermissions\n      );\n    }\n\n    // mcpClients 变更 -> 重新注册工具\n    if (\n      nextState.mcpClients !== prevState.mcpClients\n    ) {\n      const newTools = collectMcpTools(\n        nextState.mcpClients\n      );\n      store.set(prev => ({\n        ...prev,\n        mcpTools: newTools\n      }));\n    }\n\n    // speculationState 变更 -> 执行控制\n    if (\n      nextState.speculationState\n        !== prevState.speculationState\n    ) {\n      if (nextState.speculationState.status === "active") {\n        console.log(\n          "推测执行启动 at "\n          + nextState.speculationState.startTime\n        );\n      }\n    }\n\n    prevState = nextState;\n  });\n}\n\n// selectors.ts: 派生状态\nfunction selectAvailableTools(\n  state: DeepImmutable<AppState>\n): ToolDefinition[] {\n  const builtinTools = getBuiltinTools();\n  const mcpTools = state.mcpTools;\n  const pluginTools = state.installedPlugins\n    .flatMap(p => p.tools || []);\n\n  return [...builtinTools, ...mcpTools, ...pluginTools]\n    .filter(\n      t => isToolPermitted(t, state.grantedPermissions)\n    );\n}\n\n// bootstrap/state.ts: 全局单例（非 React）\nlet globalState: AppState | null = null;\n\nfunction getGlobalState(): AppState {\n  if (!globalState) {\n    throw new Error("State not initialized");\n  }\n  return globalState;\n}\n\nfunction initGlobalState(state: AppState): void {\n  globalState = state;\n}'}],interactive:'\n    <h3>状态管理交互式演练</h3>\n    <div class="interactive-demo">\n      <div class="step" data-step="1">\n        <h4>Step 1: 理解 AppState 结构</h4>\n        <p>AppState 是一个拥有 300+ 字段的巨型对象。通过 DeepImmutable 包装，所有字段在类型层面都是只读的。直接修改会导致 TypeScript 编译错误。</p>\n        <div class="code-highlight">\n          <pre>// 编译错误! DeepImmutable 阻止直接修改\nconst state = getAppState();\nstate.model = "claude-4";  // Error: readonly\n\n// 正确方式: 通过 setAppState 更新\nsetAppState(prev => ({\n  ...prev,\n  model: "claude-4"\n}));</pre>\n        </div>\n      </div>\n      <div class="step" data-step="2">\n        <h4>Step 2: Store 的 subscribe 机制</h4>\n        <p>createStore 的 subscribe 方法允许注册监听器。当状态变更时，所有监听器同步执行。返回的函数用于取消订阅。</p>\n        <div class="code-highlight">\n          <pre>const unsub = appStateStore.subscribe(state => {\n  console.log("Model:", state.model);\n  console.log("Tools:", state.mcpTools.length);\n});\n\n// 触发状态变更\nsetAppState(prev => ({ ...prev, model: "claude-4" }));\n// 输出: Model: claude-4, Tools: 42\n\n// 取消订阅\nunsub();</pre>\n        </div>\n      </div>\n      <div class="step" data-step="3">\n        <h4>Step 3: 副作用链式触发</h4>\n        <p>状态变更会触发 onChangeAppState 中注册的副作用。例如切换模型会连锁触发 token 限制重算，这又会更新 UI 中的剩余 token 显示。</p>\n        <div class="code-highlight">\n          <pre>用户切换模型: claude-3.5 &#x2192; claude-4\n\nonChangeAppState 检测 model 变更:\n  1. 获取 claude-4 的 token 限制\n  2. 更新 tokenUsage.maxTokens: 4096 &#x2192; 16384\n  3. 更新 tokenUsage.contextWindow: 200k &#x2192; 1M\n  4. UI 自动刷新显示新的限制值</pre>\n        </div>\n      </div>\n      <div class="step" data-step="4">\n        <h4>Step 4: SpeculationState 生命周期</h4>\n        <p>推测执行的状态在 idle 和 active 之间切换。active 状态携带 abort 函数和路径追踪引用，用于在用户输入不匹配时中止推测。</p>\n        <div class="code-highlight">\n          <pre>推测执行状态流转:\n  idle &#x2192; 检测到用户可能的下一步操作\n  active {\n    startTime: 1711900000,\n    writtenPaths: {"src/api.ts", "src/types.ts"},\n    boundary: { maxTokens: 2000 }\n  }\n  &#x2192; 用户确认 &#x2192; 提交推测结果\n  &#x2192; 或用户否认 &#x2192; abort() &#x2192; 回滚 &#x2192; idle</pre>\n        </div>\n      </div>\n    </div>\n  '},c19:{content:'\n    <h2>19.1 自定义 Ink 分支</h2>\n    <p>Claude Code <strong>没有使用</strong> npm 上的 Ink 包，而是在 <strong>ink/</strong> 目录维护了一个包含 <strong>80+ 文件</strong>的自定义 Ink 分支。分叉的核心原因包括：</p>\n    <ul>\n      <li><strong>性能优化</strong> — 原版 Ink 在高频输出（如 streaming 回复）场景下存在闪烁和性能瓶颈。Claude Code 的渲染器实现了帧合并、差量更新和闪烁防护</li>\n      <li><strong>鼠标与点击支持</strong> — 原版 Ink 不支持鼠标事件。自定义版本增加了鼠标点击、滚轮、焦点管理等完整的交互支持</li>\n      <li><strong>虚拟滚动</strong> — 对长输出实现了虚拟滚动机制（useVirtualScroll），只渲染可见区域，大幅降低内存占用</li>\n      <li><strong>Vim 模式集成</strong> — 内置完整的 Vim 键绑定系统，支持 motions、operators 和 text objects</li>\n    </ul>\n\n    <h2>19.2 React Reconciler 与终端 DOM</h2>\n    <p>Claude Code 的终端 UI 基于 <strong>React Reconciler</strong> 构建，将 React 的组件模型映射到终端输出：</p>\n    <ul>\n      <li><strong>reconciler.ts</strong> — 自定义 React Reconciler，实现了 createInstance、appendChild、commitUpdate 等核心方法。将 React 元素树转换为终端 DOM 节点树</li>\n      <li><strong>dom.ts</strong> — 终端 DOM 层，定义了 DOMElement 和 DOMText 节点类型。每个节点携带样式（颜色、边距、flex 属性等）和内容信息</li>\n      <li><strong>renderer.ts</strong> — 渲染器，遍历 DOM 树生成 ANSI 转义序列输出。包含帧率控制和差量渲染优化</li>\n    </ul>\n\n    <h2>19.3 Flexbox 布局引擎</h2>\n    <p><strong>layout/</strong> 目录通过 <strong>yoga-layout</strong> 绑定实现了终端中的 Flexbox 布局：</p>\n    <ul>\n      <li>支持 flexDirection、justifyContent、alignItems 等标准 Flexbox 属性</li>\n      <li>支持 margin、padding、border 盒模型</li>\n      <li>支持百分比和固定尺寸</li>\n      <li>Yoga 引擎将布局计算映射到终端行列坐标系统</li>\n    </ul>\n\n    <h2>19.4 核心组件与 Hooks</h2>\n    <p>自定义 Ink 提供了一组核心组件和 Hooks：</p>\n    <ul>\n      <li><strong>组件</strong>: Box（布局容器）、Text（文本渲染）、ScrollBox（可滚动容器）、Button（可点击按钮）、Link（超链接）、App（根应用容器）</li>\n      <li><strong>Hooks</strong>: useInput（键盘输入处理）、useAnimationFrame（动画帧回调）、useSelection（选区管理）</li>\n      <li><strong>事件系统</strong>: keyboard、mouse、click、focus、terminal resize 等事件</li>\n    </ul>\n\n    <h2>19.5 屏幕与快捷键</h2>\n    <p><strong>screens/</strong> 定义了应用的主要界面：</p>\n    <ul>\n      <li><strong>REPL.tsx</strong> — 主交互界面，包含输入区、输出区、状态栏和工具栏</li>\n      <li><strong>Doctor.tsx</strong> — 诊断界面，检查环境配置和依赖状态</li>\n      <li><strong>ResumeConversation.tsx</strong> — 会话恢复界面</li>\n    </ul>\n    <p><strong>keybindings/</strong>（14 个文件）提供了完整的快捷键系统：parser 解析键序列、resolver 解析绑定冲突、schema 定义绑定格式、defaultBindings 提供默认配置。</p>\n\n    <h2>19.6 Vim 模式与语音输入</h2>\n    <p><strong>vim/</strong>（5 个文件）实现了终端中的 Vim 编辑模式：</p>\n    <ul>\n      <li><strong>motions</strong> — 光标移动：h/j/k/l、w/b/e、0/$</li>\n      <li><strong>operators</strong> — 操作符：d(delete)、c(change)、y(yank)</li>\n      <li><strong>textObjects</strong> — 文本对象：iw(inner word)、a"(around quotes)</li>\n      <li><strong>transitions</strong> — 模式切换：Normal/Insert/Visual</li>\n    </ul>\n    <p><strong>语音输入</strong>通过 useVoice、useVoiceIntegration 和 voiceStreamSTT 实现实时语音转文字输入。</p>\n\n    <h2>19.7 输出优化</h2>\n    <p><strong>frame.ts</strong> 实现帧合并和闪烁防护，<strong>optimizer.ts</strong> 优化 ANSI 输出序列长度。两者配合确保即使在高频 streaming 场景下也能流畅显示。</p>\n  ',architecture:'\n    <div class="arch-diagram">\n      <h3>终端 UI 框架架构图</h3>\n      <div class="arch-flow">\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px; margin-bottom: 16px;">\n          <h4 style="color: #e94560;">React 组件层</h4>\n          <div style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 10px;">\n            <div style="background: #16213e; padding: 12px; border-radius: 8px; border: 2px solid #e94560; text-align: center;">\n              <div style="font-weight: bold;">REPL.tsx</div>\n              <div style="font-size: 11px; color: #aaa;">主界面</div>\n            </div>\n            <div style="background: #16213e; padding: 12px; border-radius: 8px; border: 1px solid #e94560; text-align: center;">\n              <div style="font-weight: bold;">Doctor.tsx</div>\n              <div style="font-size: 11px; color: #aaa;">诊断界面</div>\n            </div>\n            <div style="background: #16213e; padding: 12px; border-radius: 8px; border: 1px solid #e94560; text-align: center;">\n              <div style="font-weight: bold;">Resume.tsx</div>\n              <div style="font-size: 11px; color: #aaa;">会话恢复</div>\n            </div>\n          </div>\n          <div style="display: flex; justify-content: center; gap: 8px; margin-top: 10px; flex-wrap: wrap;">\n            <div style="background: #0d1b2a; padding: 6px 12px; border-radius: 16px; border: 1px solid #e94560; font-size: 12px;">Box</div>\n            <div style="background: #0d1b2a; padding: 6px 12px; border-radius: 16px; border: 1px solid #e94560; font-size: 12px;">Text</div>\n            <div style="background: #0d1b2a; padding: 6px 12px; border-radius: 16px; border: 1px solid #e94560; font-size: 12px;">ScrollBox</div>\n            <div style="background: #0d1b2a; padding: 6px 12px; border-radius: 16px; border: 1px solid #e94560; font-size: 12px;">Button</div>\n            <div style="background: #0d1b2a; padding: 6px 12px; border-radius: 16px; border: 1px solid #e94560; font-size: 12px;">Link</div>\n          </div>\n        </div>\n        <div style="text-align: center; font-size: 24px; color: #e94560;">&#x2193;</div>\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px; margin-bottom: 16px;">\n          <h4 style="color: #0f3460;">React Reconciler + DOM</h4>\n          <div style="display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 10px;">\n            <div style="background: #16213e; padding: 12px; border-radius: 8px; border: 1px solid #0f3460; text-align: center;">\n              <div style="font-weight: bold; font-size: 13px;">reconciler.ts</div>\n              <div style="font-size: 11px; color: #aaa;">React &#x2192; Terminal DOM</div>\n            </div>\n            <div style="background: #16213e; padding: 12px; border-radius: 8px; border: 1px solid #0f3460; text-align: center;">\n              <div style="font-weight: bold; font-size: 13px;">dom.ts</div>\n              <div style="font-size: 11px; color: #aaa;">DOMElement 树</div>\n            </div>\n            <div style="background: #16213e; padding: 12px; border-radius: 8px; border: 1px solid #0f3460; text-align: center;">\n              <div style="font-weight: bold; font-size: 13px;">layout/ (yoga)</div>\n              <div style="font-size: 11px; color: #aaa;">Flexbox 布局</div>\n            </div>\n          </div>\n        </div>\n        <div style="text-align: center; font-size: 24px; color: #0f3460;">&#x2193;</div>\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px; margin-bottom: 16px;">\n          <h4 style="color: #533483;">渲染 &amp; 输入</h4>\n          <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 12px;">\n            <div style="background: #2d1b4e; padding: 14px; border-radius: 8px; border: 1px solid #533483;">\n              <div style="font-weight: bold; font-size: 13px;">renderer.ts</div>\n              <div style="font-size: 11px; color: #aaa;">DOM &#x2192; ANSI 输出</div>\n              <div style="font-size: 11px; color: #aaa;">frame.ts 帧合并</div>\n              <div style="font-size: 11px; color: #aaa;">optimizer.ts 序列优化</div>\n            </div>\n            <div style="background: #2d1b4e; padding: 14px; border-radius: 8px; border: 1px solid #533483;">\n              <div style="font-weight: bold; font-size: 13px;">输入系统</div>\n              <div style="font-size: 11px; color: #aaa;">keybindings/ (14 files)</div>\n              <div style="font-size: 11px; color: #aaa;">vim/ (5 files)</div>\n              <div style="font-size: 11px; color: #aaa;">useVoice 语音输入</div>\n            </div>\n          </div>\n        </div>\n        <div style="text-align: center; font-size: 24px; color: #533483;">&#x2193;</div>\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px;">\n          <h4 style="color: #48c9b0;">终端输出</h4>\n          <div style="display: flex; justify-content: center; gap: 16px;">\n            <div style="background: #16213e; padding: 12px 20px; border-radius: 8px; border: 1px solid #48c9b0;">\n              <div style="font-weight: bold;">process.stdout</div>\n              <div style="font-size: 11px; color: #aaa;">ANSI 转义序列</div>\n            </div>\n            <div style="background: #16213e; padding: 12px 20px; border-radius: 8px; border: 1px solid #48c9b0;">\n              <div style="font-weight: bold;">process.stdin</div>\n              <div style="font-size: 11px; color: #aaa;">按键/鼠标事件</div>\n            </div>\n          </div>\n        </div>\n      </div>\n    </div>\n  ',codeExamples:[{title:"React Reconciler 与终端 DOM",language:"typescript",code:'// dom.ts: 终端 DOM 节点定义\ninterface DOMElement {\n  nodeName: string;\n  parentNode: DOMElement | null;\n  childNodes: Array<DOMElement | DOMText>;\n  style: DOMStyle;\n  yogaNode: YogaNode | null;\n  // 事件处理\n  onClick?: (event: ClickEvent) => void;\n  onFocus?: () => void;\n  onBlur?: () => void;\n}\n\ninterface DOMText {\n  nodeName: "#text";\n  nodeValue: string;\n  parentNode: DOMElement | null;\n}\n\ninterface DOMStyle {\n  color?: string;\n  backgroundColor?: string;\n  flexDirection?: "row" | "column";\n  justifyContent?: "flex-start" | "center" | "flex-end";\n  alignItems?: "flex-start" | "center" | "flex-end";\n  width?: number | string;\n  height?: number | string;\n  marginTop?: number;\n  marginBottom?: number;\n  paddingLeft?: number;\n  paddingRight?: number;\n  borderStyle?: "single" | "double" | "round";\n}\n\n// reconciler.ts: React Reconciler 实现\nconst reconciler = ReactReconciler({\n  supportsMutation: true,\n\n  createInstance(\n    type: string,\n    props: Record<string, unknown>\n  ): DOMElement {\n    const node: DOMElement = {\n      nodeName: type,\n      parentNode: null,\n      childNodes: [],\n      style: extractStyle(props),\n      yogaNode: Yoga.Node.create()\n    };\n    applyYogaStyle(node.yogaNode, node.style);\n    return node;\n  },\n\n  createTextInstance(text: string): DOMText {\n    return {\n      nodeName: "#text",\n      nodeValue: text,\n      parentNode: null\n    };\n  },\n\n  appendChild(parent: DOMElement, child: DOMElement | DOMText) {\n    child.parentNode = parent;\n    parent.childNodes.push(child);\n    if ("yogaNode" in child && child.yogaNode) {\n      parent.yogaNode?.insertChild(\n        child.yogaNode,\n        parent.yogaNode.getChildCount()\n      );\n    }\n  },\n\n  commitUpdate(\n    node: DOMElement,\n    _oldProps: Record<string, unknown>,\n    newProps: Record<string, unknown>\n  ) {\n    node.style = extractStyle(newProps);\n    if (node.yogaNode) {\n      applyYogaStyle(node.yogaNode, node.style);\n    }\n  }\n});'},{title:"渲染器与帧优化",language:"typescript",code:'// renderer.ts: DOM 树 -> ANSI 输出\nclass TerminalRenderer {\n  private lastOutput = "";\n  private frameBuffer: string[] = [];\n  private rafId: number | null = null;\n\n  render(rootNode: DOMElement): void {\n    // 1. Yoga 计算布局\n    rootNode.yogaNode?.calculateLayout(\n      this.termWidth,\n      this.termHeight\n    );\n\n    // 2. 遍历 DOM 树生成 ANSI\n    const output = this.renderNode(rootNode, 0, 0);\n\n    // 3. 帧合并：防止高频刷新闪烁\n    this.frameBuffer.push(output);\n    if (this.rafId === null) {\n      this.rafId = requestAnimationFrame(() => {\n        this.flush();\n        this.rafId = null;\n      });\n    }\n  }\n\n  private renderNode(\n    node: DOMElement,\n    x: number,\n    y: number\n  ): string {\n    const layout = node.yogaNode?.getComputedLayout();\n    if (!layout) return "";\n\n    let result = "";\n    const absX = x + layout.left;\n    const absY = y + layout.top;\n\n    // 绘制背景和边框\n    if (node.style.backgroundColor) {\n      result += this.drawBackground(\n        absX, absY, layout.width, layout.height,\n        node.style.backgroundColor\n      );\n    }\n\n    // 递归渲染子节点\n    for (const child of node.childNodes) {\n      if (child.nodeName === "#text") {\n        result += this.drawText(\n          absX, absY, child.nodeValue,\n          node.style.color\n        );\n      } else {\n        result += this.renderNode(\n          child as DOMElement, absX, absY\n        );\n      }\n    }\n\n    return result;\n  }\n\n  // frame.ts: 帧合并与闪烁防护\n  private flush(): void {\n    if (this.frameBuffer.length === 0) return;\n\n    // 取最后一帧（丢弃中间帧）\n    const latest =\n      this.frameBuffer[this.frameBuffer.length - 1];\n    this.frameBuffer = [];\n\n    // 差量输出优化\n    if (latest !== this.lastOutput) {\n      const optimized = optimizeAnsi(\n        this.lastOutput, latest\n      );\n      process.stdout.write(optimized);\n      this.lastOutput = latest;\n    }\n  }\n}\n\n// optimizer.ts: ANSI 序列压缩\nfunction optimizeAnsi(\n  prev: string, next: string\n): string {\n  // 找到第一个差异位置\n  let diffStart = 0;\n  while (\n    diffStart < prev.length\n    && diffStart < next.length\n    && prev[diffStart] === next[diffStart]\n  ) {\n    diffStart++;\n  }\n  // 只输出变化部分 + 光标定位\n  return cursorTo(diffStart) + next.slice(diffStart);\n}'},{title:"快捷键系统与 Vim 模式",language:"typescript",code:'// keybindings/parser.ts: 键序列解析\ninterface KeyBinding {\n  key: string;           // "ctrl+s", "escape j"\n  command: string;\n  when?: string;         // 条件表达式\n  priority?: number;\n}\n\nfunction parseKeySequence(\n  raw: string\n): ParsedKey[] {\n  return raw.split(" ").map(part => {\n    const modifiers: string[] = [];\n    let key = part;\n\n    if (key.includes("+")) {\n      const segments = key.split("+");\n      key = segments.pop()!;\n      modifiers.push(...segments);\n    }\n\n    return { key, modifiers };\n  });\n}\n\n// keybindings/resolver.ts: 绑定解析与冲突处理\nclass KeybindingResolver {\n  private bindings: KeyBinding[] = [];\n\n  resolve(\n    event: KeyEvent,\n    context: KeyContext\n  ): KeyBinding | null {\n    const matches = this.bindings.filter(b => {\n      const parsed = parseKeySequence(b.key);\n      return matchesKey(parsed, event)\n        && (!b.when || evaluateWhen(b.when, context));\n    });\n\n    if (matches.length === 0) return null;\n\n    // 优先级排序：用户自定义 > 默认\n    matches.sort(\n      (a, b) => (b.priority || 0) - (a.priority || 0)\n    );\n    return matches[0];\n  }\n}\n\n// vim/motions.ts: Vim 光标移动\nconst vimMotions: Record<string, VimMotion> = {\n  h: { type: "char", direction: -1 },\n  l: { type: "char", direction: 1 },\n  j: { type: "line", direction: 1 },\n  k: { type: "line", direction: -1 },\n  w: { type: "word", direction: 1 },\n  b: { type: "word", direction: -1 },\n  "0": { type: "lineStart" },\n  "$": { type: "lineEnd" }\n};\n\n// vim/operators.ts: Vim 操作符\nconst vimOperators: Record<string, VimOperator> = {\n  d: {\n    name: "delete",\n    execute(range: TextRange, buffer: Buffer) {\n      const deleted = buffer.getText(range);\n      buffer.delete(range);\n      return { register: deleted };\n    }\n  },\n  c: {\n    name: "change",\n    execute(range: TextRange, buffer: Buffer) {\n      const deleted = buffer.getText(range);\n      buffer.delete(range);\n      return {\n        register: deleted,\n        enterMode: "insert"\n      };\n    }\n  },\n  y: {\n    name: "yank",\n    execute(range: TextRange, buffer: Buffer) {\n      return {\n        register: buffer.getText(range)\n      };\n    }\n  }\n};\n\n// vim/transitions.ts: 模式转换\ntype VimMode = "normal" | "insert" | "visual";\n\nfunction handleVimTransition(\n  currentMode: VimMode,\n  key: string\n): VimMode {\n  if (currentMode === "normal" && key === "i")\n    return "insert";\n  if (currentMode === "normal" && key === "v")\n    return "visual";\n  if (currentMode === "insert" && key === "escape")\n    return "normal";\n  if (currentMode === "visual" && key === "escape")\n    return "normal";\n  return currentMode;\n}'}],interactive:'\n    <h3>终端 UI 框架交互式演练</h3>\n    <div class="interactive-demo">\n      <div class="step" data-step="1">\n        <h4>Step 1: React 组件到终端 DOM</h4>\n        <p>开发者使用 JSX 编写终端界面，和 Web React 开发体验几乎一致。Box 和 Text 组件通过自定义 Reconciler 转换为终端 DOM 节点。</p>\n        <div class="code-highlight">\n          <pre>// JSX 组件\n&lt;Box flexDirection="column" padding={1}&gt;\n  &lt;Text color="green" bold&gt;Claude Code&lt;/Text&gt;\n  &lt;Box marginTop={1}&gt;\n    &lt;Text&gt;Ready for input...&lt;/Text&gt;\n  &lt;/Box&gt;\n&lt;/Box&gt;\n\n// 转换为 DOMElement 树\nDOMElement { nodeName: "Box", style: { flexDirection: "column" }\n  DOMElement { nodeName: "Text", style: { color: "green" }\n    DOMText { nodeValue: "Claude Code" }\n  }\n  DOMElement { nodeName: "Box", style: { marginTop: 1 }\n    DOMText { nodeValue: "Ready for input..." }\n  }\n}</pre>\n        </div>\n      </div>\n      <div class="step" data-step="2">\n        <h4>Step 2: Yoga 布局计算</h4>\n        <p>Yoga 引擎将 Flexbox 属性映射到终端行列坐标。每个 DOMElement 的 yogaNode 接收布局参数，计算出精确的 x/y/width/height。</p>\n        <div class="code-highlight">\n          <pre>Yoga 布局计算 (终端 80x24):\n  Box(root):  x=0, y=0, w=80, h=24\n    Text:     x=1, y=1, w=78, h=1  (padding=1)\n    Box:      x=1, y=3, w=78, h=1  (marginTop=1)</pre>\n        </div>\n      </div>\n      <div class="step" data-step="3">\n        <h4>Step 3: ANSI 渲染与帧优化</h4>\n        <p>renderer.ts 将布局结果转换为 ANSI 转义序列。frame.ts 在高频更新时合并多帧为一次输出，optimizer.ts 压缩重复的 ANSI 序列。</p>\n        <div class="code-highlight">\n          <pre>渲染流水线:\n  DOM 树 &#x2192; Yoga 布局 &#x2192; ANSI 生成 &#x2192; 帧合并 &#x2192; 差量输出\n\n帧合并示例 (16ms 内):\n  Frame 1: "Hello W..."  (丢弃)\n  Frame 2: "Hello Wo..." (丢弃)\n  Frame 3: "Hello World"  (输出!)\n\n差量输出: 只重绘变化的字符位置</pre>\n        </div>\n      </div>\n      <div class="step" data-step="4">\n        <h4>Step 4: Vim 模式与快捷键</h4>\n        <p>用户按 Escape 进入 Vim Normal 模式，可使用 hjkl 移动、dd 删除行、yy 复制行。keybindingResolver 处理键序列匹配和优先级。</p>\n        <div class="code-highlight">\n          <pre>Vim 操作流程:\n  按键: "d" &#x2192; 等待操作符...\n  按键: "w" &#x2192; 组合为 "dw" (delete word)\n\n  operator: "d" (delete)\n  motion: "w" (word forward)\n  &#x2192; 计算 word 范围: [col=5, col=12]\n  &#x2192; 删除文本，存入寄存器\n  &#x2192; 更新 DOM，重新渲染</pre>\n        </div>\n      </div>\n    </div>\n  '},c20:{content:'\n    <h2>20.1 Bridge 系统概览</h2>\n    <p>Claude Code 的 <strong>bridge/</strong> 目录包含 <strong>30 个文件</strong>，实现了与 IDE（如 VS Code、JetBrains）的双向通信系统。Bridge 让 Claude Code 可以作为 IDE 的后端引擎运行，IDE 作为前端提供可视化界面。</p>\n    <ul>\n      <li><strong>bridgeMain.ts</strong> — Bridge 主循环，管理连接建立、心跳检测和消息路由。启动后持续监听来自 IDE 的请求</li>\n      <li><strong>replBridge.ts</strong> — REPL 会话桥接，将终端 REPL 的输入输出转发到 IDE 的聊天面板</li>\n      <li><strong>bridgeMessaging.ts</strong> — 基于 JSON 的消息协议，定义了请求/响应/通知三种消息类型。支持消息序列化、反序列化和超时控制</li>\n      <li><strong>bridgePermissionCallbacks.ts</strong> — 权限委托机制，将文件写入、命令执行等权限确认请求转发给 IDE 端显示对话框，用户在 IDE 中确认后回传结果</li>\n    </ul>\n\n    <h2>20.2 认证与安全</h2>\n    <p>Bridge 通信使用多层安全机制：</p>\n    <ul>\n      <li><strong>jwtUtils.ts</strong> — JWT 令牌工具，用于 Bridge 连接的身份验证。生成短期令牌、验证签名和过期时间</li>\n      <li><strong>trustedDevice.ts</strong> — 设备信任管理，记录和验证已授权的设备指纹。首次连接需要用户确认信任</li>\n      <li><strong>workSecret.ts</strong> — 工作区密钥，为每个项目生成唯一的通信密钥，防止跨项目的未授权访问</li>\n    </ul>\n\n    <h2>20.3 远程会话管理</h2>\n    <p><strong>remote/</strong> 目录管理远程 Claude Code 会话：</p>\n    <ul>\n      <li><strong>RemoteSessionManager.ts</strong> — 远程会话管理器，负责创建、恢复和销毁远程会话。支持断线重连和状态同步</li>\n      <li><strong>SessionsWebSocket.ts</strong> — WebSocket 连接管理，在本地客户端和远程 Claude Code 实例之间建立实时双向通道</li>\n    </ul>\n    <p>远程模式允许 Claude Code 在云端服务器运行（更强的计算资源），本地客户端仅负责 UI 渲染和用户交互。</p>\n\n    <h2>20.4 Teleport — 环境迁移</h2>\n    <p><strong>utils/teleport/</strong>（4 个文件）实现环境迁移功能，将本地开发环境"传送"到远程：</p>\n    <ul>\n      <li><strong>gitBundle</strong> — 将 Git 仓库打包为 bundle 文件，包含所有未推送的提交和未暂存的更改</li>\n      <li><strong>environments</strong> — 环境快照管理，记录环境变量、工具版本和配置状态</li>\n      <li><strong>environmentSelection</strong> — 环境选择器，让用户选择要迁移的环境配置子集</li>\n    </ul>\n\n    <h2>20.5 Computer Use — 计算机操控</h2>\n    <p><strong>utils/computerUse/</strong>（15 个文件）实现了 AI 直接操控计算机的能力：</p>\n    <ul>\n      <li><strong>屏幕控制</strong> — 截图、OCR 文字识别、界面元素定位</li>\n      <li><strong>应用检测</strong> — 识别当前活动应用程序和窗口状态</li>\n      <li><strong>输入处理</strong> — 模拟键盘输入、鼠标点击和拖拽操作</li>\n      <li><strong>MCP 服务器</strong> — 将 Computer Use 能力暴露为 MCP 工具，其他代理也可调用</li>\n    </ul>\n\n    <h2>20.6 CLI 传输层</h2>\n    <p>CLI 的通信通过 <strong>handlers/</strong>（6 个文件）和 <strong>transports/</strong>（7 个文件）实现：</p>\n    <ul>\n      <li><strong>handlers</strong> — 请求处理器，分别处理 REPL、Bridge、Agent、远程等不同模式的消息</li>\n      <li><strong>transports</strong> — 传输层抽象，支持标准输入输出（stdio）、WebSocket、HTTP SSE 等多种传输协议</li>\n    </ul>\n\n    <h2>20.7 Deep Link 处理</h2>\n    <p><strong>utils/deepLink/</strong> 处理 <code>claude://</code> 协议的深度链接。用户可以通过浏览器中的链接直接打开 Claude Code 并自动执行特定操作（如打开项目、恢复会话、执行命令）。</p>\n  ',architecture:'\n    <div class="arch-diagram">\n      <h3>Bridge / IDE / 远程集成架构图</h3>\n      <div class="arch-flow">\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px; margin-bottom: 16px;">\n          <h4 style="color: #e94560;">IDE / 客户端层</h4>\n          <div style="display: flex; justify-content: center; gap: 12px;">\n            <div style="background: #16213e; padding: 12px 18px; border-radius: 8px; border: 2px solid #e94560;">\n              <div style="font-weight: bold;">VS Code 扩展</div>\n              <div style="font-size: 11px; color: #aaa;">Bridge Client</div>\n            </div>\n            <div style="background: #16213e; padding: 12px 18px; border-radius: 8px; border: 1px solid #e94560;">\n              <div style="font-weight: bold;">JetBrains 插件</div>\n              <div style="font-size: 11px; color: #aaa;">Bridge Client</div>\n            </div>\n            <div style="background: #16213e; padding: 12px 18px; border-radius: 8px; border: 1px solid #e94560;">\n              <div style="font-weight: bold;">终端 REPL</div>\n              <div style="font-size: 11px; color: #aaa;">本地直连</div>\n            </div>\n          </div>\n        </div>\n        <div style="text-align: center; font-size: 24px; color: #e94560;">&#x2193; JSON 消息协议 &#x2193;</div>\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px; margin-bottom: 16px;">\n          <h4 style="color: #0f3460;">Bridge 通信层 (bridge/)</h4>\n          <div style="display: grid; grid-template-columns: repeat(4, 1fr); gap: 8px;">\n            <div style="background: #16213e; padding: 10px; border-radius: 8px; text-align: center; border: 1px solid #0f3460; font-size: 12px;">\n              <div style="font-weight: bold;">bridgeMain</div>\n              <div style="font-size: 10px; color: #aaa;">主循环</div>\n            </div>\n            <div style="background: #16213e; padding: 10px; border-radius: 8px; text-align: center; border: 1px solid #0f3460; font-size: 12px;">\n              <div style="font-weight: bold;">replBridge</div>\n              <div style="font-size: 10px; color: #aaa;">REPL 桥接</div>\n            </div>\n            <div style="background: #16213e; padding: 10px; border-radius: 8px; text-align: center; border: 1px solid #0f3460; font-size: 12px;">\n              <div style="font-weight: bold;">messaging</div>\n              <div style="font-size: 10px; color: #aaa;">消息协议</div>\n            </div>\n            <div style="background: #16213e; padding: 10px; border-radius: 8px; text-align: center; border: 1px solid #0f3460; font-size: 12px;">\n              <div style="font-weight: bold;">permissions</div>\n              <div style="font-size: 10px; color: #aaa;">权限委托</div>\n            </div>\n          </div>\n          <div style="display: flex; justify-content: center; gap: 8px; margin-top: 10px;">\n            <div style="background: #0d1b2a; padding: 6px 14px; border-radius: 16px; border: 1px solid #e94560; font-size: 11px;">JWT 认证</div>\n            <div style="background: #0d1b2a; padding: 6px 14px; border-radius: 16px; border: 1px solid #e94560; font-size: 11px;">设备信任</div>\n            <div style="background: #0d1b2a; padding: 6px 14px; border-radius: 16px; border: 1px solid #e94560; font-size: 11px;">工作区密钥</div>\n          </div>\n        </div>\n        <div style="text-align: center; font-size: 24px; color: #0f3460;">&#x2193;</div>\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px; margin-bottom: 16px;">\n          <h4 style="color: #533483;">远程 &amp; 传输</h4>\n          <div style="display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 10px;">\n            <div style="background: #2d1b4e; padding: 12px; border-radius: 8px; border: 1px solid #533483;">\n              <div style="font-weight: bold; font-size: 13px;">remote/</div>\n              <div style="font-size: 11px; color: #aaa;">SessionManager</div>\n              <div style="font-size: 11px; color: #aaa;">WebSocket</div>\n            </div>\n            <div style="background: #2d1b4e; padding: 12px; border-radius: 8px; border: 1px solid #533483;">\n              <div style="font-weight: bold; font-size: 13px;">transports/</div>\n              <div style="font-size: 11px; color: #aaa;">stdio / WS / SSE</div>\n              <div style="font-size: 11px; color: #aaa;">7 个适配器</div>\n            </div>\n            <div style="background: #2d1b4e; padding: 12px; border-radius: 8px; border: 1px solid #533483;">\n              <div style="font-weight: bold; font-size: 13px;">handlers/</div>\n              <div style="font-size: 11px; color: #aaa;">6 种请求处理器</div>\n            </div>\n          </div>\n        </div>\n        <div style="text-align: center; font-size: 24px; color: #533483;">&#x2193;</div>\n        <div class="arch-layer" style="background: #1a1a2e; padding: 20px; border-radius: 12px;">\n          <h4 style="color: #48c9b0;">扩展能力</h4>\n          <div style="display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 10px;">\n            <div style="background: #16213e; padding: 12px; border-radius: 8px; border: 1px solid #48c9b0; text-align: center;">\n              <div style="font-weight: bold; font-size: 13px;">Teleport</div>\n              <div style="font-size: 11px; color: #aaa;">环境迁移 (4 files)</div>\n              <div style="font-size: 11px; color: #aaa;">gitBundle / 环境快照</div>\n            </div>\n            <div style="background: #16213e; padding: 12px; border-radius: 8px; border: 1px solid #48c9b0; text-align: center;">\n              <div style="font-weight: bold; font-size: 13px;">Computer Use</div>\n              <div style="font-size: 11px; color: #aaa;">屏幕操控 (15 files)</div>\n              <div style="font-size: 11px; color: #aaa;">截图 / 输入 / OCR</div>\n            </div>\n            <div style="background: #16213e; padding: 12px; border-radius: 8px; border: 1px solid #48c9b0; text-align: center;">\n              <div style="font-weight: bold; font-size: 13px;">Deep Link</div>\n              <div style="font-size: 11px; color: #aaa;">claude:// 协议</div>\n              <div style="font-size: 11px; color: #aaa;">项目/会话直达</div>\n            </div>\n          </div>\n        </div>\n      </div>\n    </div>\n  ',codeExamples:[{title:"Bridge 消息协议与主循环",language:"typescript",code:'// bridgeMessaging.ts: JSON 消息协议\ntype BridgeMessage =\n  | BridgeRequest\n  | BridgeResponse\n  | BridgeNotification;\n\ninterface BridgeRequest {\n  type: "request";\n  id: string;\n  method: string;\n  params: Record<string, unknown>;\n}\n\ninterface BridgeResponse {\n  type: "response";\n  id: string;           // 对应 request.id\n  result?: unknown;\n  error?: { code: number; message: string };\n}\n\ninterface BridgeNotification {\n  type: "notification";\n  method: string;\n  params: Record<string, unknown>;\n}\n\n// bridgeMain.ts: Bridge 主循环\nclass BridgeMain {\n  private transport: BridgeTransport;\n  private handlers = new Map<\n    string,\n    (params: Record<string, unknown>) => Promise<unknown>\n  >();\n\n  async start(port: number): Promise<void> {\n    this.transport = await createTransport(port);\n\n    // 注册内置处理器\n    this.handlers.set(\n      "chat/send", this.handleChatSend.bind(this)\n    );\n    this.handlers.set(\n      "file/read", this.handleFileRead.bind(this)\n    );\n    this.handlers.set(\n      "permission/request",\n      this.handlePermission.bind(this)\n    );\n\n    // 主消息循环\n    while (true) {\n      const msg = await this.transport.receive();\n      if (msg.type === "request") {\n        this.handleRequest(msg);\n      }\n    }\n  }\n\n  private async handleRequest(\n    req: BridgeRequest\n  ): Promise<void> {\n    const handler = this.handlers.get(req.method);\n    if (!handler) {\n      this.transport.send({\n        type: "response",\n        id: req.id,\n        error: { code: -1, message: "Unknown method" }\n      });\n      return;\n    }\n\n    try {\n      const result = await handler(req.params);\n      this.transport.send({\n        type: "response",\n        id: req.id,\n        result\n      });\n    } catch (err) {\n      this.transport.send({\n        type: "response",\n        id: req.id,\n        error: {\n          code: -1,\n          message: String(err)\n        }\n      });\n    }\n  }\n\n  // 向 IDE 发送通知（无需响应）\n  notify(method: string, params: Record<string, unknown>): void {\n    this.transport.send({\n      type: "notification",\n      method,\n      params\n    });\n  }\n}'},{title:"认证与 Teleport 环境迁移",language:"typescript",code:'// jwtUtils.ts: JWT 认证工具\ninterface JwtPayload {\n  sub: string;          // 用户 ID\n  deviceId: string;     // 设备指纹\n  workspaceId: string;  // 工作区 ID\n  exp: number;          // 过期时间\n  iat: number;          // 签发时间\n}\n\nfunction createJwt(\n  payload: JwtPayload,\n  secret: string\n): string {\n  const header = { alg: "HS256", typ: "JWT" };\n  const headerB64 = base64url(JSON.stringify(header));\n  const payloadB64 = base64url(JSON.stringify(payload));\n  const signature = hmacSHA256(\n    headerB64 + "." + payloadB64,\n    secret\n  );\n  return headerB64 + "." + payloadB64 + "." + signature;\n}\n\n// trustedDevice.ts: 设备信任\nclass TrustedDeviceManager {\n  private trustedDevices: Set<string>;\n\n  isTrusted(deviceId: string): boolean {\n    return this.trustedDevices.has(deviceId);\n  }\n\n  async trustDevice(deviceId: string): Promise<void> {\n    this.trustedDevices.add(deviceId);\n    await this.persistToFile();\n  }\n}\n\n// workSecret.ts: 工作区密钥\nfunction getWorkSecret(\n  workspacePath: string\n): string {\n  const secretPath = join(\n    workspacePath, ".claude", "work_secret"\n  );\n  if (existsSync(secretPath)) {\n    return readFileSync(secretPath, "utf-8");\n  }\n  // 首次使用，生成新密钥\n  const secret = randomBytes(32).toString("hex");\n  writeFileSync(secretPath, secret);\n  return secret;\n}\n\n// utils/teleport/gitBundle.ts: Git 仓库打包\nasync function createGitBundle(\n  repoPath: string\n): Promise<Buffer> {\n  // 1. 收集未推送的提交\n  const unpushed = await execGit(\n    repoPath, "log", "--oneline", "@{u}..HEAD"\n  );\n\n  // 2. 收集未暂存的更改\n  const stashResult = await execGit(\n    repoPath, "stash", "create"\n  );\n\n  // 3. 创建 bundle\n  const bundle = await execGit(\n    repoPath, "bundle", "create", "-",\n    "--all", "--not", "--remotes"\n  );\n\n  return bundle;\n}\n\n// utils/teleport/environments.ts: 环境快照\ninterface EnvironmentSnapshot {\n  nodeVersion: string;\n  pythonVersion: string;\n  envVars: Record<string, string>;\n  gitConfig: Record<string, string>;\n  installedTools: string[];\n  timestamp: number;\n}\n\nasync function captureEnvironment(): Promise<EnvironmentSnapshot> {\n  return {\n    nodeVersion: process.version,\n    pythonVersion: await getCommandOutput("python3 --version"),\n    envVars: filterSafeEnvVars(process.env),\n    gitConfig: await getGitConfig(),\n    installedTools: await detectInstalledTools(),\n    timestamp: Date.now()\n  };\n}'},{title:"Computer Use 与远程会话",language:"typescript",code:'// utils/computerUse: AI 计算机操控\ninterface ScreenCapture {\n  width: number;\n  height: number;\n  data: Buffer;         // PNG 图像数据\n  timestamp: number;\n}\n\ninterface ClickAction {\n  type: "click";\n  x: number;\n  y: number;\n  button: "left" | "right" | "middle";\n}\n\ninterface TypeAction {\n  type: "type";\n  text: string;\n  modifiers?: string[];\n}\n\ntype ComputerAction =\n  | ClickAction\n  | TypeAction\n  | { type: "screenshot" }\n  | { type: "scroll"; x: number; y: number; delta: number };\n\n// Computer Use MCP 服务器\nclass ComputerUseMcpServer {\n  getTools(): ToolDefinition[] {\n    return [\n      {\n        name: "computer_screenshot",\n        description: "截取当前屏幕截图",\n        inputSchema: { type: "object", properties: {} }\n      },\n      {\n        name: "computer_click",\n        description: "在指定坐标点击",\n        inputSchema: {\n          type: "object",\n          properties: {\n            x: { type: "number" },\n            y: { type: "number" },\n            button: { type: "string" }\n          }\n        }\n      },\n      {\n        name: "computer_type",\n        description: "输入文本内容",\n        inputSchema: {\n          type: "object",\n          properties: {\n            text: { type: "string" }\n          }\n        }\n      }\n    ];\n  }\n\n  async handleTool(\n    name: string,\n    params: Record<string, unknown>\n  ): Promise<unknown> {\n    switch (name) {\n      case "computer_screenshot":\n        return await captureScreen();\n      case "computer_click":\n        return await performClick(\n          params.x as number,\n          params.y as number,\n          (params.button as string) || "left"\n        );\n      case "computer_type":\n        return await typeText(params.text as string);\n      default:\n        throw new Error("Unknown tool: " + name);\n    }\n  }\n}\n\n// remote/RemoteSessionManager.ts: 远程会话管理\nclass RemoteSessionManager {\n  private sessions = new Map<string, RemoteSession>();\n\n  async createSession(\n    config: RemoteConfig\n  ): Promise<RemoteSession> {\n    // 1. 建立 WebSocket 连接\n    const ws = new SessionsWebSocket(config.endpoint);\n    await ws.connect();\n\n    // 2. JWT 认证\n    const token = createJwt({\n      sub: config.userId,\n      deviceId: config.deviceId,\n      workspaceId: config.workspaceId,\n      exp: Date.now() + 3600_000,\n      iat: Date.now()\n    }, config.secret);\n    await ws.authenticate(token);\n\n    // 3. 可选: Teleport 环境\n    if (config.teleport) {\n      const bundle = await createGitBundle(config.repoPath);\n      const env = await captureEnvironment();\n      await ws.send("teleport", { bundle, env });\n    }\n\n    const session: RemoteSession = {\n      id: generateSessionId(),\n      ws,\n      status: "connected",\n      createdAt: Date.now()\n    };\n\n    this.sessions.set(session.id, session);\n    return session;\n  }\n\n  // 断线重连\n  async reconnect(sessionId: string): Promise<void> {\n    const session = this.sessions.get(sessionId);\n    if (!session) throw new Error("Session not found");\n\n    await session.ws.reconnect();\n    session.status = "connected";\n  }\n}'}],interactive:'\n    <h3>Bridge 与远程集成交互式演练</h3>\n    <div class="interactive-demo">\n      <div class="step" data-step="1">\n        <h4>Step 1: IDE 建立 Bridge 连接</h4>\n        <p>VS Code 扩展启动时，通过本地端口与 Claude Code 进程建立 Bridge 连接。首次连接需要设备信任确认和 JWT 认证。</p>\n        <div class="code-highlight">\n          <pre>VS Code 扩展启动:\n  1. 发现本地 Claude Code 进程 (port: 7463)\n  2. 生成设备指纹: "device_abc123"\n  3. 首次连接: 弹出信任确认对话框\n  4. 用户确认 &#x2192; trustedDevice.trustDevice()\n  5. 创建 JWT: { sub: "user", deviceId: "device_abc123" }\n  6. Bridge 连接建立 &#x2714;</pre>\n        </div>\n      </div>\n      <div class="step" data-step="2">\n        <h4>Step 2: 消息双向通信</h4>\n        <p>IDE 通过 Bridge 发送用户消息，Claude Code 处理后通过通知推送结果。权限确认请求被转发到 IDE 显示对话框。</p>\n        <div class="code-highlight">\n          <pre>IDE &#x2192; Bridge: {\n  type: "request", id: "r1",\n  method: "chat/send",\n  params: { message: "重构 auth 模块" }\n}\n\nBridge &#x2192; IDE: {\n  type: "notification",\n  method: "chat/streaming",\n  params: { content: "我来分析..." }\n}\n\nBridge &#x2192; IDE: {\n  type: "request", id: "p1",\n  method: "permission/request",\n  params: { action: "write", path: "src/auth.ts" }\n}\nIDE &#x2192; Bridge: { type: "response", id: "p1", result: true }</pre>\n        </div>\n      </div>\n      <div class="step" data-step="3">\n        <h4>Step 3: Teleport 环境迁移</h4>\n        <p>用户需要将本地项目迁移到远程强力服务器上运行 Claude Code。Teleport 打包 Git 仓库和环境快照，通过 WebSocket 传送到远程实例。</p>\n        <div class="code-highlight">\n          <pre>Teleport 流程:\n  1. gitBundle: 打包未推送提交 + 未暂存更改 (2.3MB)\n  2. 环境快照: Node 20.x, Python 3.11, 12 个工具\n  3. WebSocket 传输 &#x2192; 远程服务器\n  4. 远程恢复: git unbundle + 环境变量设置\n  5. 远程 Claude Code 就绪 &#x2714;\n  6. 本地 &#x2192; 远程无缝切换</pre>\n        </div>\n      </div>\n      <div class="step" data-step="4">\n        <h4>Step 4: Computer Use 操控</h4>\n        <p>Claude Code 通过 Computer Use MCP 服务器直接操控桌面应用。截图识别界面元素，模拟点击和输入完成自动化任务。</p>\n        <div class="code-highlight">\n          <pre>Computer Use 操作序列:\n  1. computer_screenshot &#x2192; 截取桌面 (1920x1080)\n  2. AI 分析: 识别到 "Submit" 按钮 at (450, 320)\n  3. computer_click(450, 320, "left") &#x2192; 点击按钮\n  4. 等待页面加载...\n  5. computer_screenshot &#x2192; 验证操作结果\n  6. computer_type("测试数据") &#x2192; 填写表单\n  7. AI 确认: 任务完成 &#x2714;</pre>\n        </div>\n      </div>\n    </div>\n  '}};var m=t(6445);function h(n){let{layer:e,className:t}=n;return(0,o.jsx)("span",{className:(0,m.cn)("layer-".concat(e),"layer-badge",t),children:g.RR[e].label})}var x=t(2115);function v(n){var e,t;let{tabs:s}=n,[i,r]=(0,x.useState)(null!=(t=null==(e=s[0])?void 0:e.id)?t:""),l=(0,x.useCallback)(()=>{var n,e;for(let n=s.length-1;n>=0;n--){let e=document.getElementById("section-".concat(s[n].id));if(e&&e.getBoundingClientRect().top<=124)return void r(s[n].id)}r(null!=(e=null==(n=s[0])?void 0:n.id)?e:"")},[s]);return(0,x.useEffect)(()=>(window.addEventListener("scroll",l,{passive:!0}),l(),()=>window.removeEventListener("scroll",l)),[l]),(0,o.jsx)("div",{className:"sticky-tabs-bar",children:(0,o.jsx)("div",{className:"sticky-tabs-inner",children:s.map(n=>(0,o.jsxs)("button",{onClick:()=>(n=>{let e=document.getElementById("section-".concat(n));if(e){let n=e.getBoundingClientRect().top+window.scrollY-116;window.scrollTo({top:n,behavior:"smooth"})}})(n.id),className:(0,m.cn)("sticky-tab-item",i===n.id&&"sticky-tab-active"),children:[n.icon,n.label]},n.id))})})}var y=t(9418),b=t(3594),f=t(7503),k=t(323);y.A.registerLanguage("typescript",b.A),y.A.registerLanguage("ts",b.A),y.A.registerLanguage("tsx",b.A),y.A.registerLanguage("javascript",b.A),y.A.registerLanguage("js",b.A),y.A.registerLanguage("bash",f.A),y.A.registerLanguage("shell",f.A),y.A.registerLanguage("json",k.A);let T={typescript:"TypeScript",ts:"TypeScript",tsx:"TSX",javascript:"JavaScript",js:"JS",bash:"Bash",shell:"Shell",json:"JSON"};function S(n){var e;let{code:t,language:s="typescript",title:i}=n,[r,l]=(0,x.useState)(!1),a=t.trim(),d=(0,x.useMemo)(()=>{try{return y.A.highlight(a,{language:s}).value}catch(n){return a.replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;")}},[a,s]),c=a.split("\n").length,p=(0,x.useCallback)(async()=>{try{await navigator.clipboard.writeText(a),l(!0),setTimeout(()=>l(!1),2e3)}catch(e){let n=document.createElement("textarea");n.value=a,n.style.position="fixed",n.style.opacity="0",document.body.appendChild(n),n.select(),document.execCommand("copy"),document.body.removeChild(n),l(!0),setTimeout(()=>l(!1),2e3)}},[a]),g=null!=(e=T[s])?e:s.toUpperCase();return(0,o.jsxs)("div",{className:"code-block",children:[(0,o.jsxs)("div",{className:"code-block-header",children:[(0,o.jsxs)("div",{className:"code-block-dots",children:[(0,o.jsx)("span",{className:"code-dot code-dot-red"}),(0,o.jsx)("span",{className:"code-dot code-dot-yellow"}),(0,o.jsx)("span",{className:"code-dot code-dot-green"})]}),i&&(0,o.jsx)("span",{className:"code-block-title",children:i}),(0,o.jsx)("div",{className:"flex-1"}),(0,o.jsx)("span",{className:"code-lang-pill",children:g}),(0,o.jsxs)("button",{onClick:p,className:"code-copy-btn","aria-label":r?"已复制":"复制代码",title:r?"已复制":"复制代码",children:[r?(0,o.jsx)("svg",{width:"14",height:"14",viewBox:"0 0 24 24",fill:"none",stroke:"currentColor",strokeWidth:"2.5",strokeLinecap:"round",strokeLinejoin:"round",children:(0,o.jsx)("polyline",{points:"20 6 9 17 4 12"})}):(0,o.jsxs)("svg",{width:"14",height:"14",viewBox:"0 0 24 24",fill:"none",stroke:"currentColor",strokeWidth:"2",strokeLinecap:"round",strokeLinejoin:"round",children:[(0,o.jsx)("rect",{x:"9",y:"9",width:"13",height:"13",rx:"2",ry:"2"}),(0,o.jsx)("path",{d:"M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"})]}),(0,o.jsx)("span",{className:"code-copy-label",children:r?"已复制":"复制"})]})]}),(0,o.jsxs)("div",{className:"code-block-body",children:[(0,o.jsx)("div",{className:"code-line-numbers","aria-hidden":"true",children:Array.from({length:c},(n,e)=>(0,o.jsx)("span",{children:e+1},e))}),(0,o.jsx)("pre",{className:"code-block-pre",children:(0,o.jsx)("code",{className:"hljs language-".concat(s),dangerouslySetInnerHTML:{__html:d}})})]})]})}var C=t(8391);let P={c01:(0,x.lazy)(()=>Promise.all([t.e(462),t.e(809)]).then(t.bind(t,7809))),c02:(0,x.lazy)(()=>Promise.all([t.e(462),t.e(185)]).then(t.bind(t,8185))),c03:(0,x.lazy)(()=>Promise.all([t.e(462),t.e(943)]).then(t.bind(t,9943))),c04:(0,x.lazy)(()=>Promise.all([t.e(462),t.e(958)]).then(t.bind(t,5958))),c05:(0,x.lazy)(()=>Promise.all([t.e(462),t.e(222)]).then(t.bind(t,7222))),c06:(0,x.lazy)(()=>Promise.all([t.e(462),t.e(94)]).then(t.bind(t,6094))),c07:(0,x.lazy)(()=>Promise.all([t.e(462),t.e(724)]).then(t.bind(t,1724))),c08:(0,x.lazy)(()=>Promise.all([t.e(462),t.e(244)]).then(t.bind(t,5244))),c09:(0,x.lazy)(()=>Promise.all([t.e(462),t.e(719)]).then(t.bind(t,3719))),c10:(0,x.lazy)(()=>Promise.all([t.e(462),t.e(368)]).then(t.bind(t,1368))),c11:(0,x.lazy)(()=>Promise.all([t.e(462),t.e(355)]).then(t.bind(t,4593))),c12:(0,x.lazy)(()=>Promise.all([t.e(462),t.e(290)]).then(t.bind(t,1290))),c13:(0,x.lazy)(()=>Promise.all([t.e(462),t.e(424)]).then(t.bind(t,7424))),c14:(0,x.lazy)(()=>Promise.all([t.e(462),t.e(533)]).then(t.bind(t,2533))),c15:(0,x.lazy)(()=>Promise.all([t.e(462),t.e(575)]).then(t.bind(t,5575))),c16:(0,x.lazy)(()=>Promise.all([t.e(462),t.e(824)]).then(t.bind(t,2824))),c17:(0,x.lazy)(()=>Promise.all([t.e(462),t.e(707)]).then(t.bind(t,1707))),c18:(0,x.lazy)(()=>Promise.all([t.e(462),t.e(11)]).then(t.bind(t,1392))),c19:(0,x.lazy)(()=>Promise.all([t.e(462),t.e(230)]).then(t.bind(t,5230))),c20:(0,x.lazy)(()=>Promise.all([t.e(462),t.e(52)]).then(t.bind(t,2052)))};function w(n){let{chapterId:e}=n,t=P[e];return t?(0,o.jsxs)("div",{className:"rounded-xl border border-[var(--color-border)] bg-[var(--color-bg)] overflow-hidden",children:[(0,o.jsxs)("div",{className:"flex items-center gap-2 px-5 py-3 border-b border-[var(--color-border)] bg-[var(--color-bg-secondary)]",children:[(0,o.jsx)(r.A,{className:"h-4 w-4 text-[var(--color-accent)]"}),(0,o.jsx)("span",{className:"text-sm font-semibold text-[var(--color-text-secondary)]",children:"互动演示"})]}),(0,o.jsx)("div",{className:"p-5",children:(0,o.jsx)(x.Suspense,{fallback:(0,o.jsxs)("div",{className:"flex flex-col gap-3 animate-pulse",children:[(0,o.jsx)("div",{className:"h-4 w-3/4 rounded bg-[var(--color-bg-tertiary)]"}),(0,o.jsx)("div",{className:"h-40 rounded-lg bg-[var(--color-bg-tertiary)]"}),(0,o.jsx)("div",{className:"h-4 w-1/2 rounded bg-[var(--color-bg-tertiary)]"})]}),children:(0,o.jsx)(t,{})})})]}):null}let A=[{id:"learn",label:"学习",icon:(0,o.jsx)(l.A,{className:"h-4 w-4"})},{id:"architecture",label:"架构",icon:(0,o.jsx)(a.A,{className:"h-4 w-4"})},{id:"source",label:"源码",icon:(0,o.jsx)(d.A,{className:"h-4 w-4"})},{id:"interactive",label:"互动",icon:(0,o.jsx)(r.A,{className:"h-4 w-4"})}];function M(n){let{chapterId:e}=n,t=g.u$[e],s=u[e];if(!t||!s)return(0,o.jsxs)("div",{className:"flex flex-col items-center justify-center py-32 text-center",children:[(0,o.jsx)("div",{className:"text-6xl font-black text-[var(--color-text-muted)] opacity-20",children:"404"}),(0,o.jsx)("h1",{className:"mt-4 text-2xl font-bold text-[var(--color-text)]",children:"章节未找到"}),(0,o.jsxs)("p",{className:"mt-2 text-[var(--color-text-muted)]",children:["找不到章节 ",(0,o.jsx)("code",{className:"px-1.5 py-0.5 rounded bg-[var(--color-bg-tertiary)] text-sm font-mono",children:e})]}),(0,o.jsxs)(i(),{href:"/",className:"mt-6 inline-flex items-center gap-2 rounded-lg bg-[var(--color-accent)] px-4 py-2 text-sm font-semibold text-white transition-colors hover:bg-[var(--color-accent-hover)]",children:[(0,o.jsx)(c.A,{className:"h-4 w-4"}),"返回首页"]})]});let m=g.pt.indexOf(e),x=m>0?g.pt[m-1]:null,y=m<g.pt.length-1?g.pt[m+1]:null,b=x?g.u$[x]:null,f=y?g.u$[y]:null,k=e.replace("c","").padStart(2,"0");return(0,o.jsxs)("div",{className:"pb-20",children:[(0,o.jsxs)("header",{className:"mb-6",children:[(0,o.jsxs)(i(),{href:"/",className:"group mb-6 inline-flex items-center gap-1.5 text-sm font-medium text-[var(--color-text-muted)] transition-colors hover:text-[var(--color-accent)]",children:[(0,o.jsx)(c.A,{className:"h-3.5 w-3.5 transition-transform group-hover:-translate-x-0.5"}),"返回目录"]}),(0,o.jsxs)("div",{className:"flex items-center gap-3 mb-3",children:[(0,o.jsx)(h,{layer:t.layer}),(0,o.jsxs)("span",{className:"text-sm font-mono font-semibold text-[var(--color-text-muted)]",children:["第 ",k," 章"]})]}),(0,o.jsx)("h1",{className:"text-3xl font-extrabold tracking-tight text-[var(--color-text)] sm:text-4xl",children:t.title}),(0,o.jsx)("p",{className:"mt-2 text-lg text-[var(--color-text-secondary)]",children:t.subtitle}),(0,o.jsxs)("div",{className:"relative mt-6 rounded-xl border border-[var(--color-border)] overflow-hidden",children:[(0,o.jsx)("div",{className:"absolute inset-0 opacity-[0.08]",style:{background:"linear-gradient(135deg, var(--layer-".concat(t.layer,"), var(--color-accent))")}}),(0,o.jsx)("div",{className:"relative px-5 py-4",children:(0,o.jsxs)("div",{className:"flex items-start gap-3",children:[(0,o.jsx)("div",{className:"mt-0.5 flex h-8 w-8 shrink-0 items-center justify-center rounded-lg",style:{backgroundColor:"var(--layer-".concat(t.layer,"-light)")},children:(0,o.jsx)(l.A,{className:"h-4 w-4",style:{color:"var(--layer-".concat(t.layer,")")}})}),(0,o.jsxs)("div",{children:[(0,o.jsx)("div",{className:"text-xs font-semibold uppercase tracking-wider text-[var(--color-text-muted)] mb-1",children:"核心洞察"}),(0,o.jsx)("p",{className:"text-[15px] font-medium leading-relaxed text-[var(--color-text)]",children:t.keyInsight})]})]})})]})]}),(0,o.jsx)(v,{tabs:A}),(0,o.jsxs)("section",{id:"section-learn",className:"chapter-section",children:[(0,o.jsxs)("div",{className:"section-header",children:[(0,o.jsx)("div",{className:"section-header-icon bg-blue-500/10",children:(0,o.jsx)(l.A,{className:"h-4 w-4 text-blue-500"})}),(0,o.jsxs)("div",{children:[(0,o.jsx)("h2",{className:"section-header-title",children:"学习"}),(0,o.jsx)("p",{className:"section-header-subtitle",children:"概念讲解与核心设计分析"})]})]}),(0,o.jsx)("div",{className:"prose-custom",dangerouslySetInnerHTML:{__html:s.content}})]}),(0,o.jsxs)("section",{id:"section-architecture",className:"chapter-section",children:[(0,o.jsxs)("div",{className:"section-header",children:[(0,o.jsx)("div",{className:"section-header-icon bg-emerald-500/10",children:(0,o.jsx)(a.A,{className:"h-4 w-4 text-emerald-500"})}),(0,o.jsxs)("div",{children:[(0,o.jsx)("h2",{className:"section-header-title",children:"架构"}),(0,o.jsx)("p",{className:"section-header-subtitle",children:"模块关系与设计决策"})]})]}),(0,o.jsx)("div",{className:"prose-custom",dangerouslySetInnerHTML:{__html:s.architecture}})]}),(0,o.jsxs)("section",{id:"section-source",className:"chapter-section",children:[(0,o.jsxs)("div",{className:"section-header",children:[(0,o.jsx)("div",{className:"section-header-icon bg-amber-500/10",children:(0,o.jsx)(d.A,{className:"h-4 w-4 text-amber-500"})}),(0,o.jsxs)("div",{children:[(0,o.jsx)("h2",{className:"section-header-title",children:"源码"}),(0,o.jsxs)("p",{className:"section-header-subtitle",children:["共 ",s.codeExamples.length," 个关键代码示例"]})]})]}),s.codeExamples.length>0?(0,o.jsx)("div",{className:"space-y-8",children:s.codeExamples.map((n,e)=>(0,o.jsxs)("div",{className:"relative",children:[(0,o.jsxs)("div",{className:"flex items-center gap-3 mb-3",children:[(0,o.jsx)("span",{className:"flex h-6 w-6 items-center justify-center rounded-full bg-[var(--color-accent)] text-[10px] font-bold text-white shadow-sm",children:String(e+1).padStart(2,"0")}),e<s.codeExamples.length-1&&(0,o.jsx)("div",{className:"h-px flex-1 bg-gradient-to-r from-[var(--color-border)] to-transparent"})]}),(0,o.jsx)(S,{code:n.code,language:n.language,title:n.title})]},e))}):(0,o.jsxs)("div",{className:"rounded-xl border border-dashed border-[var(--color-border)] py-12 text-center",children:[(0,o.jsx)(d.A,{className:"mx-auto h-8 w-8 text-[var(--color-text-muted)] opacity-40"}),(0,o.jsx)("p",{className:"mt-3 text-sm text-[var(--color-text-muted)]",children:"暂无源码示例"})]})]}),(0,o.jsxs)("section",{id:"section-interactive",className:"chapter-section",children:[(0,o.jsxs)("div",{className:"section-header",children:[(0,o.jsx)("div",{className:"section-header-icon bg-purple-500/10",children:(0,o.jsx)(r.A,{className:"h-4 w-4 text-purple-500"})}),(0,o.jsxs)("div",{children:[(0,o.jsx)("h2",{className:"section-header-title",children:"互动"}),(0,o.jsx)("p",{className:"section-header-subtitle",children:"步进式流程演示"})]})]}),(0,o.jsx)("div",{className:"mb-8",children:(0,o.jsx)(w,{chapterId:e})}),(0,o.jsx)("div",{className:"prose-custom",dangerouslySetInnerHTML:{__html:s.interactive}})]}),t.sourceFiles&&t.sourceFiles.length>0&&(0,o.jsx)("section",{className:"mt-12 mb-10",children:(0,o.jsxs)(C.Zp,{children:[(0,o.jsxs)("div",{className:"flex items-center gap-2 mb-3",children:[(0,o.jsx)(d.A,{className:"h-4 w-4 text-[var(--color-text-muted)]"}),(0,o.jsx)("h3",{className:"text-sm font-semibold text-[var(--color-text-secondary)]",children:"相关源文件"})]}),(0,o.jsx)("div",{className:"flex flex-wrap gap-2",children:t.sourceFiles.map(n=>(0,o.jsx)("span",{className:"inline-flex items-center rounded-md border border-[var(--color-border)] bg-[var(--color-bg-secondary)] px-2.5 py-1 text-xs font-mono text-[var(--color-text-muted)] transition-colors hover:text-[var(--color-text-secondary)] hover:border-[var(--color-accent)]/30",children:n},n))})]})}),(0,o.jsxs)("nav",{className:"grid grid-cols-2 gap-4 pt-6 border-t border-[var(--color-border)]",children:[b?(0,o.jsxs)(i(),{href:"/".concat(x),className:"group flex items-center gap-3 rounded-xl border border-[var(--color-border)] bg-[var(--color-bg)] px-4 py-3 transition-all hover:border-[var(--color-accent)]/30 hover:shadow-md",children:[(0,o.jsx)(c.A,{className:"h-4 w-4 shrink-0 text-[var(--color-text-muted)] transition-transform group-hover:-translate-x-0.5"}),(0,o.jsxs)("div",{className:"min-w-0",children:[(0,o.jsx)("div",{className:"text-xs text-[var(--color-text-muted)]",children:"上一章"}),(0,o.jsx)("div",{className:"truncate text-sm font-semibold text-[var(--color-text)] group-hover:text-[var(--color-accent)] transition-colors",children:b.title})]})]}):(0,o.jsxs)(i(),{href:"/",className:"group flex items-center gap-3 rounded-xl border border-[var(--color-border)] bg-[var(--color-bg)] px-4 py-3 transition-all hover:border-[var(--color-accent)]/30 hover:shadow-md",children:[(0,o.jsx)(c.A,{className:"h-4 w-4 shrink-0 text-[var(--color-text-muted)] transition-transform group-hover:-translate-x-0.5"}),(0,o.jsxs)("div",{className:"min-w-0",children:[(0,o.jsx)("div",{className:"text-xs text-[var(--color-text-muted)]",children:"返回"}),(0,o.jsx)("div",{className:"truncate text-sm font-semibold text-[var(--color-text)] group-hover:text-[var(--color-accent)] transition-colors",children:"目录首页"})]})]}),f?(0,o.jsxs)(i(),{href:"/".concat(y),className:"group flex items-center justify-end gap-3 rounded-xl border border-[var(--color-border)] bg-[var(--color-bg)] px-4 py-3 transition-all hover:border-[var(--color-accent)]/30 hover:shadow-md text-right",children:[(0,o.jsxs)("div",{className:"min-w-0",children:[(0,o.jsx)("div",{className:"text-xs text-[var(--color-text-muted)]",children:"下一章"}),(0,o.jsx)("div",{className:"truncate text-sm font-semibold text-[var(--color-text)] group-hover:text-[var(--color-accent)] transition-colors",children:f.title})]}),(0,o.jsx)(p.A,{className:"h-4 w-4 shrink-0 text-[var(--color-text-muted)] transition-transform group-hover:translate-x-0.5"})]}):(0,o.jsxs)(i(),{href:"/",className:"group flex items-center justify-end gap-3 rounded-xl border border-[var(--color-border)] bg-[var(--color-bg)] px-4 py-3 transition-all hover:border-[var(--color-accent)]/30 hover:shadow-md text-right",children:[(0,o.jsxs)("div",{className:"min-w-0",children:[(0,o.jsx)("div",{className:"text-xs text-[var(--color-text-muted)]",children:"完成"}),(0,o.jsx)("div",{className:"truncate text-sm font-semibold text-[var(--color-text)] group-hover:text-[var(--color-accent)] transition-colors",children:"返回目录"})]}),(0,o.jsx)(p.A,{className:"h-4 w-4 shrink-0 text-[var(--color-text-muted)] transition-transform group-hover:translate-x-0.5"})]})]})]})}},6445:(n,e,t)=>{"use strict";function o(){for(var n=arguments.length,e=Array(n),t=0;t<n;t++)e[t]=arguments[t];return e.filter(Boolean).join(" ")}t.d(e,{cn:()=>o})},6549:(n,e,t)=>{"use strict";t.d(e,{Jj:()=>r,RR:()=>o,pt:()=>i,u$:()=>s});let o={foundation:{label:"启动与基础",color:"blue"},engine:{label:"核心引擎",color:"emerald"},tools:{label:"工具系统",color:"amber"},security:{label:"安全与权限",color:"purple"},integration:{label:"扩展与集成",color:"red"},"ui-state":{label:"界面与状态",color:"slate"}},s={c01:{id:"c01",title:"启动引擎",subtitle:"从命令行到就绪态",coreAddition:"CLI 解析 → 并行预取 → 初始化管道",keyInsight:"通过并行预取和延迟初始化，将启动时间压缩到毫秒级",layer:"foundation",sourceFiles:["main.tsx","entrypoints/init.ts","replLauncher.tsx"]},c02:{id:"c02",title:"配置系统",subtitle:"七层配置优先级",coreAddition:"CLI → 策略 → 远程 → Flag → 项目 → 用户 → 全局",keyInsight:"7 层配置合并确保企业策略始终优先，同时保持开发者灵活性",layer:"foundation",sourceFiles:["utils/settings/","utils/config.ts","schemas/"]},c03:{id:"c03",title:"查询引擎",subtitle:"SDK 与 Headless 的统一",coreAddition:"QueryEngine 类 — 会话生命周期管理",keyInsight:"QueryEngine 是 CLI REPL 和 SDK 编程接口的统一抽象层",layer:"engine",sourceFiles:["QueryEngine.ts"]},c04:{id:"c04",title:"查询循环",subtitle:"AsyncGenerator 驱动的心脏",coreAddition:"while(true) 循环 \xd7 10 步迭代管道",keyInsight:"AsyncGenerator 让流式事件、工具调用、错误恢复自然串联为一个无限循环",layer:"engine",sourceFiles:["query.ts","query/"]},c05:{id:"c05",title:"API 客户端",subtitle:"流式响应与智能重试",coreAddition:"流式处理 \xd7 Thinking 模式 \xd7 缓存管理",keyInsight:"通过 Prompt Cache 断点检测和预连接，最大限度减少首 token 延迟",layer:"engine",sourceFiles:["services/api/claude.ts","services/api/"]},c06:{id:"c06",title:"工具架构",subtitle:"30+ 属性的 Tool 接口",coreAddition:"Tool 类型定义 \xd7 工具注册表 \xd7 特性门控",keyInsight:"编译期死代码消除(bun:bundle DCE)实现零运行时开销的特性门控",layer:"tools",sourceFiles:["Tool.ts","tools.ts"]},c07:{id:"c07",title:"工具执行引擎",subtitle:"并发编排与流式执行",coreAddition:"StreamingToolExecutor \xd7 工具分区 \xd7 结果预算",keyInsight:"安全工具并行执行，非安全工具串行隔离，通过兄弟 AbortController 实现级联中止",layer:"tools",sourceFiles:["services/tools/"]},c08:{id:"c08",title:"内置工具全景",subtitle:"Bash\xb7文件\xb7搜索\xb7LSP",coreAddition:"42+ 工具的完整实现图谱",keyInsight:"Bash 工具通过 AST 解析实现语义级命令分类，区分只读/破坏性/搜索操作",layer:"tools",sourceFiles:["tools/BashTool/","tools/FileEditTool/","tools/GrepTool/","tools/LSPTool/"]},c09:{id:"c09",title:"计划与工作流",subtitle:"结构化的任务推进",coreAddition:"计划模式 \xd7 技能系统 \xd7 用户交互",keyInsight:"ToolSearch 延迟加载机制让 40+ 工具仅在需要时才进入上下文窗口",layer:"tools",sourceFiles:["tools/EnterPlanModeTool/","tools/SkillTool/","tools/ToolSearchTool/"]},c10:{id:"c10",title:"权限系统",subtitle:"五层安全决策",coreAddition:"规则 → Hook → 分类器 → 沙箱 → 用户确认",keyInsight:"YOLO 自动模式通过转录分类器实现无人值守下的安全执行",layer:"security",sourceFiles:["utils/permissions/"]},c11:{id:"c11",title:"Hook 生命周期",subtitle:"可编程的拦截点",coreAddition:"PreToolUse → PostToolUse → Stop \xd7 三种执行方式",keyInsight:"Hook 系统让用户在不修改源码的情况下，完全控制工具执行的前后逻辑",layer:"security",sourceFiles:["utils/hooks/"]},c12:{id:"c12",title:"MCP 集成",subtitle:"五种传输协议",coreAddition:"Stdio / SSE / HTTP / WebSocket / SDK 传输",keyInsight:"MCP 将外部服务统一抽象为工具/资源/命令三种原语，实现无限扩展",layer:"integration",sourceFiles:["services/mcp/"]},c13:{id:"c13",title:"内存与上下文",subtitle:"四级压缩管道",coreAddition:"CLAUDE.md \xd7 MEMORY.md \xd7 Snip → Micro → Collapse → Auto",keyInsight:"渐进式压缩策略在保留关键上下文的同时，将 token 消耗控制在预算内",layer:"integration",sourceFiles:["memdir/","services/compact/","services/SessionMemory/"]},c14:{id:"c14",title:"子代理系统",subtitle:"递归的 query() 调用",coreAddition:"Fork 模型 \xd7 MCP 共享 \xd7 内置代理",keyInsight:"子代理通过递归调用 query() 实现，与主代理共享 MCP 连接但隔离上下文",layer:"integration",sourceFiles:["tools/AgentTool/","tools/SendMessageTool/"]},c15:{id:"c15",title:"Teams 多代理协作",subtitle:"Swarm 编排架构",coreAddition:"团队创建 \xd7 后端适配 \xd7 权限同步 \xd7 记忆共享",keyInsight:"Swarm 通过 iTerm/Tmux/InProcess 三种后端，支持从本地到分布式的多代理协作",layer:"integration",sourceFiles:["tools/TeamCreateTool/","utils/swarm/","coordinator/"]},c16:{id:"c16",title:"任务与调度",subtitle:"五种任务类型",coreAddition:"Main / Agent / Shell / Teammate / Dream \xd7 Cron",keyInsight:'DreamTask 在后台静默执行，实现了 "AI 做梦" 式的异步知识整理',layer:"integration",sourceFiles:["tasks/","tools/ScheduleCronTool/","services/autoDream/"]},c17:{id:"c17",title:"Skills 与 Plugins",subtitle:"可扩展的能力生态",coreAddition:"技能加载 \xd7 插件市场 \xd7 83+ 斜杠命令",keyInsight:"Skills 通过 Frontmatter 声明 Hook，让 Markdown 文件具有编程能力",layer:"integration",sourceFiles:["skills/","plugins/","commands/"]},c18:{id:"c18",title:"状态管理",subtitle:"DeepImmutable 状态树",coreAddition:"AppState 300+ 字段 \xd7 createStore \xd7 副作用",keyInsight:"DeepImmutable 类型约束 + 函数式更新，在无框架依赖下实现可预测状态管理",layer:"ui-state",sourceFiles:["state/","bootstrap/state.ts"]},c19:{id:"c19",title:"终端 UI 框架",subtitle:"自定义 Ink 的 React 终端",coreAddition:"Reconciler → DOM → Renderer \xd7 Flexbox \xd7 Vim",keyInsight:"通过自定义 React Reconciler，将 React 组件树渲染为 ANSI 终端输出",layer:"ui-state",sourceFiles:["ink/","components/","screens/","vim/"]},c20:{id:"c20",title:"Bridge 与远程集成",subtitle:"IDE 双向通信与远程控制",coreAddition:"Bridge 协议 \xd7 Teleport \xd7 Computer Use \xd7 远程会话",keyInsight:"Bridge 将 Claude Code 从独立 CLI 升级为 IDE 的智能内核",layer:"ui-state",sourceFiles:["bridge/","remote/","utils/teleport/","utils/computerUse/"]}},i=["c01","c02","c03","c04","c05","c06","c07","c08","c09","c10","c11","c12","c13","c14","c15","c16","c17","c18","c19","c20"],r=[{layer:"foundation",chapters:["c01","c02"]},{layer:"engine",chapters:["c03","c04","c05"]},{layer:"tools",chapters:["c06","c07","c08","c09"]},{layer:"security",chapters:["c10","c11"]},{layer:"integration",chapters:["c12","c13","c14","c15","c16","c17"]},{layer:"ui-state",chapters:["c18","c19","c20"]}]},8391:(n,e,t)=>{"use strict";t.d(e,{Zp:()=>i});var o=t(5155),s=t(6445);function i(n){let{children:e,className:t,hover:i}=n;return(0,o.jsx)("div",{className:(0,s.cn)("rounded-xl border border-[var(--color-border)] bg-[var(--color-bg)] p-5 transition-theme",i&&"hover:border-[var(--color-accent)]/30 hover:shadow-lg hover:shadow-[var(--color-accent)]/5 transition-all duration-200",t),children:e})}}},n=>{n.O(0,[619,620,441,255,358],()=>n(n.s=4032)),_N_E=n.O()}]);